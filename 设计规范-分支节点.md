# 民国人生 · 结构化分支设计规范（v1）
> 参照现代项目《结构化社会人生模拟》九条规则，改造为民国版；本文件是**设计源数据规范**，供 `choices-data.js` / 引擎逐步落地。
> 约定：`HI` 史实权威 / `MID` 需核实 / `LO` 仅方向。凡运行时才能确定的值，一律显式写 `unknown`，**禁止编造**。

---

## 第 0 部分：九条规则的民国映射（方法论）

| 现代规则 | 民国落地形态 | 已有基建 / 需新增 |
|---|---|---|
| 共同父快照 | 每个抉择节点冻结一份 `P` 快照（人物+六维+四资源+echo+关系+照护+未知项），所有分支逐字段继承 | 需新增：快照冻结机制（当前引擎是连续 G，需在情境弹出时 snapshot） |
| 人物主体性 | 病母/妻/保长/东家/同乡是独立主体，有自己的目标与拒绝/退出权；玩家决定≠他人同意 | 需新增：他人 `consent` / `response` 字段，不可被玩家动作直接写真 |
| 概念分离 | 征召通知≠被抓走≠入伍≠上前线≠士兵身份；报价≠现钱≠到手；婚约≠婚姻≠对方同意留守 | 部分已有（gate/roll 区分资格与结果），需补"过程态"字段 |
| 社会制度 | 每个结果由具体机制产生：保甲摊派、名额配额、买丁费、二五减租、庚款名额、金圆券兑换…带地区+年份版本 | 已有 `eraMultiplier`、事件 `year`/`gate`；需补"名额容量"类机制 |
| 连续人生 | 每个选择展开 15 段（见下模板），禁止从"选择"直跳"成/败结局" | 需扩引擎：当前是"选项→一次结算"，需支持多阶段随时间展开 |
| 有限资源 | 生计/教育/婚育/照护/健康共享同一个人的：现金·时间(四时段)·精神(spiritMax)·健康·地点·信息·制度资格 | 已有（四时段+精神+边际递减），符合 |
| 反价值排名 | 不产出人生分/孝顺分/圆满结局；只存可观察事实、人物解释、关系影响、仍开放的未来 | 需改：现有 `endingVerdict` 有 sum 分级，应改为**事实陈述**而非评分 |
| 时间与死亡 | 人物只知当时可知信息，不得用后来的沦陷/通胀倒填决策；死亡须"发生+确认"，死后不再产生动作 | 需新增：信息时点校验 + 死亡确认状态机 |
| 输出格式 | 13 元素（本文件每个节点严格照此写） | — |

### 15 段连续人生模板（每条分支都要走一遍）
`①选择前审计 ②信息披露 ③他人独立回应 ④资格与可行性 ⑤实际合同/行动 ⑥第一阶段结果 ⑦普通生活窗口 ⑧外部冲击 ⑨多系统冲突 ⑩修订/暂停/退出 ⑪一年后 ⑫五年后 ⑬更长期 ⑭新状态快照 ⑮继续/修改/退出/重入`

> 当前项目里，主家庭线已经把这 15 段进一步压缩成一条更便于落地的数据模板：`前提/入口抉择 → 跨年活法 → 战时回应 → 战后几年中继 → 风声再紧或制度冲击 → 1949 去留/后半生安放 → 终局事实回收`。凡是 `江南沈家 / 上海工商家 / 苏北贫农家` 这种主家庭树，都应尽量补出这条中继链，而不是从战时直接跳到终局。
> 运行时总表补充：为避免后续又回到“只盯一棵成熟树的单枝修边”，现统一先看 `window.__DEMO.inspectWholeGameProgressBundle()`。它会共用同一批预计算结果，把 `整局阶段 / 九行模板迁移分布 / 九行主链蓝图分布 / 整局完工账本硬指标 / 尾部 route-local` 压成一份整局汇总，先回答“这一步是不是在补整局”。若要继续下钻，再按这份汇总给出的前沿层去读 `window.__DEMO.inspectWholeGameCompletionLedger()`、`window.__DEMO.inspectWholeGameMainlineBlueprint()`、`window.__DEMO.inspectWholeGameTemplateMigrationBoard()`；若确实需要候选级 `同键/同骨架/compare-only` 证据阶段，再显式下钻 `window.__DEMO.inspectTopRouteLocalCandidateBundle()`。后续每次补新树或新路径前，应先看这份整局汇总，确认该步是在补整局骨架、模板迁移、主系统，还是仍只是在细化单条 route-local。
> 九行作战板补充：若当前问题不是“整局指标是否继续成立”，而是“苏北样板现在迁到了哪一行、下一步该补哪棵树/哪条路径才真会改变整局完整度”，再优先看 `window.__DEMO.inspectWholeGameDesignPlaybook()`。它会按 9 行主家庭树/主路径并排返回 `当前角色 / 四层模板 / 证据密度 / 独立轨 / 下一步`，把“整局完成度”进一步翻成逐行可执行的模板作战板，避免只看到总数已齐，却不知道该补哪一行。

> HUD 模板补充：主家庭线不应只在终局里回收“制度冲击”，还应在运行态显性给出一层 `制度过程态`。当前 `江南沈家 / 上海工商家` 已把这层收敛成保守口径：前战准备是否已起、战时/战后中继是否在压回、风声或金圆券后的收束是否已启动。后续补新家庭时，也应优先沿这套“阶段态 HUD”写法推进，而不是只堆年度事件说明。

> 主家庭独立新轨补充：当一条主家庭线已经具备基本闭环后，下一层优先不是继续润色单一终局句，而是把它往 `action / year event / HUD / ending ledger` 四层同步推进。当前 `江南沈家 / 上海工商家` 已开始沿这条规则继续下沉：例如把“守书脉 / 护家门 / 续女学 / 分口粮 / 查仓单 / 跑会馆”压成真正可反复计划的年头动作，并给它们补对应的 `前战账 / 战后旧账 / 风声前夜账` 年度事件；`纱厂女工` 这轮也沿同一口径补出了 `夜校字路 -> 战时代写/认票据 -> 后半生代写记账细活` 这一条非确认型独立后半生轨，并为它补上 `战后工账与口粮票旧账 / 代写信与记工账余账` 两条分流专属余账。当矩阵里已经存在同一家族或同一路径的多个后半生代表态时，下一步还应继续把这些不同收口逐条补进自然回放，避免长期只靠一种代表活法在运行时“被验到”。若某主链分流已经有专属动作，但 HUD 的 `跨年余账` 仍只回退到通用 `战后旧账`，下一步应优先补**分流专属的中继年度事件**，而不是回头只抛光终局句。后续补其他主家庭时，也应优先沿这一口径判断“这一步是否让整盘更完整”。
> **成熟模板与前沿分离补充**：当某组 structured unknown 已在 `window.__DEMO.inspectUnknownStructurePortfolio()` 里达到 `allAxesReady`，它仍属于整局模板库存，但不应继续自动占据 atlas / 看板的“当前前沿”。`window.__DEMO.inspectFamilyRouteAtlasCoverage()` 与 `window.__DEMO.inspectGlobalMainlineBoard()` 现已把“已成熟模板”与“仍待推进的 unknown / route-local 候选”分开：前者继续保留在库存摘要里，后者才继续影响 `completionPriorityLabel`。这样当 `return-route-landing / draft-parameter-boundary / gold-preservation-boundary / long-confirm-truth` 已经成熟后，下一步就会自然转去 `上海工商家 / 江南沈家 / 苏北留乡主分流` 这类仍挂着 route-local 候选的主家庭/主路径，而不是继续被 ready bucket 虚高排序。
> **主体牵制锚点门路骨架补充**：当 `江南沈家 / 上海工商家 / 纱厂女工 / 苏北` 已经出现“家门/婚家锚点 + 外部门路”这类共享骨架时，不应继续只靠 `supportCluster` 或字面主体组合判断它是不是 route-local。运行时现新增 `window.__DEMO.inspectTensionAnchorConnectionCoverage()`，专门把“家门锚点是否已经和外部门路形成共享骨架”压成一层中间模板；随后 `inspectRouteLocalPromotionSignals()` / `inspectRouteLocalCandidateBlockers()` 会优先把这层作为 shared template 证据，用来区分“整条家门/门路骨架已共享，只剩安身细分仍保守留在线内”与“连骨架都还没共享，必须继续保留 route-local”。
> **主体牵制主家庭比较模板补充**：当 `江南沈家 / 上海工商家 / 苏北 / 难民` 已经进一步出现“家门/婚家锚点 + 安身簇 + 外部门路”这类共享中层骨架时，也不应继续只停在“锚点门路骨架已共享”这一层。运行时现新增 `window.__DEMO.inspectTensionFamilyComboCoverage()`，专门把这类 `familyAnchor + supportCluster + connections` 压成一层 **主家庭比较模板矩阵**；随后 `inspectRouteLocalCandidateBlockers()` 会优先把 `shared-family-combo-template-but-local-detail` 解释成“主家庭骨架已共享，只剩更细的安身/账房/工友组合仍 route-local”，避免把整树之间已经共模的部分继续误判成全新细枝。

> 辅助主体 profile 补充：`病母 / 妻 / 婚家 / 账房 / 工友家口` 不应分成“苏北靠特判、沈家/顾家靠规则表”两套世界。当前运行时已把 `subeipoor`（苏北贫农家 / 佃农 / 士兵）这一组的 `病母 / 妻` 也收进与 `jiangnanshen / shanghaigongshang / millworker / refugee` 同口径的 `AUX_SUBJECT_RULES` 式辅助主体层；此前已把共享 `海外多年轨` 收成 `overseas` profile，用 `旧同学 / 旧门路 / 亲眷` 这一层非家庭主体去承接 `abroad:*` 多年回响；随后又把同一个 `subjects.connections` 扩到 `江南沈家 / 上海工商家` 的**非海外主轨**，去承接 `门生 / 学会名帖 / 报馆旧交 / 同业会馆 / 熟客旧交 / 赈济联系人` 这些外部门路；本轮再继续把它扩到 `纱厂女工 / 难民流亡者`，分别承接 `熟工 / 夜校 / 里弄旧识 / 口粮票 / 递信字路 / 代写家信 / 记工账` 与 `亲族屋檐 / 捎话旧路 / 旧街坊`。对于 `flee-refugee`，运行时现已不再只切去 `refugee` 单 profile，而是按 `subeipoor + refugee` 双 profile 并账，使苏北逃亡线能同时保留 `病母 / 妻` 与 `connections` 两层主体回应。后续补新主线时，优先新增 profile、标签与判读规则，而不是再写一个专用 HUD 函数。
> 运行时一致性补充：辅助主体规则不应只在“最终 HUD 推导”里按多 profile 生效、而在年度主体推进里又悄悄回退成单 profile。当前 `demo.html` 已把这层应用逻辑收成统一 helper，令 `advanceSubjects()` 与 `syncDerivedStatuses()` 都按当前全部 active aux profiles 推进同一套 `AUX_SUBJECT_RULES`，避免 `flee-refugee` 这类双 profile 线在年内推进与年终显示之间出现口径漂移。
> 自然回放补充：现有 `natural-subei-flee-home` 已明确断言同一次真实年份推进里会同时露出 `妻子去留 = 妻跟你一起上路` 与 `旧屋亲邻 / 外头活路 = 重新接上`；本轮继续把这组双 profile 从“并账”推进到“牵制”，使 `natural-subei-flee-home` 会显性断言 `主体牵制 = 妻子跟着上路以后，旧屋未独自兜住后半生，回乡照应先顶上来`，`natural-subei-flee-away-root` 会显性断言 `主体牵制 = 病母后事办过以后，妻子与后方屋檐一起把日子接住`，用来验证“苏北家中主体 + 难民外部门路”这组双 profile 不只同时在场，还能进入同一条运行时牵制规则。

> 外部门路分项事实补充：即便在同一个 `subjects.connections` 桶里，一条外部门路回应也常常包含多个可观察对象（例如“同业会馆 + 旧交”“学会名帖 + 报馆旧交”“捎话人 + 旧街坊”）。为避免把外部门路长期压扁成一句总括，运行时现允许把 `connections.response` 进一步拆成多条**分项事实**写入 HUD/终局口径（仅做拆分，不得凭空新增对象；若无法拆分则回退原单句）。

> **unknown 台账补充**：下一阶段的重点不应再是“把 remaining mixed 清空”，而是把已经在运行时显式写成 `unknown` 的制度参数边界与多年确认缺口系统化登记（只登记，不补细节）。`demo.html` 现新增 `window.__DEMO.inspectUnknownLedger()`，不只会按主路线桶统计这些 `unknown` 的触发情况，还会进一步按**可复用字段组**归并，并输出 `recommendedFieldGroups`，帮助决定哪些 `unknown` 值得先抽成统一字段/机制台账，哪些应继续留在各线内的证据不足处。
> **unknown 字段组验证补充**：当某几组 `unknown` 已经被 `recommendedFieldGroups` 连续判成最高优先级后，下一步不应只停在“统计出现频次”，还应把它们锁进专门的代表态 / 自然回放覆盖检查。`demo.html` 现新增 `window.__DEMO.inspectUnknownBoundaryCoverage()`，先把 `多年确认真相边界 / 多年确认后续去向边界 / 名额转嫁承接边界` 这三组高频字段组锁进统一验证，再决定是否继续往更细字段或机制层推进。
> **名额转嫁承接字段层补充**：`名额只是转给了别家、承接户身份仍 unknown` 这类制度边界，不应只停在终局一句话；运行时现把它收成 `payload.quotaTransfers[]`，统一返回 `recordedBy / evidenceLabels / missingEvidenceLabels`，并新增 `window.__DEMO.inspectQuotaTransferEvidenceCoverage()` 做主链矩阵 + 自然回放双层验收。这里仍不补“承接户是谁”，只把“缺口如何被记账、证据落在哪、仍缺哪些制度细节”结构化。
> **名额转嫁分流/层级补充**：当这组 `quotaTransfers[]` 已经稳定横跨 `躲丁 / 买丁 / 托情 / 病身免役` 四条留乡主分流时，下一步不应继续只看“命中了多少条 related 场景”；运行时现进一步把它补成 `branchKind / branchKindLabel / transferTier / transferTierLabel / currentTransferLabel`，并新增 `window.__DEMO.inspectQuotaTransferBranchCoverage()` 与 `window.__DEMO.inspectQuotaTransferTierCoverage()`，显性回答“是哪一条缓过分流把名额转给了别家、当前停在首轮还是二次再转嫁”。这里仍不补承接户身份与地方摊派细数，只把 shared unknown 的分流/层级结构化。

> **名额后账回应字段层补充**：名额转走以后，玩家常会选择“递小礼赔心意 / 闭门不问装作不知”这类后账动作。为了避免这类后账永远只靠终局一句 `unknown` 描述，运行时现把它收成 `payload.quotaRippleResponses[]`，统一返回 `recordedBy / evidenceLabels / missingEvidenceLabels`，并新增 `window.__DEMO.inspectQuotaRippleResponseEvidenceCoverage()` 做主链矩阵 + 自然回放验收。这里仍不补“对方心账/村里口风到底怎么想、怎么记”，只把“边界如何出现、靠谁可观察、缺口是什么”结构化。后续验收不应只锁住 `quota:quiet` 一种保守变体，也应把 `quota:small-gift` 这类“递小礼以后，对方怎么接、怎么想仍 unknown”的分支一并纳入代表态与自然回放，避免字段已定义、其中一半变体却长期没进统一检查。
> **多年确认状态快照补充**：当 `soldier:missing / refugee:loss / labor:injury / labor:blacklist` 已经进入统一 `LONG_CONFIRM_RULES` 后，不应继续只靠零散 echo 或终局文案判断是否“确认到了哪一层”。`demo.html` 现把它们对外收成 `payload.longConfirmations` 与 `window.__DEMO.inspectLongConfirmCoverage()`：前者让 `inspect()/previewEnding()` 直接返回 `ongoing / prolonged / confirmed` 阶段、关联的 `unknownFieldGroups` 与边界说明，后者则检查这些状态快照是否已同时进入主链矩阵与自然回放。
> **多年确认来源层补充**：当 `payload.longConfirmations[]` 已经稳定带出 `recordedBy` 后，不应继续只把这层来源埋在证据字段里。运行时现为它新增 `recordedByArchetypes`，并补出 `window.__DEMO.inspectLongConfirmRecordedByCoverage()` 与 `window.__DEMO.inspectLongConfirmSourceArchetypeCoverage()`：前者把 `活下来继续上路的人 / 家里活着的人 / 你自己 / 厂门口风 / 工头与稳工位的认人后账` 这组字面来源收成可比较矩阵，后者再保守抽成 `活着继续把后账记下去的人 / 门口口风与认人后账` 两类来源原型。这里仍不补失联真相、折损名单、诊断细节或黑名单流转机制，只把“这层多年后账究竟靠谁被看见”结构化。
> **制度流程细节字段层补充**：当 `抓丁/入伍时礼钱与规矩`、`躲/逃被抓回后的惩处口径`、`女工黑名单流转机制` 这类缺口已经在多条主线反复出现时，不应继续只停在 `unknown 台账` 的摘要里。运行时现把它们收成 `payload.institutionDetails[]`，统一返回 `currentKnownLabel / recordedBy / evidenceLabels / missingEvidenceLabels`，并新增 `window.__DEMO.inspectInstitutionDetailEvidenceCoverage()` 做主链矩阵 + 自然回放验收；随后再用 `window.__DEMO.inspectInstitutionRouteCoverage()` 把这些字段按主路线桶收成**制度路线矩阵**，显性回答“哪些主路线已经真正接上制度边界层、哪些还只是命中了 unknown 字段组”。这里仍不补地方细数、惩处条文或幕后人名，只把“已经知道到哪一层、靠谁记账、还缺什么证据”结构化。
> **地方兵役参数边界补充**：当 `本地抽丁比例 / 壮丁费或买丁礼价 / 保甲摊派与免役认定口径` 已经不只出现在苏北单一分支，而是稳定横跨 `苏北留乡 / 苏北士兵 / 苏北逃亡` 三条主路线时，不应继续只把它作为 `institutionDetails[]` 里的一个通用标签。运行时现把它进一步拆成独立 `payload.draftInstitutionBoundaries[]`，统一返回 `routeKind / currentPressureLabel / currentResponseLabel / responseTier / recordedBy / evidenceLabels / missingEvidenceLabels`，并新增 `window.__DEMO.inspectDraftInstitutionBoundaryCoverage()` 与 `window.__DEMO.inspectDraftInstitutionRouteCoverage()`，显性回答“同一拨抓丁压力最后各被接成了哪种主回应”。这一轮再继续补出 `window.__DEMO.inspectDraftInstitutionTierCoverage()`，把同一组边界反查成 `缓过一回 / 被带走 / 离乡避征 / 周旋未稳` 四层回应矩阵，避免 `周旋未稳` 这层长期只存在于字段函数、却没有进入统一验收；并继续补出 `window.__DEMO.inspectDraftInstitutionRecordedByCoverage()`，把同一组边界再反查成 `村里传到门口的告示与口风 / 保甲点验桌前后账 / 一家离乡前仓促应对后账` 三类来源矩阵，显性回答“抓丁压力目前究竟靠哪类可观察来源被系统看见、哪些来源已经跨路线复用”；本轮再继续补出 `window.__DEMO.inspectDraftInstitutionSourceArchetypeCoverage()`，把这三类字面来源继续抽成 `村里门口口风 / 抓丁临门后的应对后账` 两类共享来源原型，使这组制度边界也能在保留 route-local 字面差异的前提下进入跨路线来源模板比较。这里仍不补地方细数与礼价，只把边界如何跨路线出现、靠谁记账、缺什么证据结构化。
> **金圆券保全边界补充**：当 `上海工商家` 男女线共享的 `金圆券风暴：保全、清账与退路折算口径` 已经不再只是某一条主线的附带 unknown，而是稳定横跨 `工商家男线 / 工商家女线` 两条主路线时，不应继续只把它作为 `institutionDetails[]` 里的一个通用标签。运行时现把它进一步拆成独立 `payload.goldPreservationBoundaries[]`，统一返回 `routeKind / currentPreservationLabel / currentReckoningLabel / recordedBy / evidenceLabels / missingEvidenceLabels`，并新增 `window.__DEMO.inspectGoldPreservationBoundaryCoverage()` 与 `window.__DEMO.inspectGoldPreservationRouteCoverage()`，显性回答“顾家男女线各自先保了哪一层、后账又卡在哪一层折算”。这一轮再继续补出 `preservationKind / preservationKindLabel / preservationArchetype / preservationArchetypeLabel / reckoningTier / reckoningTierLabel`，以及 `window.__DEMO.inspectGoldPreservationKindCoverage()` / `window.__DEMO.inspectGoldPreservationArchetypeCoverage()` / `window.__DEMO.inspectGoldPreservationTierCoverage()`，把同一组边界继续反查成“先守哪种细保全姿态、它属于哪类更高一层保全原型、后账又落在哪层重摊”的共享矩阵。这里仍不补具体折价口径、保住的资产明细或门路真相，只把边界如何跨路线出现、靠谁记账、缺什么证据结构化。
> **support × institution 联动补充**：当 `工作队进村 / 返乡重站丈量场` 这类制度边界已经进入 `payload.institutionDetails[]` 后，不应只有 `难民 / 苏北逃亡返乡` 能回答“谁先把人接住”。运行时现也把 `归乡士兵` 晚段的 `旧屋 / 家口安身底盘` 单独接进 `support`，让 `归乡士兵重站丈量场` 也进入与 `工商家金圆券风暴`、`难民/苏北逃亡返乡认地` 同级的 `support × institution` 联动模板；证据不足处仍只保留 `unknown`，不补更细折算细节。
> **返乡认地折算边界补充**：当共享制度缺口已经稳定显示 `旧屋/旧地/后路怎样一起折算` 与 `丈量与认人时怎样重新认你这一路旧账` 横跨 `苏北留乡 / 归乡士兵 / 苏北逃亡返乡 / 难民返乡` 多条主路线时，不应继续只让它们停在 `payload.institutionDetails[]` 的 `missingEvidenceLabels` 里。运行时现把这两层再拆成独立 field group `workteam-recognition-boundary`，并用 `payload.workteamRecognitionBoundaries[]` 专门结构化“当前站在哪种认人/认地姿态、仍缺哪两层折算证据”；随后又把字面 `recordedBy` 继续保守抽成 `丈量场上的认人口风 / 旧账/旧路/旧地重新接回后的后账` 这组共享来源原型，并新增 `window.__DEMO.inspectWorkteamRecognitionSourceArchetypeCoverage()` 做主链矩阵 + 自然回放验收。这里仍保留 `工作队进村后的丈量/认人口风 / 旧街坊与回乡路上续回的消息 / 归乡以后家口与旧地重新接线的后账` 这些 route-local 字面差异，不为了对称硬并成一句来源。
> **旧路回接落点边界补充**：当 `abroad:probe-return`、`refugee/flee:return-bridge` 与 `soldier:return-* / soldier:team-*` 已经稳定表示“人在试着把旧路接回”，下一步不应继续只停在一句 `unknown`。运行时现把这类缺口收成 `payload.returnRouteLandings[]`，统一返回 `routeKind / currentLandingLabel / landingTier / landingTierLabel / landingArchetype / landingArchetypeLabel / recordedBy / recordedByArchetypes / evidenceLabels / missingEvidenceLabels`，并新增 `window.__DEMO.inspectReturnRouteLandingEvidenceCoverage()`、`window.__DEMO.inspectReturnRouteLandingRouteCoverage()`、`window.__DEMO.inspectReturnRouteLandingTierCoverage()`、`window.__DEMO.inspectReturnRouteLandingArchetypeCoverage()`、`window.__DEMO.inspectReturnRouteLandingRecordedByCoverage()` 与 `window.__DEMO.inspectReturnRouteLandingSourceArchetypeCoverage()`，把这组 shared unknown 继续压成字段层、路线矩阵、落点层级矩阵、落点原型矩阵、字面来源矩阵与来源原型矩阵。这里仍不补“究竟哪座城、哪一条门路、哪一户旧屋真正接住”，只把“当前已回身到哪层、属于哪类大姿态、靠谁记账、这些来源是否已跨路线复用”结构化；其中 `landingTier` 只保守抽 `试探旧路 / 旧屋试接 / 收脚探口风 / 旧屋旧账回手 / 试路留外 / 两头并压` 这组已有运行时落点层级，而 `landingArchetype` 只再保守压成 `试路探口风型 / 旧屋旧账回接型 / 旧路已触，但外路仍压身型` 三类共享姿态，不为对称硬并真相。
> **模板矩阵验收补充**：当“苏北样板抽取”已经不再只是文档口径，而确实迁移到了 `苏北士兵 / 苏北逃亡 / 沈家 / 工商家 / 女工 / 难民 / 海外轨` 这些主路线后，不应继续只靠 README 人工比对。运行时现新增 `window.__DEMO.inspectTemplateExtractionCoverage()`，按主路线桶统一汇总 `五段闭环 / 外围主体层 / 制度层 / unknown 字段组 / support × institution / support × longConfirm` 这几层 route 级摘要，直接回答“哪条线已经完整复用模板、哪条线当前还挂着哪些整局级字段缺口”。这层仍只做迁移验收，不为对称硬造新剧情。

> HUD/终局口径补充：当 `family` 模式中途 **切到另一条主轨**（例如 `sold-mill` 纱厂女工、`flee-refugee` 难民流亡），HUD 事实层与终局事实回收应优先按“新主轨”呈现，避免仍拿原家庭主链的关键字段误读后续人生；`苏北贫农家` 是一个有意保留的例外：切到 `soldier/refugee` 时仍沿用“抓丁→土改→总账”的派生事实层，以便把“出身旧账”与“外出新轨”并账记回同一套账册。

---

# 节点 N-01：苏北贫农家 男 · 1937 抓丁抉择
> 填补现有 `conscript` 事件与 `militia-run` 动作之间从未展开的抉择。上游父节点：`enlighten`(6岁开蒙)、`arranged-marriage`(15-20婚约)、`sell-for-cure`(病母)。

## ① 共同父快照 P₀（所有分支只读继承，禁改）
| 字段 | 值 | 来源 |
|---|---|---|
| 人物 | 李根生（家庭=苏北贫农家 · 男 · 生于1910） | 设计给定 |
| 时间 | 1937 年秋（全面抗战爆发、淞沪会战期间） | HI |
| 年龄 | 27 | 派生 |
| 地点 | 苏北乡村（尚未沦陷，但征兵与难民潮逼近） | HI |
| 人生路径 | 留乡佃农（stay-farm，13岁默认走上） | 引擎 |
| 工作 | 佃租耕作 + 农闲给东家打短工 | 引擎 track=tenant |
| 教育底色 | `unknown`（取决于6岁 `enlighten`；贫农多为 edu:none 或夜校识字） | 运行时 |
| 住房 | 租住草房，无地契 | position 起点12 |
| 现金 | `unknown`（起点 money 6，逐年波动，设计层判定"极薄"） | 运行时累计 |
| 资产 | 无田契；仅农具口粮 | position≈12± |
| 债务 | `unknown`（若曾 sell-for-cure 借贷 → echo `debt:usury/debt:cure`） | 运行时 echo |
| 关系 | 病母在堂（care-mother 线）；可能已有婚约/妻（arranged-marriage 是否触发=`unknown`） | 运行时 |
| 照护 | 病母需长期照料——既是关系账来源，也是行动占格负担 | 引擎 |
| 健康 | `unknown`（起点 health 80，逐年劳损） | 运行时 |
| 六维 | 体魄较高·手艺中·学识极低·心智中低·人脉声望极低（起点值明确，累计 `unknown`） | 起点表 |
| 未知项 | 是否被抽中、战线推进速度、家人能否随迁、来年年景、母亲余寿 | 明确 unknown |

## ② 本节点必须区分的概念（不得互相自动推出）
- 保甲**征召通知/摊派名额** ≠ **实际被抓走** ≠ **编入部队** ≠ **上前线** ≠ **士兵身份**（现役/逃兵/伤退/阵亡各不同）
- 保长口头"你家可免" ≠ 名额真落到别家 ≠ **免役文书** ≠ 实际没被抓
- "缴壮丁费/雇人顶替(买丁)"**报价** ≠ 家里**凑得出的现钱** ≠ 保长**实际收下并放人**
- **躲丁(逃役)** ≠ **逃兵**(入伍后逃) ≠ **拉夫**(临时抓差)——法律后果与污名不同
- 参军 ≠ 抗日英雄声望；也可能被吃空饷、当炮灰、病饿死于非战斗减员
- 婚约 ≠ 婚姻 ≠ 妻子同意他走/留 ≠ 妻子会留守务农并照护病母

## ③ 参与人物及逐人权利
| 人物 | 独立目标 | 自己的权利（玩家不可代写） | 关键约束 |
|---|---|---|---|
| 李根生(本人) | 活命·守家·免饥 | 躲/顶/买/从军/逃亡的选择权；信息有限 | 现金·健康·名额 |
| 病母 | 被照护·不拖累儿子 | 可**拒绝被抛下**，也可能主动劝他逃或劝他别连累全家；有独立健康轨迹与死亡确认 | 非"关系修复器"，非负担数值 |
| 妻/未婚妻(若已触发) | 自身安稳 | 对"留守+照护病母"有**同意/拒绝/退出权**；他从军≠她同意留守；他失踪/阵亡确认后她可回娘家/改嫁 | 独立主体 |
| 保长/甲长 | 完成上级配额·维系人情 | 摊派与放行的裁量权 | 受上级配额、贿赂、宗族关系约束，非全能 |
| 东家(地主) | 保住租子 | 可因李被抓/逃亡而**夺佃换人** | 李的生计约束方 |
| 同村壮丁/同乡 | 各自免役 | 竞争同一批名额，其选择反过来影响李是否被抽中 | 名额容量守恒 |

## ④ 相关制度与规则（地区+年份版本）
- 1937《兵役法》已颁但基层执行混乱，实际以保甲"派丁/买丁/拉夫"并行为主。`HI`（大方向）
- 摊派比例（如三丁抽一类）与"壮丁费/买丁"具体数额：**苏北地方具体版本 `MID·需核实`**，设计层暂以区间示意，禁止写死具体银元数。
- 免役需现钱→富户特权，贫农多凑不出（信息不对称+有限资源）。`MID`
- 名额有**容量**：一保若干名，抽中与否受宗族势力/贿赂/人情影响。`LO·机制方向`
- 逃役被抓回加重处置；举家逃亡=脱离保甲→失佃权与户籍处境（position 崩）。`MID`
- 外部时钟：苏北 1938 年后逐步沦陷，征兵主体更替+难民潮叠加。`HI`（**但 1937 决策时本人不可知，见 INV-7**）

## ⑤ 六条非排名分支（皆从 P₀ 继承，无"最优解"）
- **C1 躲丁**：农忙藏匿/投亲，赌名额落别家
- **C2 应征入伍**：主动或被动从军
- **C3 凑钱买丁/缴费顶替**：以现钱换免役
- **C4 举家逃亡**：脱离保甲，改用脚程、落脚与家口分拆去换活路
- **C5 托人情走动**：走宗族/东家关系求免
- **C6 装病自伤免役**：以身体代价换资格豁免

> 下面 **C1、C2、C3、C4、C5、C6 均已按 15 段写全**；其中 `C4` 运行时现已接到 `逃路上·先过哪一道坎`、`逃到后方·先把家安在哪`、`战后回乡吗·逃亡后的旧地旧人`，但尚未单列出完整难民专属 track。

---

### C1 躲丁（逃役藏匿）— 15 段完整展开
1. **选择前审计**：现钱不足以买丁(`unknown`但设计判定极可能不够)；体魄尚可、地形熟；病母无法随藏。
2. **信息披露**：本人只知"这一保要抽丁、日子将近"；**不知**确切名额数、是否已被点名、战线何时到（不可用1938沦陷倒填）。
3. **他人独立回应**：病母——可能催他快走(怕失子)或骂他不孝(怕连累)，`response=unknown`由其性格轨迹定；妻(若在)——可能同意也可能以"抛下我们"拒绝；保长——发现人跑会记名/迁怒家属。
4. **资格与可行性**：藏匿窗口取决于 body / 地形熟悉(craft) / 有无可投亲(network)。
5. **实际行动**：`militia-run` 动作占满四时段、spirit-5、无产出——藏一天就误一天农活生计。
6. **第一阶段结果(判定)**：`roll chanceBy:[body,mind,network]`。躲过=名额落别家(可能是同乡被抓→关系账代价)；败露=被抓回加重，转入 C2 且 position/relation 受损。
7. **普通生活窗口**：躲丁期间田荒、短工丢、病母无人照料(照护缺口→母 health 下滑)。
8. **外部冲击**：战线推进、二次点丁、灾荒叠加(era 冲击 money)。
9. **多系统冲突**：躲丁(保命) ↔ 生计(误农) ↔ 照护(弃母) ↔ 关系(妻/母/同乡)四系统抢同一份时间与现金。
10. **修订/暂停/退出**：可中途改缴费(转C3,若凑到钱)、或索性逃亡(转C4)、或被抓(被动转C2)。
11. **一年后**：若躲成→保住命但欠下同乡人情/母病加重；并应显式记下“这回名额转给了别家、具体哪户仍 `unknown`”这一层事实；echo `evade:draft / quota:shifted`。
12. **五年后**：抗战中后期或已沦陷区，逃役者身份影响其在新秩序/宗族中的处境，`unknown`按走向。
13. **更长期**：1949 总账时，"当年躲了丁"可能被邻里记着(声望)也可能无人再提。
14. **新状态快照 P₁**：track 不变(仍 stay-farm)；新增 echo `evade:draft / quota:shifted`；母/妻 consent 与 health 按各自轨迹更新；名额压力可能转移到同乡，但承接者身份保持 `unknown` 或另立节点，不得偷写。
15. **继续/修改/退出/重入**：可重入 C2(二次抓丁)、C4(逃亡)；不可回到"未被点丁"的 P₀(时间单调)。

### C2 应征入伍（从军）— 15 段完整展开
1. **选择前审计**：主动应征(换安家费/免家属被扰)或被动被抓；body 达标才编入战斗部队，否则拉夫做役。
2. **信息披露**：只知"入伍/被带走"，**不知**部队番号、是否上前线、能否活着回——严禁预读隐藏寿命。
3. **他人独立回应**：妻——**对留守+独力照护病母有独立同意/拒绝权**，可能留守、可能回娘家、可能改嫁(须待他失踪/阵亡**确认**后)；病母——可能以死相劝或默许；东家——大概率夺佃。
4. **资格与可行性**：编入战斗部队 gate≈body；否则为辎重/民夫(拉夫)。
5. **实际合同/行动**：入伍=离开本村→track 由 stay-farm 切至新的 `soldier` 轨；运行时现已先补出 `新兵营里·先熬哪一层日子`，把“被带走”与“真正上前线”拆开：先经过待编、操练、杂差、抱同乡和营里饥饿这些军中普通生活，再进入会战或后续分化。家中则同步留下照护/生计真空。
6. **第一阶段结果**：安家费(报价≠到手，可能被克扣)；家属或得暂时免扰。
7. **普通生活窗口**：军营/行军日常——操练、饥饿、疫病、会战后的补充整训、辎重与勤务线的消耗；**活过一场会战** ≠ **已经离开军队** ≠ **已经能回乡**。非战斗减员仍是民国士兵主要死因之一 `HI`。
8. **外部冲击**：会战、溃退、拉夫扩编、欠饷。
9. **多系统冲突**：本人生死(健康) ↔ 家中照护真空(母无人管) ↔ 妻的独立去留 ↔ 佃权丧失(处境)。
10. **修订/暂停/退出**：可逃兵(→逃兵污名 echo，被抓回极刑风险)、伤退、被俘、失踪。
11. **一年后**：运行时现已把一年内的从军链拆成两层：先是 `soldier:drilled / soldier:camp-support / soldier:camp-bond` 这类新兵营中继，说明人物先在操练、勤务或同乡照应里熬过军中普通生活；之后才在 `1938` 左右分化成 `soldier:survive / soldier:wounded / soldier:rear / soldier:missing` 四层：活过火线、重伤后送、落到后勤/辎重线、或在溃散与失联中变成"没有准信"。这些都不是同一回事。
12. **五年后**：生还者如今也不再直接从 `soldier:survive` 跳到 `1945` 复员；运行时已补出 `会战之后·队伍里还怎么熬`，先把“活过火线以后还在军里怎么过”拆成 `soldier:front-hold / soldier:front-shift-rear / soldier:front-learned` 三层：继续跟着老部队硬熬、从前线转落后方线、或在军里认粮册跑差并给战后先留一点门路。若转落后方线，后面还会继续进入 `辎重后勤·熬年头`，再沉淀成 `soldier:rear-hold / soldier:rear-learned / soldier:rear-routed` 三层，把“后方线”继续拆成熬年月、学军中手艺、给复员后多留门路。

   到 `1945` 的“复员”也不再被写成一句“回到村口”。运行时新增 `抗战胜利·复员去路`，先把复员后的去向拆成三条事实态：`soldier:demob-home`（先回乡看看家口与旧地还剩下什么）、`soldier:demob-stay`（先留在驻地/县城找活把脚跟站住）、`soldier:demob-drift`（跟着旧同袍继续闯一阵）。其中只有选择 `soldier:demob-home` 才会进入 `解甲归乡`；并且 **不预设病母是否已亡**，若死讯成立也必须经“发生 + 确认”写回。

   若选择先在外头落脚，运行时会继续进入 `复员在外·先把日子接住`，把外头的活法至少写成两条可观察事实：`life:city-labor`（苦力/短工）与 `life:security-work`（地方武装/保安团饭碗）；仍不做价值排名。

   若选择 `soldier:demob-home`，或此前已因 `soldier:wounded` 伤退回乡，运行时现在也不再把他直接并回通用 `风声先到·工作队未到`。而是先进入 `归乡之后·旧地风声`，把复员/伤退之后“旧地还接不接得上、家口和身子先稳哪一头”拆成 `soldier:return-listen / soldier:return-cautious / soldier:return-order` 三层；到 `1947` 再经 `工作队进村·归乡人怎么认旧地` 继续分化成 `soldier:team-witness / soldier:team-guarded / soldier:team-order`，到 `1948` 再进入 `分田以后·归乡人的日子怎么接`，沉淀成 `soldier:settle-ledger / soldier:settle-mutual / soldier:settle-watch`。这条链只回收“归乡人如何重新接旧地”，不把退伍兵自动写成通用 `land:cadre / land:claim / land:hesitant`。

   伤退者则会落到残身谋生或依亲接济。失踪者如今已在运行时补成三层：先是**家中无准信**，妻子只会在“继续留家等消息”与“先回娘家另找活路”之间作独立安排，并分别显式写成 `wife:missing-wait-home / wife:missing-separate`；再过两年若仍无准信，会再写下 `soldier:missing-prolonged`，说明“没有准信”本身已变成一层长期生活事实；再过几年若仍无准信，家中才会把这桩失联**按亡故确认记账**，并分别显式写成 `wife:missing-confirm-home / wife:missing-confirm-natal`。这仍不等于“真相大白”，也**不会**被自动写成“已可改嫁”。
13. **更长期**：1949 时退伍/伤兵身份在新旧秩序中处境不同，`unknown`；若曾长期失联，运行时如今已把“无准信阶段 / 拖长阶段 / 按亡故确认”三层拆开，但确认后的再婚/改嫁继续保留 `unknown`。
14. **新状态快照 P₁**：track=soldier；本人 health 大概率下滑；**病母/妻的状态由其独立轨迹推进，可能在他离家期间死亡/离开——须经确认，不由玩家代写**。
15. **继续/修改/退出/重入**：重入乡村(生还)、重入 flee-refugee(溃退随难民)、或进入"死亡确认"终止分支。

---

### C3 凑钱买丁 / 缴费顶替 — 15 段完整展开
1. **选择前审计**：想走这条路，前提不是“知道可以买”，而是家里手边真有可变现的现钱或能借到的钱；贫农通常只够想到，不够做到。
2. **信息披露**：本人只知“有人靠现钱缓过一回”，**不知**当地这次真实报价、保长是否肯收、收了是否真放人；具体银元数仍为 `unknown`。
3. **他人独立回应**：病母可能反对“拿救命钱去买丁”，也可能催他先保命；妻若在，可能拒绝把婚后口粮和嫁妆一并押进去；保长有收不收、收了算不算数的裁量；被买来顶替的壮丁是独立主体，不得写成“花钱自动生效”的道具。
4. **资格与可行性**：gate≈money，可叠加“可借债 / 可典当 / 可卖物件”；**可筹到的钱** ≠ **桌上真拿得出的现钱** ≠ **对方报价/规矩** ≠ **最后成交**，这些层次必须分离。
5. **实际合同/行动**：运行时现在已拆成同年两步：先判“能不能把钱与话真正递到桌上”。若连这一步都没做成，写入 `draft:buying-stalled`；若做成，则以 `draft:buying` 表示“钱先凑上桌、话递进去了”，再进入 `买丁桌上·收钱算不算数`，由第二步决定是 `draft:bought / bought:off`、`draft:buying-failed + draft:soldier`，还是临时收手改入 `draft:evade`。
6. **第一阶段结果**：若连桌都没上成，应明确写回“试过凑钱买免，但现钱和面子先折进去，人仍被带走”；若保长收钱放人，得到的是“本次免役”，并应额外写入“这回名额只是转给别家、具体落到谁家仍 `unknown`”这层事实；若钱折进去却没说成，则应明确写回“钱损失了、人仍被带走”；若临到桌前收手，则转入 C1 躲丁并保留已折进去的茶水钱/面子账。**成功只说明这一回没被带走，不说明旧账消失，也不说明名额消失。**
7. **普通生活窗口**：免役以后，田照种、债照还、病母照护照旧发生；但现钱被抽空，普通生活先被亏空吃掉。
8. **外部冲击**：1938 后战线推进、物价波动、失佃风险与灾荒叠加，让“当年花钱脱身”继续发酵成债务和地缘代价。
9. **多系统冲突**：免役(保命) ↔ 现金见底(生计) ↔ 典当/借债(处境) ↔ 家中照护与妻子同意(关系) 同时争抢同一笔资源。
10. **修订/暂停/退出**：凑钱中途可改躲丁或举家逃亡；买丁成功后也可能因亏空转入 `sell-for-cure`、高利贷、失佃或外出短工，**不能回到“没花过这笔钱”的状态**。
11. **一年后**：运行时已落 `买丁之后·几年后的亏空`；后果分化为 `bought:debt-burden / tenant:right-lost / bought:seasonal-migrant` 等事实，说明免役后的第一年主要是在补窟窿。
12. **五年后**：到 `1946` 前后，运行时以 `买丁后账·风声再起` 把旧账继续推进成 `bought:ledger-ready / bought:hold-life / bought:stay-migrant`；这不是政治站位分数，而是旧账在新风向前的不同收束方式。
13. **更长期**：到 `1948`，运行时已接 `分田以后·买丁旧账怎么摊`，分化为 `bought:settle-spoken / bought:settle-hold / bought:settle-remote`；到 `1949` 总账，统一只按事实句回收“买丁、失佃、外出短工、新地如何接上”，不写成道德排名。
14. **新状态快照 P₁**：新增 `draft:buying-stalled / draft:buying / draft:buying-failed / draft:buying-aborted / draft:bought / bought:off / quota:shifted / bought:* / land:pre* / bought:settle-*`；money 下降、position/tenantRight 可能受损；病母/妻与顶替者均按各自轨迹推进。
15. **继续/修改/退出/重入**：可继续留乡务农、转外出短工、进入减租/土改中继层；不能把“买丁成功”直接推出“后来必得田/必被清算”，均保持 `unknown` 直到运行时真正走到那一步。

### C4 举家逃亡 — 15 段完整展开
1. **选择前审计**：这条路拿去换免役的，不是钱，也不是人情，而是整户人家的地缘、脚程和之后好多年的安身资格。举家一走，就默认原来的佃耕秩序、保甲编册和熟人路数都要断一截。
2. **信息披露**：本人只知“这回若不走，抓丁可能就落到自己头上”；**不知**路上哪条线还能通、哪处收容点还在、原乡会不会很快沦陷、战后还能不能回去。后方哪座城、哪处村、哪门亲最终真能接住他们，都仍是 `unknown`。
3. **他人独立回应**：病母不是包袱数值，她可以同意被带上路，也可以要求先托亲族，甚至明确拒绝离乡；妻若在，也有独立的跟走/留下权，不能被玩家直接代写；亲族和同乡即便愿意接人，也只是“可能收留”，不是稳定保底。
4. **资格与可行性**：能不能真走成，不取决于“想不想”，而取决于 body / network / mind 与家中病母、妻、盘缠是否允许整户移动。**脱离保甲** ≠ **已经安全到后方** ≠ **在后方落稳脚**，三层必须分离。
5. **实际合同/行动**：运行时先落 `draft:flee`，随后必须继续经过 `逃亡前夜·病母上不上路`，把病母是“同行 / 暂托亲族 / 自己决定留下”分开写清；妻子的去留则由她在运行时独立回应，不由玩家替她签字。
6. **第一阶段结果**：进入 `逃路上·先过哪一道坎` 后，分化为 `road:survived / road:stalled / road:detoured / relief:dependent`。成功过关只说明一家人暂时活到了下一站；被卡住、绕路或靠赈济续命，都仍是活下去的不同事实，不是输赢评分。
7. **普通生活窗口**：逃到后方以后，日子很快从“逃命”变成“找地方住、找饭碗、看病母和家口还能不能续上”。普通生活不再是原乡佃耕，而是搭棚、借宿、跑零工、托家口、靠赈济续命。
8. **外部冲击**：战区推进、关卡盘查、赈济稀缺、后方物价与劳力市场变化，都会继续改写逃亡者的落脚方式。人物在 1937 做出逃亡决定时，不能预读战后是否准许返乡、原乡是否还认旧佃权。
9. **多系统冲突**：免役(保命) ↔ 原乡断开(处境) ↔ 家口不散(关系/照护) ↔ 路上盘缠与体力(有限资源) 同时抢同一份人和钱。病母跟不跟得上路、妻跟不跟走、壮劳力要不要外出找活，都在争同一口气。
10. **修订/暂停/退出**：到了后方可以先搭棚、投亲、分开谋生，也可以在战后试探回乡；但任何修订都不能删除“这家人当年是为躲抓丁离乡”的事实。回乡探路也只是试探，不得一步写成“从此回到原样”。
11. **一年后**：运行时已接 `逃到后方·先把家安在哪`，分化为 `flee:settle-shack / flee:kin-shelter / flee:split-support`。这三条分别对应“先把家拢住”“借别人屋檐缓气”“饭碗与家口分成两头扯”。
12. **五年后**：到 `1946`，运行时再以 `战后回乡吗·逃亡后的旧地旧人` 继续分成 `flee:return-probe / flee:stay-away / flee:still-drifting`。重点不是是否“胜利归来”，而是原乡、旧佃权、新落脚处三者之间到底还能不能重新接上。
13. **更长期**：到 `1949` 总账，统一只按事实句回收“当年是否举家离乡、路上怎样过关、后方怎样落脚、战后是否试探回乡或继续漂着走”，不把逃亡写成勇敢勋章，也不写成道德失败。
14. **新状态快照 P₁**：新增 `draft:flee / flee:carry-mother|left-mother|mother-stayed / road:* / relief:dependent / flee:settle-* / flee:return-probe|stay-away|still-drifting`；position 会长期低于留乡线，病母/妻则继续按各自主体轨迹推进，并可能进入死亡确认或独立去留。
15. **继续/修改/退出/重入**：可继续漂泊、在后方定住、战后回乡试探，或在 `1949` 时仍停在“脚程还没完全停下”的状态；但无论哪条，都不能再回到 `P₀` 那个“还在原乡等抓丁上门”的时间点。

### C5 托人情求免 — 15 段完整展开
1. **选择前审计**：这条路的核心不是“认识人”，而是手里到底有没有能动用的人情、宗族辈分或东家门路；贫农多数只能托一次，托完就伤筋动骨。
2. **信息披露**：本人只知“有人能说上话”，**不知**对方肯不肯帮、帮到哪一步、免下来的名额最后落到谁家；名额转移对象仍为 `unknown`。
3. **他人独立回应**：病母或妻子可能反对去求旧势力，也可能认为先保命再说；族叔/东家有自己的算盘，不是玩家的代理人；同乡壮丁会受名额转移影响，是被波及的独立主体。
4. **资格与可行性**：gate≈network+relation；还要看旧关系是否愿意为贫农消耗一次信用。求情能开门，不等于事情必成。
5. **实际合同/行动**：实际发生的是托话、递礼、跑腿、说情；运行时现在已拆成同年两步：先写 `draft:pull`，只表示“旧情和礼已经递到桌上”；随后再进入 `托情桌上·这回缓不缓得下`，由第二步决定是否真的写成 `patronage:exempt + quota:shifted`，或落成 `patronage:failed + draft:soldier`，也可临门收手改入 `draft:evade`。这不是“制度性免役”。
6. **第一阶段结果**：第一层成功只说明“话递进去了”，不说明这回已经免掉；第二层成功才说明这回真缓下来了，且名额压力必须转给别家，满足 INV-6。若第二层失败，则应写回 `patronage:failed + draft:soldier` 这类事实，而不是只剩“人财两失”的描述。
7. **普通生活窗口**：缓过抓丁以后，日子并不更自由；你要给人家出力、跑腿、低头，普通生活被长期人情债侵入。
8. **外部冲击**：战局变化与村庄风向更替，会让旧关系的价值和风险一起变化；旧人情不会自动因年头过去而作废。
9. **多系统冲突**：免役(保命) ↔ 人情债(关系) ↔ 同乡替你承接名额(社会代价) ↔ 后来站位是否还能自持(主体性) 同时成立。
10. **修订/暂停/退出**：可以选择及时还情、拖着不还、甚至拿现钱买断，但每种处理都会改写后面能不能独立表态；不能把“欠情”与“未欠情”混为一体。
11. **一年后**：运行时已落 `托情免丁·人情债`，分化出 `patronage:owed / patronage:default / patronage:paid`，说明后果不是当年免了就结束，而是几年都在还或拖。
12. **五年后**：到 `1946`，运行时的 `旧人情与新风向` 把这笔旧账推进成 `patronage:shield-old / patronage:cut-tie / patronage:keep-head-low`，并同步写入 `land:prelisten / land:precautious / land:prebusy`。
13. **更长期**：到 `1948`，运行时已接 `分田时候·旧情让你开口`，继续分化成 `patronage:state-facts / patronage:repay-cutoff / patronage:absent-hearing`；到 `1949` 总账，只回收“是否仍替旧关系留情、是否只讲事实、是否退到人群后面”，不判忠奸高低。
14. **新状态快照 P₁**：新增 `draft:pull / patronage:exempt / patronage:failed / quota:shifted / patronage:* / land:pre*`；relation 变厚或变坏，但不等于自动升阶；同乡名额承接者仍需保留为 `unknown` 或独立节点，不得吞没。
15. **继续/修改/退出/重入**：可继续留乡、参加减租/分田节点，也可因旧情压力更缩回去；无论哪条都不能抹掉“这回是靠关系缓过去”的历史事实。

### C6 装病 / 自伤免役 — 15 段完整展开
1. **选择前审计**：这条路拿去交换的不是钱，而是身体、名声与之后很多年的劳力；它不是“骗一次就完”，而是把后半生的一部分健康押出去。
2. **信息披露**：本人只知“病弱可能免役”，**不知**军医/保甲认不认、装到什么程度才够、以后会不会被村里盯着；诊断口径与豁免标准仍多为 `unknown`。
3. **他人独立回应**：病母可能宁可他留下也不愿真伤身；妻若在，可能反对把往后几十年的劳力赔掉；保长/军医有识破与否的裁量，不能被玩家直接决定。
4. **资格与可行性**：症状 ≠ 诊断 ≠ 豁免；纯装病要靠表演、时机与他人信不信，自伤则要付出永久代价。两者都不是稳定捷径。
5. **实际合同/行动**：真正发生的是装病、少出门、找见证、甚至真把身体弄坏；运行时现在也拆成同年两步：先写 `draft:fakesick`，只表示“症状先演到了点验/复验桌前”；随后再进入 `装病验身·军医认不认账`，由第二步决定是否真的写成 `draft:exempt-health + quota:shifted`，或落成 `fakesick:exposed + draft:soldier`，也可临门认命转入从军。不可一步写成“从此安稳”。
6. **第一阶段结果**：第一层成功只说明“把病相演到了桌前”，不说明这回已经免掉；第二层成功才说明本次真免役，但这一回名额并没消失，而是改由别家去顶，承接者身份仍 `unknown`。若败露，则应写回 `fakesick:exposed + draft:soldier` 这类事实，而不是只在描述里说“被识破”。成功也只说明“这回没被带走”，不说明身体账结束。
7. **普通生活窗口**：免役后仍要下田、照护、过日子；可一旦病身或假病持续存在，普通劳动能力会被长期压低。
8. **外部冲击**：几年后灾荒、劳力不足、家口压力会逼着人重新面对这副身体；战局变化只是背景，真正长期存在的是旧伤旧病。
9. **多系统冲突**：免役(保命) ↔ 劳力损失(生计) ↔ 污名与躲闪(名声/关系) ↔ 病母照护(家庭) 同时争一副身体。
10. **修订/暂停/退出**：可继续装、可认命恢复干活、也可能为了坐实旧谎而真自伤；每一种都会把身体与名声推到不同方向。
11. **一年后**：运行时已落 `装病免役·身后尾巴`，分化为 `fakesick:kept / fakesick:work-on / fakesick:self-harm`，说明“装病”不会停在征召当年。
12. **五年后**：到 `1943`，运行时的 `装病之后·身体成了真账` 把它继续沉淀成 `injury:hard-labor / injury:shift-craft / injury:withdrawn`；核心是劳动方式被改写，而不是情绪评语。
13. **更长期**：到 `1946` 与 `1948`，运行时继续以 `病身还在·新账来到门口`、`分田以后·病身扛不扛得住` 推成 `injury:show-scar / injury:guard-body / injury:hide-at-home / injury:settle-*`；到 `1949` 总账，只回收“病身怎样约束站位和生计”，不把伤疤写成勋章或耻辱分。
14. **新状态快照 P₁**：新增 `draft:fakesick / draft:exempt-health / quota:shifted / fakesick:exposed / fakesick:* / injury:* / land:pre*`；health/body 下滑可能长期存在；人物可转轻手艺，但不自动治愈。
15. **继续/修改/退出/重入**：可继续留乡、转轻手艺、进入土改中继层，也可更缩回家门；无论哪条，都不能删除“免役是靠病身换来的”这一事实。

## ⑥ 各分支近/中/长期后果一览（不排名，只列事实与开放未来）
| 分支 | 近期(当年) | 中期(约五年/抗战中) | 长期(1949前后) |
|---|---|---|---|
| C1躲丁 | 保命但误农/弃母，欠同乡人情 | 逃役身份影响宗族处境 | 邻里记忆不定，开放 |
| C2从军 | 离家、家中真空、佃权失 | 伤/病/生还/失踪分化 | 退伍伤兵身份处境未知 |
| C3买丁 | 免役但家底被抽空，可能转成高利贷、失佃或农闲外出补窟窿 | 到 `1946` 前后继续分化为 `bought:ledger-ready / bought:hold-life / bought:stay-migrant` | 到 `1948-1949` 只回收“旧账怎么摊、新地怎么接、是否仍两头奔”的事实 |
| C4逃亡 | 转难民、失佃权户籍 | 流亡途中生死(road-crisis) | 后方落脚或散失 |
| C5人情 | 免役但欠人情、名额转嫁同乡 | 到 `1946` 前后继续分化为 `patronage:shield-old / patronage:cut-tie / patronage:keep-head-low` | 到 `1948-1949` 只回收“仍替旧情留话 / 只讲事实 / 退到人群后面”的事实 |
| C6装病 | 免役但把身体与名声一起押出去 | 到 `1943-1946` 前后继续沉淀为 `injury:hard-labor / injury:shift-craft / injury:withdrawn / injury:show-scar / injury:guard-body / injury:hide-at-home` | 到 `1948-1949` 只回收“病身怎样约束分田后的活法与生计”的事实 |

## ⑦ 普通生活窗口
抓丁不是每天发生；节点之间是**普通务农生活**：四时段安排下田/短工/夜校/照护/歇脚，受边际递减与 era 冲击。抓丁节点是插入普通生活流的一次"外部冲击型情境"。

## ⑧ 外部冲击（本人当时不可知，按年份客观发生）
1937 淞沪→苏北渐入战区；1938 后沦陷与二次征兵；灾荒/瘟疫叠加；`eraMultiplier` 对 money/position 的年度冲击。

## ⑨ 暂停/退出/修改/重入路径
- 暂停：躲丁是可反复的"拖延态"，非一次性终局。
- 退出：买丁/逃亡/装病都是"退出征召"的不同门，代价各异。
- 修改：躲丁中途可转买丁或逃亡。
- 重入：躲过一次仍可能二次点丁；生还士兵可重入乡村生活流。

## ⑩ 新状态字段（需在引擎 G 中新增）
`draftStatus`(未点/已点/躲/入伍/买免/逃亡/免役)、echo：`evade:draft / soldier:survive|wounded|rear|missing|missing-prolonged|missing-confirmed / wife:missing-wait-home|missing-separate|missing-confirm-home|missing-confirm-natal / mother:buried|mother:buried-plain / bought:off / patronage:exempt / quota:shifted / quota:quiet|quota:small-gift|quota:run-errand / bought:off-again|bought:off-again-failed / patronage:exempt-again|patronage:failed-again / draft:exempt-health-again|fakesick:exposed-again / quota:shifted-again / quota:again-quiet|quota:again-small-gift|quota:again-run-errand`、他人字段：`mother.health / mother.alive / wife.present / wife.consent`、`tenantRight`(佃权在否)、`quotaPressure`(名额压力，可转移同乡)。

补充（运行时落地）：`demo.html` 当前已把 `draftStatus / tenantRight / quotaPressure / landStage` 以**派生状态字段**形式接入 `G`，并在苏北贫农家线路的 HUD 与终局页显性展示，用来把散落在 `echo` 里的“抓丁过程态 / 佃权·地缘 / 名额压力 / 觉醒-土改路径”收束成可见事实；但它目前仍是**从已发生回响倒推出的运行时汇总**，尚未上升为完整的父快照冻结或统一过程态状态机。

补充（运行时映射）：当 `C3/C5/C6` 缓过抓丁写入 `quota:shifted` 后，跨年到 `1938+` 会进入情境 `名额转嫁·村里记账`（`quota-shift-aftermath`），把“名额确实转给了别家、具体哪户仍 `unknown`”这一层事实落成三种后账回响：`quota:quiet / quota:small-gift / quota:run-errand`（不做价值排名）。

补充（运行时映射）：对 `C3/C5/C6`，运行时现也补入“缓过一回 ≠ 从此安全”的**二次摊派**最小缺口：在 `1938` 可能再次遇到征召压力情境 `再摊派·买丁不保 / 旧情不保 / 复验风声`，并分别写回 `bought:off-again|bought:off-again-failed / patronage:exempt-again|patronage:failed-again / draft:exempt-health-again|fakesick:exposed-again`；若再次缓过，会写入 `quota:shifted-again`，并在 `1939+` 进入 `名额再转嫁·村里再记一笔`（`quota-shift-again-aftermath`）落成 `quota:again-*` 三条后账回响。承接名额的具体哪一户仍显式保留为 `unknown`，不做价值排名。

## ⑪ 可机器检查的不变量（invariants）
- INV-1 分支切换不得修改 P₀ 任一字段（父快照只读）。
- INV-2 `wife.consent`/`mother` 状态独立存在，玩家动作**不得直接写真**，只能触发其独立回应。
- INV-3 买丁/缴费支出 ≤ 可支配现金(现钱+可借)，不得透支为负现金。
- INV-4 死亡确认后该人物不再产生 action / 消费资源 / 接受 echo 写入 / 生成新记忆。
- INV-5 同一时刻只能在一个 track；soldier 与 stay-farm 互斥。
- INV-6 名额容量守恒：一保抽中人数 ≤ 配额；C5 免役须有对应同乡承接名额。
- INV-7 时间单调：1937 决策不得读取 1938+ 沦陷/通胀信息。
- INV-8 概念态不可跳跃：`征召通知→被抓→入伍→上前线` 每步各需独立判定，不得一步到位。

## ⑫ 图谱：父子节点与跨系统连接
```
上游: enlighten(6) · arranged-marriage(15-20) · sell-for-cure(病母)
                         │
                    ┌────┴ P₀ (N-01 1937抓丁) ┐
        ┌─────┬─────┼─────┬─────┬─────┐
       C1躲丁 C2从军 C3买丁 C4逃亡 C5人情 C6装病
        │     │     │     │      │     │
跨系统: 生计⟷照护(病母)⟷关系(妻/母/同乡)⟷处境(佃权)⟷声望(逃役/逃兵/洋奴无关)
        │     │            │
   evade:draft │        override→flee-refugee轨(接 road-crisis)
              soldier轨→(生还/伤/失踪)→死亡确认状态机
                         │
下游汇入: stay-or-go-1949 总账(按 echo 分化，非评分)
```

---

# 节点 N-02：苏北贫农家 · 减租告示落空到分田之后
> 这是 `rent-negotiate` 与 `land-reform-choice` 之间的中段连接层，专门解决“二五减租听见过一次”与“1947 以后真正站进丈量/分田现场”之间原先过薄的问题。本轮已按**连续人生 15 段**补成正式稿，并与 `assets/choices-data.js` 与 `demo.html` 当前运行时对齐。

## ① 共同父快照 P₀（承接自 `rent-negotiate`）
| 字段 | 值 | 说明 |
|---|---|---|
| 时间 | 1931—1946 之间 | 运行时按 `yearFrom` 命中 |
| 人物 | 李根生 / 苏北贫农家留乡佃农 | 与 N-01 同一主体 |
| 已知记忆 | `rent:reduced / awake:rights / awake:frustrated` 其一或数组合 | 只记录是否“见过政策落地 / 落空”，不自动推出政治身份 |
| 仍未知 | 村中谁先串联、工作队何年真正进村、本人是否会被选为骨干 | 一律 `unknown`，不得预写 |

## ② 概念分离
- 听见减租/分田**口号** ≠ 真正见到田亩被清丈 ≠ 自己已经分到地 ≠ 已经成为骨干。
- 记得那次告示落空 ≠ 必然怀疑一切；也可能转成更会记账、更会认人、更谨慎观望。
- 工作队**风声先到** ≠ 工作队已经进村；前者只决定站位预热，不得直接写成“翻身已发生”。

## ③ 连续人生 15 段正式稿
1. **选择前审计**：这里的核心不是“信不信分田”，而是此前到底见过什么。父快照只允许读取 `rent:reduced / awake:rights / awake:frustrated`、抓丁旧账、病母/妻与现金位置；**没有**哪一条可直接推出“1947 必然站出来”。
2. **信息披露**：1931—1946 之间，李根生最多只能知道“那次告示是否落过地”“谁在村里先串联”“工作队风声有没有到”。工作队何时真正进村、自己是否会被留下帮登记、最后能否分到哪一块地，都仍是 `unknown`。
3. **他人独立回应**：病母、妻、同乡、旧东家、曾经替他说话的人，都有自己的目标。病母可能要他先顾活人日子，妻可能赞成先听风声也可能反对再惹事，旧关系也可能要求他“到时说句话”，**都不能由玩家代写**。
4. **资格与可行性**：能否从“听过一次告示”继续走到“分田现场站位”，取决于识字、记账能力、关系厚薄、身体能否扛事，以及此前抓丁后账有没有把人压垮。`awake:*` 决定的是底色，不等于制度资格。
5. **实际行动 / 中间节点**：运行时当前已落的行动层有三组：
   - 觉醒中继：`减租告示之后·信过一次的人` → `awake:ledger / awake:buried / awake:listening`
   - 风声前夜：`风声先到·工作队未到` → `land:prelisten / land:precautious / land:prebusy`
   - 三条抓丁后账专属中继：
     - `买丁后账·风声再起` → `bought:ledger-ready / bought:hold-life / bought:stay-migrant`
     - `旧人情与新风向` → `patronage:shield-old / patronage:cut-tie / patronage:keep-head-low`
     - `病身还在·新账来到门口` → `injury:show-scar / injury:guard-body / injury:hide-at-home`
6. **第一阶段结果**：第一阶段真正沉淀下来的，不是“翻身”或“保守”评价，而是几类可观察事实：你有没有把旧告示经验沉成账本与记性；你在风声前夜是先认人、先谨慎还是先顾活计；抓丁旧账是否继续以债务、人情、病身的形式压回 1946 年前后。
7. **普通生活窗口**：在工作队正式进村前，大部分年份仍由种地、借粮、照护病母、婚育与农闲短工构成。所谓“觉醒路径”必须嵌在普通生活里，不能把 1931 直接写成 1947。
8. **外部冲击**：战局、灾荒、物价、征兵、失佃、工作队真正进村的时点，都是按年份外部发生的客观冲击。人物在 1931、1937、1946 的每个选择，都不能预读 1948 之后的落点。
9. **多系统冲突**：这一段连续人生里始终是四套账互相拉扯：生计账、照护账、旧关系账、身体账。`C3/C5/C6` 的旧账若存在，会真实压缩他在土改前夜能动用的现金、名声、脚程与表态强度。
10. **修订 / 暂停 / 退出**：从“听过一次告示”到“工作队进村”，人物可以反复修订站位：继续听风声、继续埋头活计、先去认人、在旧情前缩回去、或把旧病旧债摆到台面上。任何修订都不能删除前史，只能在原有事实之上叠加。
11. **一年后**：若先命中 `rent-rights-memory`，一年后的差别主要表现为 `awake:ledger / awake:buried / awake:listening`；若在 1946 命中风声节点，则一年后的差别表现为 `land:prelisten / land:precautious / land:prebusy` 与三条抓丁专属前夜状态。
12. **五年后**：到 `1947-1948`，运行时把这条中段继续推进成两层，而不再把 `C3/C5/C6` 直接并回通用分田站位：
   - 通用分田站位：`land:cadre / land:claim / land:hesitant`
   - 三条抓丁专属“工作队真进村”中继：
     - `工作队进村·亏空账摆上桌` → `bought:team-ledger / bought:team-hold / bought:team-remote`
     - `工作队进村·旧情逼你开口` → `patronage:team-facts / patronage:team-cutoff / patronage:team-avoid`
     - `工作队进村·病身怎么认地` → `injury:team-spoke / injury:team-guard / injury:team-home`
   - 通用分田后续：`land:settle-ledger / land:settle-crop / land:settle-cautious`
   - 三条抓丁专属分田后续：
     - `bought:settle-spoken / bought:settle-hold / bought:settle-remote`
     - `patronage:state-facts / patronage:repay-cutoff / patronage:absent-hearing`
     - `injury:settle-mutualaid / injury:settle-smallplot / injury:settle-handicraft`
13. **更长期**：到 `1949`，这里只能统一回收“旧告示怎样变成站位底色、旧债怎样进入新地、旧情怎样在认人时再逼出表态、病身怎样约束新日子”，不能写成单向胜利叙事，更不能把“留在大陆”自动写成“必然已分到地”。
14. **新状态快照 P₁**：N-02 结束后的运行时快照至少应能读取：`awake:*`、`land:pre*`、`land:*`、`bought:* / patronage:* / injury:*`，以及 `mother.* / wife.*` 这类仍按独立轨迹推进的主体状态。
15. **继续 / 修改 / 退出 / 重入**：人物可继续留乡务农、在分田后续里调整活法、或因病身/脚程/关系改走更保守路线；但**不能**回到“没听过那次告示”“没背过那笔抓丁旧账”“没经历过风声前夜”的起点。

## ④ 运行时映射
- `减租告示之后·信过一次的人`
  - 触发：`awake:rights` 或 `awake:frustrated` 之后，`yearFrom:1931`
  - 结果分化：`awake:ledger / awake:buried / awake:listening`
- `风声先到·工作队未到`
  - 触发：`yearFrom:1946`
  - 约束：仅供仍停在留乡佃农主干的通用前夜使用；若已进入 `C3/C5/C6` 的抓丁专属后账、已改走 `C4` 逃亡链、或已沿 `C2` 进入 `soldier:demob-home / life:back-to-farm / life:local-power / life:cripple-vendor / life:depend-kin / life:relearn-craft` 这些“归乡兵线”，则应让位给各自专属节点，不再与之并弹。
  - 结果分化：`land:prelisten / land:precautious / land:prebusy`
- `归乡之后·旧地风声`
  - 触发：`soldier:demob-home / life:back-to-farm / life:local-power / life:cripple-vendor / life:depend-kin / life:relearn-craft` 之后，`yearFrom:1946`
  - 约束：这是 `C2` 的归乡回接层，只写“复员/伤退以后，你先怎样把旧地风声接回余生日子”，**不**把退伍兵自动写成通用留乡佃农，也不预设“已经顺利重新取得佃权”。
  - 结果分化：`soldier:return-listen / soldier:return-cautious / soldier:return-order`
- `复员在外·世道又拧起来`
  - 触发：`life:city-labor / life:security-work` 之后，`yearFrom:1946`
  - 约束：这是 `C2` 留外分支的专属多年层，专门把“先留外头 / 继续硬熬 / 又被地方枪杆卷住 / 还是把脚程往旧乡回拨”拆开；**不**把“复员后留外”压成一句背景话。
  - 结果分化：`soldier:outside-hold / soldier:outside-return / soldier:outside-drawn`
- `留外多年后·还是往旧乡走`
  - 触发：`soldier:outside-return` 之后，`yearFrom:1947`
  - 约束：这是 `C2` 留外后晚归的专属回接层，只写“晚归的人怎么重新接旧乡”，再转入既有 `soldier:return-*` 归乡兵线；**不**把晚归直接伪装成 1945 那种“一复员就到村口”的归乡。
  - 结果分化：`soldier:return-listen / soldier:return-cautious / soldier:return-order`
- `退伍在外·又一轮乱局把你往哪边推`
  - 触发：`soldier:outside-hold / soldier:outside-drawn` 之后，`yearFrom:1948`
  - 约束：这是 `C2` 留外线到 1948 的继续分化层，把“外头硬熬 / 给自己留边路 / 继续被地方枪杆牵住”拆开；**不**自动把人物并回原乡土地账。
  - 结果分化：`soldier:outside-settle-labor / soldier:outside-settle-watch / soldier:outside-settle-drawn`
- `工作队进村·分田站位`
  - 触发：`yearFrom:1947`
  - 约束：仅供仍停在留乡佃农主干中的人物继续进入分田现场；若人物已经沿 `C3/C5/C6` 走出各自的抓丁后账专属前夜，当前运行时会先让位给它们各自的 `工作队进村` 专属节点，不再把它们并回这一层通用站位。若人物已经沿 `C4` 逃亡线走出 `draft:flee / road:* / flee:settle-* / flee:return-probe|stay-away|still-drifting / flee:return-* / flee:team-* / flee:settle-*`，当前运行时**不自动**把他并回原乡分田现场，而是先让位给逃亡线自己的专属回接层。若人物已经沿 `C2` 进入 `soldier:return-*` 这条归乡回接层，或仍停在 `soldier:outside-*` 留外线，当前运行时也会先让位给各自专属节点，不把他直接并回通用 `land:*` 三分法。
  - 结果分化：`land:cadre / land:claim / land:hesitant`
- `工作队进村·归乡人怎么认旧地`
  - 触发：`soldier:return-listen / soldier:return-cautious / soldier:return-order` 之后，`yearFrom:1947`
  - 约束：这是 `C2` 归乡兵线的专属“工作队真进村”层，只写“被抓走又活着回来的人，怎样把离家、伤退、旧地和家口带进认人的现场”，**不**自动推出“已成为通用骨干 / 领田 / 迟疑”。
  - 结果分化：`soldier:team-witness / soldier:team-guarded / soldier:team-order`
- `回乡探路后·旧地还接不接得上`
  - 触发：`flee:return-probe` 之后，`yearFrom:1947`
  - 约束：这是 `C4` 逃亡线的专属回接层，只写“探过旧乡以后怎么处理旧地消息”，**不**直接并回留乡分田主干，也不自动推出“已重新取得佃权/已站进分田现场”。
  - 结果分化：`flee:return-listen / flee:return-cautious / flee:return-yield`
- `旧地风声再起·探路后的再记一笔`
  - 触发：`flee:return-listen / flee:return-cautious / flee:return-yield` 之后，`yearFrom:1948`
  - 约束：这一步仍不把人物自动并回留乡分田主干；只写“分田风声更近时，你如何继续处理旧地消息与脚程”，不推出“已站进现场 / 已领到地”，未知项仍显式保留为 `unknown`。
  - 结果分化：`flee:return-hear / flee:return-bridge / flee:return-cut`
- `逃亡归来·重新站进丈量场`
  - 触发：`flee:return-bridge` 之后，`yearFrom:1948`
  - 约束：这是 `C4` 真正返乡后的专属“重新站进土改现场”层，只写“离乡多年的人怎样把脚程、旧屋、后方落脚处和土地账一起带进场”；**不**把逃亡者改写成从未离乡的通用 `land:*` 站位。
  - 结果分化：`flee:team-facts / flee:team-edge / flee:team-withdraw`
- `返乡以后·逃亡者把地怎么接回手`
  - 触发：`flee:team-facts / flee:team-edge / flee:team-withdraw` 之后，`yearFrom:1948`
  - 约束：这是 `C4` 返乡后专属的“分田之后”层，只写“屋、人、地、后方那头活路怎样重新过成日常”；**不**把逃亡线自动并回通用 `land:settle-*`。
  - 结果分化：`flee:settle-ledger / flee:settle-edge / flee:settle-two-ends`
- 三条抓丁后账中继层
  - `买丁后账·风声再起`：`yearFrom:1946` → `bought:ledger-ready / bought:hold-life / bought:stay-migrant`
  - `旧人情与新风向`：`yearFrom:1946` → `patronage:shield-old / patronage:cut-tie / patronage:keep-head-low`
  - `病身还在·新账来到门口`：`yearFrom:1946` → `injury:show-scar / injury:guard-body / injury:hide-at-home`
- 三条抓丁后账的 `工作队真进村` 专属中继层
  - `工作队进村·亏空账摆上桌`：`yearFrom:1947` → `bought:team-ledger / bought:team-hold / bought:team-remote`
  - `工作队进村·旧情逼你开口`：`yearFrom:1947` → `patronage:team-facts / patronage:team-cutoff / patronage:team-avoid`
  - `工作队进村·病身怎么认地`：`yearFrom:1947` → `injury:team-spoke / injury:team-guard / injury:team-home`
- 分田后续节点
  - `分田之后·新账怎么过`：`yearFrom:1948` → `land:settle-ledger / land:settle-crop / land:settle-cautious`
  - `分田以后·买丁旧账怎么摊`：`yearFrom:1948`，承接 `bought:ledger-ready / bought:hold-life / bought:stay-migrant / bought:team-ledger / bought:team-hold / bought:team-remote` → `bought:settle-spoken / bought:settle-hold / bought:settle-remote`
  - `分田时候·旧情让你开口`：`yearFrom:1948`，承接 `patronage:shield-old / patronage:cut-tie / patronage:keep-head-low / patronage:team-facts / patronage:team-cutoff / patronage:team-avoid` → `patronage:state-facts / patronage:repay-cutoff / patronage:absent-hearing`
  - `分田以后·病身扛不扛得住`：`yearFrom:1948`，承接 `injury:show-scar / injury:guard-body / injury:hide-at-home / injury:team-spoke / injury:team-guard / injury:team-home` → `injury:settle-mutualaid / injury:settle-smallplot / injury:settle-handicraft`
  - `分田以后·归乡人的日子怎么接`：`yearFrom:1948`，承接 `soldier:team-witness / soldier:team-guarded / soldier:team-order` → `soldier:settle-ledger / soldier:settle-mutual / soldier:settle-watch`

## ⑤ 与 1949 总账的事实回收
- `demo.html` 终局不再把“留在大陆”自动写成“必然已经分到地”；若没有走出 `land:cadre / land:claim / land:hesitant`，则只能回收到“土地账被重新摊开，但本轮具体站位仍未走实”。
- 若人物已改走 `C4` 逃亡线，则 `demo.html` 终局也**不再**把“留在大陆”自动写成“仍会回到原乡分田现场”；当前只按“如何离乡、如何落脚、战后如何面对旧地”的事实链回收，回乡后是否真正接入土改现场仍保留 `unknown`。
- 本轮补入 `flee:return-listen / flee:return-cautious / flee:return-yield` 后，`C4` 不再只停在“探过旧乡”这一句，而能继续按事实回收到“先听清旧地风声 / 谨慎续回屋人与家口 / 承认旧地接不上原样”三种不同后果；仍然不做价值排名，也不把它们伪装成已并回留乡主干。
- 在此基础上，若继续走到 `1948`，运行时还会再记一笔“旧地风声再起”：`flee:return-hear / flee:return-bridge / flee:return-cut` 会被一并纳入 `buildFinalLedgerFacts()` 的事实回收，用来写清“你是继续听消息、再回去试着续一点，还是把旧地彻底收回记忆里”；仍不自动推出“已站进分田现场”。
- `rent:reduced / awake:frustrated / awake:ledger / awake:listening / awake:buried / land:prelisten / land:precautious / land:prebusy` 已接入 `buildFinalLedgerFacts()`，统一按事实句回收，不做价值排名。
- 现在 `C3/C5/C6` 不只回收到“风声前夜”，也能继续回收到“工作队真进村时旧账如何再次摆上桌”，再继续进入“分田之后怎么把旧账、新地、病身、人情过下去”。
- `C3/C5/C6` 在 `1938` 的**二次摊派 / 二次名额转嫁**现在也会进入终局回收：`bought:off-again|bought:off-again-failed / patronage:exempt-again|patronage:failed-again / draft:exempt-health-again|fakesick:exposed-again / quota:shifted-again / quota:again-*` 不再只留在过程日志里，而会在 `demo.html` 的终局事实句中被写回。
- 本轮再把这层“二次名额转嫁”正式推进进**主链矩阵 + 真实自然回放 + unknown 专门覆盖**三层统一验收：`买丁 / 托情 / 病身免役` 三条留乡主分流现在都要显性回答 `名额压力 = 二次已转嫁`，并继续保留“第二张名额具体落到哪一户仍 unknown”的边界，不得只在第一次转嫁时才验收这层制度缺口。
- `demo.html` 终局现已额外区分 `bought:team-* / patronage:team-* / injury:team-*` 这一层：若本轮只走到 `1947` 的专属“工作队进村”节点，还没有继续走到 `1948` 分田后续，终局会按“已经走到工作队真进村、但分田之后尚未全部写实”的事实回收，而不再误判成通用 `land:*` 站位。
- `C2` 的归乡兵线现在也已接入 `1949` 总账：`soldier:return-* / soldier:team-* / soldier:settle-*` 会统一回收到“活着回乡以后，怎样重新听旧地风声、怎样把抓丁与离家的旧账带进工作队进村的现场、以及分田以后怎样把新地和余生重新接起来”；仍不做价值排名，也不把退伍兵伪装成通用 `land:*` 三分法。
- `C2` 的留外分支现在也继续接入 `1949` 总账：`soldier:outside-*` 会统一回收到“退伍后在外头怎样把日子熬成常年、怎样把脚程往旧乡回拨、以及怎样再次被地方枪杆和场面卷住”的事实链；仍不做价值排名，也不把人物自动写回原乡土地账。
- `C4` 现也已接入更深一层的 `1949` 总账：除了 `draft:flee / flee:carry-mother|left-mother|mother-stayed / road:* / relief:dependent / flee:settle-* / flee:return-probe|stay-away|still-drifting` 之外，`flee:return-hear|bridge|cut / flee:team-* / flee:settle-*` 也会统一回收到“如何离乡、如何过路、如何落脚、怎样真的重新站进返乡后的丈量场、以及返乡后屋与地怎样接回手”的事实链，不做价值排名。

---

# 主家庭复用模板（本轮抽取）
> 这一节只抽取当前 `actions-data.js / choices-data.js / demo.html` 已真实共用的设计做法，不补写不存在的系统。目标是把“苏北贫农家样板”复制到 `江南沈家 / 上海工商家 / 其他主家庭` 时，有一套可复用的状态字段、规则层、节点模式与验证口径。

## A. 已实现的共用状态字段
| 层 | 字段 | 当前状态 | 说明 |
|---|---|---|---|
| 基础快照 | `family / track / origin / year / age / gender` | 已实现 | 家庭、当前主路径、借用的 origin 行动表与年份年龄 |
| 人物能力 | `attrs.{body,knowledge,craft,mind,network,fame}` | 已实现 | 六维累计值，供 `gate / chanceBy / optionEnabled` 共用 |
| 有限资源 | `res.{money,health,relation,position}` | 已实现 | 钱财/健康/关系账/处境，共用同一人生资源池 |
| 过程记忆 | `echoes[]` | 已实现 | 当前最通用的“已发生事实”存储层；主链多靠它跨年传递 |
| 事件记账 | `firedEvents[]` | 已实现 | 年度事件命中记录，与情境节点相互守门 |
| 主体状态 | `subjects.mother / subjects.wife / subjects.father / subjects.household / subjects.marriage / subjects.ledger / subjects.workers / subjects.support / subjects.connections / subjects.meta` | 已实现（扩展中） | 病母、妻已接入苏北主树；`江南沈家 / 上海工商家 / 纱厂女工` 已接到父亲、家内、婚家、账房、工友家口与 `support` 回应，并继续延伸到 `1948-1949` 的晚段收束；此前已把共享 `海外多年轨` 的 `旧同学 / 旧门路 / 亲眷` 收进 `subjects.connections`，又把 `soldier` 直玩入口与苏北从军支链并进同一 `connections` 状态槽；本轮再把 `苏北留乡 workteam / 分田后账` 里 `买丁 / 托情 / 病身免役` 的门内安身底盘正式抽进 `subjects.support`，并把 `纱厂女工` 的 `labor:postwar-ledger / labor:settle-ledger` 正式抽成 `subjects.ledger`，使“工账票据 / 代写记账 / 认票据字路”不再只混在工友或外部门路文案里；同时继续把沈家/顾家同一家族内的多条外部门路抽成“跨组可并账、同组取最新”的分组回收口径，避免同一主体只剩一句总括或把 `open / return / settled` 三段重复叠账；`归乡士兵` 的 `旧乡风声 / 邻里认人 / 旧屋旧地回接` 也已沿同一套 `connections + returnRouteLandings[]` 晚段结构接入整局比较，不再只停在士兵线自说自话 |
| 派生事实 | `draftStatus / tenantRight / quotaPressure / landStage` | 已实现（苏北为主） | `demo.html` HUD / 终局页当前可见的事实收束层 |
| 联动字段 | `payload.supportInstitutionPlacements[]` | 已实现 | 把既有 `support × institution` 联动进一步收成字段层，统一返回 `currentKnownLabel / currentPlacementLabel / recordedBy / evidenceLabels / missingEvidenceLabels`，显性回答“制度风声里是谁先把人接住” |

## B. 已实现的共用规则层
- **入口层**：`family -> track -> origin` 三层分离。出生家庭不等于人生路径，路径再借用 origin 的行动表与年度事件。
- **守门层**：`triggerSatisfied()` 已同时用于情境级与选项级触发，`yearFrom / yearTo / age / hasEcho / noneEcho / sex` 在 UI 与执行层一致生效。
- **家庭层**：`triggerSatisfied()` 现已补入 `family / families` 守门，因此同一条 `origin` 可以继续按 `江南沈家 / 上海工商家` 这类家庭细分专属中继，而不用复制整条通用路径；本轮又把这层守门与 `family profile` 打通，`scholar / capitalist / gentrywoman` 直玩入口若没有显式 `family`，也会按对应 profile 命中家族专属主干，不再只在 HUD 里像主家庭、过程里却接不到主节点。
- **家庭判定优先级层**：一旦状态里已经显式写入 `family`，后续 `HUD / 终局 / 主体规则 / family helper` 都必须先服从这个家庭，再考虑 `origin` 默认 profile。否则像 `shanghaigongshang + gentrywoman` 这类组合会在晚段被错误回落到 `江南沈家` 口径，造成“主链能走通，但终局总账串树”的假闭环。
- **路径兼容层**：家庭模式一旦从 `tenant` 走成 `soldier-path / flee-refugee`，后续专属情境不能只认旧的 `tenant` 直玩入口；应同时对 `tenant + soldier` 或 `tenant + refugee` 开放，避免“家族开局能走上路径，却接不到后段多年节点”。
- **主体层**：病母/妻已落地“独立回应 / 不可由玩家直写”的最小规则；当前又把这层口径继续抽成 `demo.html` 里的 `FAMILY_SUBJECT_RULES / FAMILY_SUBJECT_LABELS`，并进一步补出 `AUX_SUBJECT_RULES / AUX_SUBJECT_LABELS`，把 `江南沈家 / 上海工商家 / 纱厂女工` 里已经真实存在的 `father / household / marriage / ledger / workers` 回应集中成可复用规则表。玩家仍只决定自己的做法，父亲、家门、婚家、账房与工友家口如何回应，必须由运行时按既有回响自动推进；并且这层规则现在不再只停在战时/战后，也覆盖到 `前战门路 / 金圆券前夜 / 风声再紧 / 后半生安放` 的晚段。`soldier:missing` 这一层现已进一步拆成“统一多年确认骨架 + 妻子独立去留回应”两层：确认年份可模板化，妻子的去留与名分确认仍不可被玩家直写。
- **主体层补充（可复用实现）**：为避免病母/妻的规则继续散落在 `advanceSubjects()` 的长 if-else 中，运行时已把苏北样板里的两类主体也抽成规则表：`MOTHER_RULES / WIFE_RULES + applySubjectRules()`。它们只负责“同一年如何回应与推进”，不新增史实设定、不补写真相，未知项继续保留 `unknown`。
- **非家庭主体层补充（共享海外轨）**：`abroad:*` 不应只靠主角自己的回响自述。当前运行时已新增 `overseas` 辅助主体 profile，用 `subjects.connections + AUX_SUBJECT_RULES` 把 `旧同学 / 旧门路 / 亲眷` 这层关系接进 HUD 与终局；当人物真正走上共享海外轨时，这层非家庭主体回应应优先让位出来。
- **非家庭主体层补充（主家庭非海外轨）**：`江南沈家 / 上海工商家` 的主干也不应只靠家门主体与主角 echo 两层承重。当前运行时已继续复用同一个 `subjects.connections`，把沈家的 `门生 / 课脉 / 报馆旧交 / 学会名帖`，以及工商家的 `同业会馆 / 熟客旧交 / 赈济联系人 / 公共门路` 接进 HUD 与终局；这样 `books/public/teach`、`biz:postwar-public/gold-burn/settle-public`、`shdaughter:*public` 这些分流都开始具备第三层“外部门路主体回应”。
- **同主体多门路分组层**：当 `江南沈家 / 上海工商家` 同一条人生线里真的会留下两组以上外部门路时，运行时不应只保留最后一句 `connections.response`。当前规则已补成“跨组可并账、同组取最新”的分组回收：例如 `门生/课脉 + 旧同事细路`、`借住屋檐 + 互帮小作` 可以同时进入 HUD / 终局，但同一组里的 `open / return / settled` 仍只保留当前阶段最新那层，避免重复叠账。
- **主体牵制层**：当同一条主线里已经同时存在 `婚家 + 门生/信札`、`账房 + 工友 + 熟客小网`、`工友账册 + 代写记账细路`、`家门 + 屋檐/小作` 这类多主体时，运行时不应只把它们平铺成几条并列事实，还应显性写出“谁先让位、谁把后半生先顶住”的关系。当前 `demo.html` 已新增 `AUX_SUBJECT_TENSION_RULES`，并把它接入 HUD 与 `buildFinalLedgerFacts()`：例如沈家女线会显性回收“婚家未退净，信札/门生先顶上来”，工商家男线会显性回收“账房先给熟客与工友分账让位”，纱厂女工线会显性回收“工友账册没有独自收住后半生，代写与记账细路并着顶上来”，工商家女线则会回收“家门先让位，屋檐与小作并着顶上来”。这层规则只能建立在当前运行时真实保留的主体状态上，若某主体状态仍是 `unknown / n/a`，就不得硬写成已发生事实。
- **主体牵制层补充（纯门内收束）**：若某条主家庭线晚段已经真实进入“家门/婚家退后，只剩门内安身先把日子接住”的状态，就不应只在 `家门回应 / 后半生安放` 里概述带过；应把这层也补成 `AUX_SUBJECT_TENSION_RULES + MAINLINE_COVERAGE_SCENARIOS + NATURAL_PLAYTHROUGH_SCENARIOS` 的双层显式断言。当前 `江南沈家 / 上海工商家` 男女线已补出这组 `家门/婚家 + 安身主体` 晚段样板；同时运行时也已把 `AUX_SUBJECT_TENSION_RULES` 的 `noneEcho` 守门接通，避免这类纯收束规则反过来遮掉仍带 `public / teach / workroom + household` 的 mixed 牵制代表态。
- **主体牵制原型层**：当 `AUX_SUBJECT_TENSION_RULES` 已不再只是几条 route-local 句子后，不应继续只靠“这条句子有没有被锁住”来判断整局复用度。运行时现已新增 `window.__DEMO.inspectTensionArchetypeCoverage()`，先把 `父辈/家门 / 婚家 / 妻子病母` 这类家庭侧事实从 `familySubjects` 与 `subjects` 两侧统一归并成**家庭锚点**，再保留 `账房 / 工友家口 / 安身主体` 这簇具体支撑主体与 `外部门路` 的差异，把牵制结构压成可比较的原型矩阵，用来回答“哪些主体组合已经跨路线站稳、哪些仍只适合停在单路线样板里”。这样像 `household` 只是落在不同槽位的工商家/沈家规则，就不会被误算成新的单路线候选；若没有新增运行时证据，则继续保留 route-local，不为对称硬做跨线共模。
- **主体牵制安身簇子类层**：当 `主体牵制支撑簇层` 已经能稳定回答“有没有安身簇”后，不应继续把 `安身 / 工友 / 账房/工友 / 工友/安身` 这些中层差异一并压扁。运行时现已新增 `window.__DEMO.inspectTensionSupportClusterCoverage()`，把真实支撑主体继续压成**安身簇子类矩阵**，专门回答“哪类安身簇子类已经跨路线复用、哪些 route-local 细组合其实只差家庭锚点或外部门路组合还没出现第二条真实路线”。这样 `江南沈家 / 上海工商家 / 纱厂女工` 的一批主家庭树前沿，就不会再只停在“共享支撑簇已成立”这一层过粗判断。
- **主体牵制支撑簇层**：当 `主体牵制原型层` 已经能稳定识别 `家庭锚点` 后，不应继续只在“细主体组合”与“回应/角色原型”之间跳读。运行时现已新增 `window.__DEMO.inspectTensionSupportCoreCoverage()`，继续把牵制结构保守压成 `家庭锚点 + 安身簇 + 外部门路` 这层**支撑簇矩阵**：先回答 `familyAnchor` 是否成立，再回答这条牵制里有没有 `supportCore/supportCluster` 与 `connections`。这样 `江南沈家 / 上海工商家` 当前大量看似不同的 `父辈/家门 + 安身主体/工友/家口 + 外部门路`、`婚家 + 安身主体/工友/家口`，就能先在“共享支撑簇已成立”这一层会合；若最终仍缺第二条细主体组合证据，就继续保留 route-local，不为整齐感硬把细组合抹平。
- **主体规则 schema 层**：当 `苏北贫农家` 已经不再只是单树样板，而 `江南沈家 / 上海工商家 / 纱厂女工 / 难民 / 海外多年轨` 也都各自接上 `support / connections / familySubjects / AUX_SUBJECT_RULES` 后，不应继续只靠肉眼判断“这些主树到底迁进了哪一层主体模板”。运行时现已新增 `window.__DEMO.inspectSubjectRuleSchemaCoverage()`，把 `FAMILY_SUBJECT_RULES / AUX_SUBJECT_RULES / AUX_SUBJECT_TENSION_RULES` 与 `家门主体槽 / 亲缘婚家槽 / 营生工友槽 / 安身承接槽 / 外部门路槽` 一起压成主体规则 schema 矩阵；当前 `schemaReadyRouteCount = 10 / 10`，其中 **7 / 10** 已具备“锚点或营生 + 安身承接 + 外部门路”三槽结构。这样后续就能直接区分“整树已经有共享主体 schema，只剩局部组合仍应 keep route-local”，还是“规则层本身仍缺槽位/规则表/双层验证”。
- **主体牵制角色原型层**：当上面这层细粒度主体组合已经足够多时，不应再只比较“病母/妻子/父辈/账房/工友”这些具体字面是否相似。运行时现已新增 `window.__DEMO.inspectTensionRoleArchetypeCoverage()`，继续把它们保守压成 `家内/家门主体 / 安身主体 / 外部门路` 三类角色组合，回答“哪类角色分工已经跨路线站稳、哪类仍只适合停在 route-local”；这层只做角色归类，不新增具体史实或关系细节。
- **终局收束原型层**：当各主路线的 `后半生安放 + buildFinalLedgerFacts()` 已经都能稳定落地后，不应继续只靠逐条读终局句来判断“整局最后都收成了哪些活法”。运行时现已新增 `window.__DEMO.inspectSettlementArchetypeCoverage()`，只按当前真实 outcome label 把收束方式归并成 `归乡收束 / 家门安身 / 自立谋生 / 外路延展 / 时局收束` 等可比较原型；若某个结果当前还只能在单路线稳妥成立，就继续保留 literal route-local，不为对称硬做整局共模。
- **非家庭主体层补充（逃亡/难民共享轨）**：`flee-refugee` 虽然保留“苏北旧账并账”的例外口径，但不应因此失去 `亲族屋檐 / 捎话旧路 / 旧街坊` 这一层外部门路。当前运行时已让 `flee-refugee` 复用 `refugee` 的 `subjects.connections + AUX_SUBJECT_RULES`，使逃亡返乡线也能在保留 `病母 / 妻 / 土地旧账` 的同时，把外头接应关系显性写进 HUD 与终局；本轮又进一步把 `refugee` 的后半生变体推进成 `AUX_SUBJECT_TENSION_RULES.refugee`，因此 `后方脚程留后路 / 返乡靠亲邻守住 / 返乡后仍两头并行` 这几类代表态，不再只剩结果名词，而会显性写出“后方屋檐未独自兜住后半生”“旧屋先接回但后方活路未退净”这一层让位关系。
- **非家庭主体层补充（苏北从军轨）**：`soldier-path` 不应长期只有 `病母 / 妻` 两层家内回应。当前运行时已把苏北从军线里已真实存在的 `军中搭伙 / 旧鞋欠账 / 旧乡风声 / 邻里认人 / 铺位差事 / 旧袍泽旧路 / 藏身旧识 / 退身旁路` 收进同一个 `subeipoor.connections` 桶，由 `AUX_SUBJECT_RULES` 按 `军中 / 归乡 / 留外 / 顺势 / 潜躲` 五类状态自动判读；这样从军线的外部关系终于能和 `flee-refugee` 一样进入 HUD 与终局，而不是只留在年度事件描述里。
- **非家庭主体层补充（苏北留乡分流）**：`买丁 / 托情 / 病身免役 / 名额转嫁` 也不应只剩 `quota:* / bought:* / patronage:* / injury:*` 这些过程回响与总账句。当前运行时已继续复用同一个 `subeipoor.connections`，把 `名额后账 / 村里口风 / 旧账口风 / 短工去路 / 旧情门路 / 村里风声 / 轻手艺门路 / 零碎活路` 接进 HUD 与终局；这样苏北主树不再是“从军/逃亡有外部门路，留乡分流只有主角自述”的两套世界。本轮又进一步把 `买丁 / 托情 / 病身免役` 走到 `workteam -> 分田后账` 的晚段时，`家口与能接住的那份地 / 家口先把旧情后的日子稳住 / 病身与轻手艺先把日子接住` 这层门内安身底盘正式抽进 `subeipoor.support`，并同步接入 `support × institution` 联动验收；与此同时，也把这三条线的 `病母后事已办 + 妻已进门过日子 + support + connections` mixed 态正式推进进 `AUX_SUBJECT_TENSION_RULES.subeipoor`，让苏北留乡主分流也能像从军/逃亡线一样显性回答“谁把分田后的日子接住”；证据暂不足的 `名额转嫁 quota:*` 仍继续停在并账层，保留 `unknown`。
- **名额后账回应字段层**：`quota:quiet / quota:small-gift / quota:run-errand` 这组后账回响，不应长期只停在 “`quotaRippleResponses[]` 已有 `recordedBy / evidenceLabels / missingEvidenceLabels`” 的层级。当前运行时已继续把这组边界推进成 `rippleKind / rippleKindLabel / rippleTier / rippleTierLabel / currentRippleLabel` 五层结构，显性回答“这笔后账目前压成了闭门不问、递小礼，还是跑差补账，以及停在首轮还是二次后账”；但对 `对方心账 / 村里口风 / 跑腿差事最后算不算抵账` 这类细节，仍继续保留 `unknown`。
- **多主体并账快照层**：当同一条主线同时存在 `婚家 + 门生旧交`、`账房 + 工友家口 + 同业旧交`、`病母/妻 + 亲族旧路` 这类多层主体时，HUD 不应继续只显示一个 `currentAuxSubjectFact()`。当前运行时已把这层收成 `currentAuxSubjectFacts()` / `withAuxSubjectFacts()`：辅助主体不再是单条插槽，而是可按 profile 顺序稳定回收成一组事实；`苏北逃亡线` 则继续保留“家中主体 + 外部门路”并账的例外口径。
- **主体 profile 继承层**：家庭主体规则不能只在 `family` 开局下成立。当前运行时已补出 `currentFamilyProfileKey()` 与 `initSubjects()` 的 profile 继承口径：`scholar -> 江南沈家`、`capitalist -> 上海工商家`、`gentrywoman -> 江南沈家（非工商家时）`，因此直玩 `origin` 时也会继承对应的 `father / household / marriage / ledger / workers` 主体底色；本轮又进一步把同一 profile 回退到 `choices-data.js / demo.html` 的 `family` 守门，使直玩入口不仅主体不断，而且能实际进入对应家庭的前战/战时/战后/晚段主节点、家族动作与年度事件。
- **确认层**：当一条主线出现“失联 / 折损但无准信”的事实时，不应立刻把它硬写成死亡或团圆；应允许运行时按“发生 → 多年无准信 → 事实确认”推进。当前 `refugee:road-loss -> refugee:loss-*`、`labor:injury-*`、`labor:blacklist-*` 与 `soldier:missing-*` 的时间推进口径都已开始收敛进 `LONG_CONFIRM_RULES`；其中 `soldier:missing` 仍额外保留妻子独立回应层，不因统一规则而抹掉主体性。
- **确认证据层**：多年确认不应只停在 `ongoing / prolonged / confirmed` 三段标签。当前运行时已把这层继续推进成统一证据快照：每条 `longConfirmations[]` 现在都必须能回答 `confirmedFactLabel`（到底确认了哪类记账事实）、`recordedBy`（靠谁把事实记下来）、`evidenceLabels`（这次运行里已经落地了哪些证据回响）与 `missingEvidenceLabels`（仍缺哪类证据，继续保留 `unknown`）；后续若要扩更细字段，应优先沿这四层继续抽象，而不是退回单条文案硬写。
- **确认真相边界字段层**：多年确认即使已经具备证据层，也不应继续把“当前到底只确认到哪一层、仍缺哪类真相”埋在叙事文案里。当前运行时已把这层继续推进到 `longConfirmations[]`：每条记录还会统一返回 `currentTruthLabel / truthUnknownLabel`，显性回答“这次运行只确认到记账事实、还是已经拖成长期约束、仍具体缺哪一类真相”；这样 `士兵失联 / 难民折损 / 女工伤病 / 女工黑名单` 就能沿同一字段口径被矩阵与自然回放一起验收。
- **确认后续去向字段层**：当多年确认已经稳定带出“活着的人先把名分与日子记稳，但更往后的去向仍是 unknown”这一层时，不应继续只把它塞在 `longConfirmations[]` 的附带布尔标记里。当前运行时已把 `soldier:missing` 的这层边界继续抽成 `longConfirmFollowups[]`，统一返回 `currentPlacementLabel / recordedBy / evidenceLabels / missingEvidenceLabels`；这样“留家守日子 / 回娘家安身”与“后续婚嫁去向仍 unknown”终于也能按字段层被主链矩阵与自然回放共同验收。
- **确认后安身承接字段层**：当某条主路线已经同时具备 `support` 与 `longConfirmations[]` 时，不应长期停在“联动已被看见”的层级；还应继续显性回答“多年后账后来由哪层安身主体接住”。当前运行时已把这层推进成 `supportLongConfirmPlacements[]`，统一返回 `currentPlacementLabel / confirmedFactLabel / currentTruthLabel / recordedBy / evidenceLabels / missingEvidenceLabels`；这样 `难民折损 / 女工伤病 / 女工黑名单` 这类多年后账，就能和 `后方屋檐 / 回乡照应 / 宿舍稳饭` 的安身层按同一字段口径共同验收。
- **主体缺席层**：一旦主线已经进入 `missing / 无准信 / 按亡故记账` 这类“本人不再是可观测主体”的阶段，就不应继续拿主角 `health` 当作“仍在场的身体”去触发普通健康终局；这时应把后续闭环交回 `家内主体回应 + 1949 专属晚段节点 + stay-or-go-1949`。当前 `soldier:missing` 已按这套口径处理，后续若补更多 `loss / disappeared` 类多年线，也应优先复用这条规则，而不是为单线硬开特判。
- **确认事实可见层**：主体一旦进入“已确认但已退场”的状态，也不应因为 `present:false` 就从 HUD / 终局事实快照里消失。当前 `病母亡故 -> mother:buried / mother:buried-plain` 已补成这套口径的首个显式样板：后事确认后，病母不再继续吃行动与状态推进，但“母亲后事已办”仍必须进入 `currentAuxSubjectFacts()`、HUD 与总账，用来回答“事实有没有被系统真正收回”。运行时里，这层口径现在也不只停在事实推导，而会继续把已确认退场主体的 `response/status` 同步回主体快照，避免 HUD 已更新、主体快照却还停在旧状态。
- **复杂终局门槛层（共享海外轨）**：`stay-or-go-1949` 的 `end:hktw / end:overseas` 不应永远只认 `money`。若人物已经把共享海外轨走到 `abroad:late-study / abroad:late-trade / abroad:late-kin` 这类晚段，就说明前面若干年已经把 `学位 / 事务 / 亲眷住处` 收成真实后路；这层后路在 `1949` 应可替代纯现金船票。当前运行时已把这条口径收进 `choices-data.js` 的公共门槛与代价判定：满足 `abroad:late-*` 时，这两类复杂终局不再额外要求同额现金，也不再重复扣除同额 `money`，避免“矩阵里能预览、自然线里永远攒不够盘缠”的假闭环。
- **阶段层**：同一主链按“入口抉择 → 同年结算 → 跨年后账 → 战时/制度冲击 → 1949 事实回收”分层；后段节点用 `noneEcho / yearTo` 让前段节点自动让位。
- **行动层**：日常行动不应永远是一张静态表；当主线已经走到军中、复员留外、归乡接地这类不同阶段时，行动也应按 `track / yearFrom / yearTo / anyEcho / noneEcho` 分阶段显隐。当前 `soldier` 线已开始用这一层把“军中日常 / 退伍在外 / 归乡后接家口”拆成不同可玩动作；并把“现役动作在退伍/归乡后必须自动退场”也正式收进同一层规则，而不是靠 UI 临时遮挡。
- **终局层**：`endingVerdict()` 负责第一页主叙事，`buildFinalLedgerFacts()` 负责 `1949总账 / 事实回收`；两层都只写事实链，不做价值排名。
- **家内回应层**：同一家庭不应只靠主角 echo 自说自话；当 `婚约裂口 / 家门南迁 / 接班开口 / 金圆券前夜先保什么 / 风声再紧后把后半生收在哪一层` 这类事实已发生时，应同时给 `father / household` 一个自动回应；若该线已经显性写出 `婚家 / 账房 / 工友家口` 这种补充主体，也应一并自动回应，并显性回收到 HUD 与终局。

## C. 可复用节点模式
| 模式 | 作用 | 当前例子 |
|---|---|---|
| `入口抉择节点` | 把“会不会发生”拆成玩家可选入口 | `保长上门·派丁`、`父母之命·婚约`、`父病接班·先稳哪一笔` |
| `同年中继节点` | 把“想这样做”与“现实真成了什么”拆开 | `买丁桌上·收钱算不算数`、`托情桌上·这回缓不缓得下` |
| `跨年后账节点` | 把一时选择沉淀成几年后的活法 | `买丁之后·几年后的亏空`、`抗婚以后·先把日子接住` |
| `时代冲击节点` | 把抗战/通胀/工作队进村写成各线自己的回应层 | `工作队进村·旧情逼你开口`、`战火压厂·先把家业往哪边挪` |
| `终局回收节点` | 把 1949 去留与前史挂回事实句 | `stay-or-go-1949` + `endingVerdict / buildFinalLedgerFacts` |
| `多年确认节点` | 把“失联/折损”推进成“多年无准信 → 后来如何记账”；若牵涉他人主体去留，则再叠一层线内回应表 | `soldier:missing-prolonged / soldier:missing-confirmed` + `wife:missing-*`、`refugee:loss-prolonged / refugee:loss-confirmed` |

## D. 验证方式模板
- 语法层：`node --check assets/choices-data.js`
- 命中层：用 `window.__DEMO.runScript()` / `startFamily()` / `previewEnding()` 做目标性年份推进，验证主链是否真的能从前段走到 `1949`
- 自然回放快照层：凡走 `runScriptedTimelineForTest()` 的检查器，后续读取 payload 时必须优先使用 `run.snapshot`（必要时再回退 `run.state`），不得再用空参 `inspectStateForTest({})` 回读全局 `G`；否则上一条脚本的终态会污染当前场景，尤其会把 `recordedBy / recordedByArchetypes / institutionDetails` 这类来源矩阵与父级总表算偏。
- 终局层：检查 `endingVerdict()` 与 `buildFinalLedgerFacts()` 是否双接线，避免“过程有 echo，终局却吞没”
- 五段锚点并回总表层：当 `inspectFamilyPhaseAtlasCoverage()` 已能稳定回答每一行 `前提 / 选择 / 跨年后果 / 长期结果 / 终局回收` 的代表节点后，不要再让这层停在独立检查器里；应继续确认 `inspectFamilyRouteAtlasCoverage()`、`inspectGlobalMainlineBoard()` 与 `inspectWholeGameProgressPlan()` 也能直接带出 `primaryPhaseSummary / phaseGapSummary / topPhaseSlotSummaries`。这样后续排优先级时，才能同时回答“哪棵树当前最该补”和“它的闭环事实锚点究竟落在哪几组真实节点上”。
- 九行叙事闭环矩阵层：当 `inspectWholeGameClosureAtlas()` 已能稳定回答“九行是否闭环”后，还应再跑 `window.__DEMO.inspectWholeGameNarrativeClosureMatrix()`。这里不新增任何剧情，只把每一行已经真实存在的 `五段链路` 与 `前提 / 制度过程态 / 跨年后果 / 时代冲击回应 / 主体回应 / 长期结果 / 终局回收` 七维事实并排读出来，专门检查“哪一整行已经能同时说清楚主要人生链路与系统层承接、哪一维仍只到单层验证或仍该保守记成 `unknown`”。只有把“闭环 ready”继续推进成“这条人生链具体由什么事实撑住”也可读，后续才不会又退回围着单枝或成熟库存反复修边。
- 牵制层：对已有多主体混合态，额外检查 HUD 与终局是否**同时**回收“主体牵制”事实，避免只把多个主体平铺显示，却没有把“谁先让位、谁留下后账”的关系真正写回系统。
- 牵制锁定层：用 `window.__DEMO.inspectTensionRuleLocks()` 枚举 `AUX_SUBJECT_TENSION_RULES`，检查每条牵制句是否都被“主链矩阵 + 自然回放”双层显式断言锁住；未锁定的规则不得默认为稳定成立，继续保留 `unknown`。
- 混合态筛查层：当上一层已经全部锁住后，不要凭印象去找“下一条该升格的牵制句”。先跑 `window.__DEMO.inspectMixedPromotionGaps()`，把所有仍只停在 `mixed` 并账层、尚未显性断言 `主体牵制` 的代表态列出来，再决定哪些值得继续补证据、哪些应长期停在并账/矩阵层。
- mixed 补锁判定：若某条组合仍被 `inspectMixedPromotionGaps()` 列成 remaining gap，但 `previewEnding()` 与对应自然脚本都已稳定命中同一句既有 `主体牵制`，说明裂口不在规则层、而在验收锁定层；此时应优先补 `MAINLINE_COVERAGE_SCENARIOS + NATURAL_PLAYTHROUGH_SCENARIOS` 的显式断言，而不是再新造一条牵制规则。
- mixed 证据优先级：若要证明某个 mixed 态已经成为“第二条真实路线”或“第二组真实来源”，优先补**真实年份自然回放**，不要只补静态 `state.echoes` 快照。像 `上海工商家` 本轮新增的 `postwar-route + settle-household`、`postwar-coop + settle-household`，就是靠 `natural-biz-route-household-mixed / natural-biz-coop-household-mixed` 才算新增运行时证据；单独捏一个静态快照，即使命中了 HUD，也不应直接记成新的整局模板。
- `主体牵制原型矩阵层`：当 `remaining frontier` 已全部锁成 `keep-unknown`、而 `外围主体分层` 也已不再稀薄后，不应继续只拿 `inspectTensionRuleLocks()` 逐句点名。还应再跑 `window.__DEMO.inspectTensionArchetypeCoverage()`，把现有牵制结构按 `familySubjectKeys / subjectKeys / hasConnections` 收成整局矩阵，显性比较 `病母/妻子 + 外部门路`、`父辈/家门 + 外部门路`、`账房/工友 + 外部门路`、`外部门路内部牵制` 这类原型究竟已经跨几条路线、是否已双层锁住；只有把“句子级锁定”推进成“原型级比较”，后续才不会继续围着某一条牵制句局部修边。
- `主体规则 schema 矩阵层`：当 `inspectTemplateExtractionCoverage()` 与 `inspectTemplateLayerReuseCoverage()` 已经能回答“模板层有没有迁过去”后，还应再跑 `window.__DEMO.inspectSubjectRuleSchemaCoverage()`。这里不新增任何剧情事实，只继续比较“哪些主路线已经共享 `家门主体槽 / 亲缘婚家槽 / 营生工友槽 / 安身承接槽 / 外部门路槽` 的主体 schema、哪些规则表已经成共模、哪些路线只剩局部组合仍该保守 route-local”；同时这层结果也应回流到 `inspectTemplateLayerReuseCoverage()` 与 `inspectGlobalCompleteness()`，让“整树已有 schema，只剩 local combo”成为运行时可回归结论，而不是推进记录里的人工判断。
- `主体牵制安身簇子类矩阵层`：当 `主体牵制支撑簇矩阵层` 已经证明“这条路线确实有安身簇”后，还应再跑 `window.__DEMO.inspectTensionSupportClusterCoverage()`，把现有牵制继续压成 `安身 / 工友 / 账房/工友 / 工友/安身` 这层中矩阵。这里不替任何路线补新事实，只继续比较“哪类安身簇子类已经跨路线复用、哪些主家庭树当前只是家庭锚点或外部门路组合仍保守 route-local”；同时 `inspectRouteLocalCandidateBlockers()` 也应读取这层结果，必要时明确返回 `shared-support-cluster-template-but-local-combo`，避免只知道“共享安身簇已成立”，却仍说不清到底是哪个中层支撑子类已经共享。
- `主体牵制支撑簇矩阵层`：当 `主体牵制原型矩阵` 已经稳定把 `家庭锚点` 与 `支撑主体` 分开后，还应再跑 `window.__DEMO.inspectTensionSupportCoreCoverage()`，把现有牵制继续压成 `家庭锚点 + 安身簇 + 外部门路` 这层中矩阵。这里不替任何路线补新事实，只回答“这组主家庭树牵制是否已经共享同一类支撑簇结构、而哪些细主体组合仍应保留 route-local”；同时 `inspectRouteLocalCandidateBlockers()` 也应读取这层结果，必要时明确返回 `shared-support-core-template-but-local-combo`，避免明明已经共享 `安身簇`，却仍把 `沈家 / 工商家` 整棵树误记成“毫无共同模板”的旧口径。
- `主体牵制回应原型矩阵层`：当细粒度主体原型已经稳定后，还应再跑 `window.__DEMO.inspectTensionResponseArchetypeCoverage()`，把现有牵制继续压成“门内未稳以前外部门路先顶上来 / 门内接稳以后外部门路退成后手 / 原有底盘未独自撑住时别路先顶上来 / 前一层牵扯退后，现成活路把日子接住”这类回应姿态矩阵。这里仍不替任何路线补新事实，只回答“回应姿态是否已经跨路线可比较、具体主体组合是否仍该保留 route-local”；同时要记得吸收已被真实运行时证明只是字面变体的写法，例如 `退成门外后手` 仍应并回“门内接稳以后，外部门路退成后手”，`先给……让位` 也仍应并回“原有底盘未独自撑住，另一层活路先顶上来”，而 `婚家退到门后，……把门内日子接住` 也应并回“前一层牵扯退后，现成活路把日子接住”，不要把模板口径裂成新的 literal 候选。只有把“主体组合比较”再推进成“回应姿态比较”，后续才更容易判断 `沈家 / 工商家` 这类主家庭树卡住的究竟是哪一层。
- `主体牵制角色原型矩阵层`：当上一步已经能稳定得到细粒度主体原型后，还应再跑 `window.__DEMO.inspectTensionRoleArchetypeCoverage()`，把现有牵制结构继续压成 `家内/家门主体 / 安身主体 / 外部门路` 三层角色矩阵，显性比较哪些角色组合已经跨路线复用、哪些仍只在单路线成立；只有把“具体主体组合比较”再推进成“角色分工比较”，后续才更容易判断某条主家庭树是在补整局模板，还是只在补局部句式。
- `终局收束原型矩阵层`：当五段闭环与七维主链矩阵都已经稳定后，不应继续只把“后半生安放”当作各路线各自成立的终局句。还应再跑 `window.__DEMO.inspectSettlementArchetypeCoverage()`，把当前真实 outcome label 继续压成整局矩阵，显性比较 `归乡收束 / 家门安身 / 自立谋生 / 外路延展 / 时局收束` 这些收束方式究竟已经跨几条路线、是否已双层锁住；若某个 outcome 目前仍只在单路线稳妥成立，就继续保留 route-local，不把它硬抬成整局模板。
- 主路径聚合层：当 mixed 裂口已经能被列出后，下一步不要再只盯单个 `id`。先跑 `window.__DEMO.inspectRouteFrontier()` 或 `window.__DEMO.inspectGlobalCompleteness()`，把剩余裂口按 `苏北留乡 / 苏北士兵 / 苏北逃亡回乡 / 沈家男女 / 工商家男女 / 女工 / 难民 / 海外多年轨` 聚合；若 mixed 裂口已基本收敛，再继续看 `window.__DEMO.inspectRouteLocalTemplateCandidates()` 给出的 route-local 候选前沿，优先回答“这一步是否让整个游戏更完整”，而不是“是否又给某一枝多写一句”。
- 主家庭/主路径 atlas 层：当主路线桶、主系统桶都已经能稳定回归后，再跑 `window.__DEMO.inspectFamilyRouteAtlasCoverage()`。这里不再只问“哪条 route bucket 缺什么”，而是把 `苏北贫农家 / 苏北留乡主分流 / 苏北士兵多年线 / 苏北逃亡回乡线 / 江南沈家 / 上海工商家 / 纱厂女工 / 难民/流亡者 / 共享海外多年轨` 这些整棵树、整条路径统一压成一张 atlas，显性比较每一行是否已经同时具备 `主路线闭环 / 七维主链 / 模板迁移`，并进一步给出 `unknownFieldGroupLabels / unknownFrontierSummary / unknownMaturitySummary / topCrossRouteUnknownRowLabel / topSingleRouteUnknownRowLabel`。这样主路径不再只显示 “unknown=几组”，而能直接区分“当前哪些边界已经成熟到跨路线模板层、哪些仍应保守留在线内”；只有把“整棵树”与“用户真正关心的主路径”一起纳入回归，后续才更容易判断这轮是在补整局，还是只在磨单一路线。
- 主家庭/主路径五段槽位层：当 atlas 已能稳定返回“这一行缺哪组 unknown / route-local”后，还应再跑 `window.__DEMO.inspectFamilyPhaseAtlasCoverage()`。这里不再只回答“这棵树/这条路径已经五段闭环了吗”，而是把同一行的 `前提/路径起手 / 主体回应 / 跨年后果 / 长期结果 / 终局事实回收` 五段，直接反查成真实 `mainline + natural` 代表节点；只有把“闭环计数成立”继续推进成“具体哪几个节点在撑这五段”也可读，后续补 `士兵多年线 / 逃亡回乡线 / 江南沈家 / 上海工商家 / 难民 / 共享海外轨` 时，才不容易又退回围着某一枝末端文案局部修边。
- frontier 处置层：当 `inspectRouteFrontier()` 已把裂口压到少数主路线后，再跑 `window.__DEMO.inspectFrontierDisposition()`。若 remaining gaps 全部落成 `keep-unknown`，表示主链矩阵与自然回放都只稳定支持“mixed 并账成立”，还不足以统一回答“谁先让位、谁先把后半生顶住”；此时应把这批裂口正式登记为 `unknown frontier`，并把下一轮重心转去**新的 profile / 外围主体**或**制度参数边界**，而不是继续在同一批 `public/teach + household` 组合上来回磨句式。
- 外围主体覆盖层：当 frontier 已明确不该继续硬升格后，再跑 `window.__DEMO.inspectPeripheralSubjectCoverage()`，按主路线桶统一汇总 `wife / mother / marriage / ledger / workers / connections` 实际进入 HUD 的覆盖情况，并继续细分 `connections.facetGroup`。目的不是再造一句新结论，而是回答“哪条主树已经具备多组外围主体、哪条路线仍缺更外层门路与长期回应”，用来决定下一轮该补哪一类**新 profile / 外围主体**。
- 多年确认状态层：当 unknown 台账已明确把 `多年确认真相边界 / 多年确认后续去向边界` 列成高优先级后，再跑 `window.__DEMO.inspectLongConfirmCoverage()`。先确认 `soldier:missing / refugee:loss / labor:injury / labor:blacklist` 这些统一规则，是否已经在**主链矩阵 + 自然回放**双层都显性露出 `ongoing / prolonged / confirmed` 状态快照，再决定是否继续往更细证据字段扩写。
- 多年确认证据层：当状态层已经双层覆盖后，再跑 `window.__DEMO.inspectLongConfirmEvidenceCoverage()`。这里要统一检查每类多年确认是否都真的带出 `confirmedFactLabel / recordedBy / evidenceLabels / missingEvidenceLabels` 四组字段；若其中任一层只在单条自然脚本里出现、或仍要靠读文案猜，就还不能算模板层真正闭环。
- 多年确认真相边界层：当证据层已经稳定后，再跑 `window.__DEMO.inspectLongConfirmTruthBoundaryCoverage()`。这里要统一检查每类多年确认是否都真的带出 `currentTruthLabel / truthUnknownLabel` 两组字段，显性回答“本次运行只确认到哪一层、仍缺哪类真相”；若仍需靠读终局句子猜，就还不能算模板层真正闭环。
- 多年确认总表成熟度层：当 `window.__DEMO.inspectLongConfirmSourceArchetypeCoverage()` 已经稳定后，再回看 `window.__DEMO.inspectUnknownStructurePortfolio()`、`window.__DEMO.inspectFamilyRouteAtlasCoverage()` 与 `window.__DEMO.inspectGlobalMainlineBoard()`。这里不应继续只按字面 `recordedBy` 计成熟度，而要把 `来源原型矩阵` 一并并回 `long-confirm-truth` 的 maturity/atlas summary；若 `活着继续把后账记下去的人` 已横跨 `苏北士兵 / 难民 / 女工`，总表就应把这组边界记成 `跨路线共享来源`，避免继续低估非苏北主路径的整局模板迁移度。
- 多年确认后续去向字段层：当 `soldier:missing / refugee:loss` 这类主线已经稳定带出“确认以后的后半生先被接住，但更细的承接人/去向仍 unknown”这一层，再跑 `window.__DEMO.inspectLongConfirmFollowupEvidenceCoverage()`。这里要统一检查 `longConfirmFollowups[]` 是否真的带出 `currentPlacementLabel / recordedBy / evidenceLabels / missingEvidenceLabels` 四层字段，而不是继续靠 `followupBoundaryUnknown` 这类附带标记让调用方自行猜；只有至少两条真实主路径都进来后，这层才算从单枝附带信息升成可复用字段层。
- `support × unknown` 联动层：当一条主路线已经既有 `support`，又有 `payload.institutionDetails[] / longConfirmations[]` 时，再跑 `window.__DEMO.inspectSupportUnknownLinkCoverage()`。这里要统一检查“谁先把人接住”与“制度/多年确认只确认到哪一层”是否真的在同一 payload 里并存，并优先锁定 `上海工商家女线·金圆券风暴`、`难民返乡重站丈量场`、`苏北逃亡返乡认地`、`归乡士兵重站丈量场` 与本轮新增的 `苏北留乡 workteam / 分田后账` 这类已被运行时证明稳定的联动样板；若某类 `support + longConfirmations` 仍无稳定代表态，就继续保留 `unknown`，不要为了对称硬补联动句。
- `support × institution` 字段层：当一条主路线已经稳定同时带出 `support` 与 `payload.institutionDetails[]` 时，再跑 `window.__DEMO.inspectSupportInstitutionPlacementCoverage()`。这里要统一检查 `supportInstitutionPlacements[]` 是否真的在 `主链矩阵 + 自然回放` 双层同时带出 `currentKnownLabel / currentPlacementLabel / recordedBy / evidenceLabels / missingEvidenceLabels`，而不是继续只记“制度边界和安身主体同场出现过”。
- `support × institution` 路线层：当上述字段层站稳后，再跑 `window.__DEMO.inspectSupportInstitutionRouteCoverage()`。这里要继续把结果压成 route 级矩阵，直接比较 `苏北留乡 / 苏北逃亡 / 苏北士兵 / 沈家/工商家相关线` 等主路线目前各自由哪层安身主体承接制度边界；只有把字段层再推进成路线矩阵，后续才更容易判断这是不是新的整局模板。
- `support + longConfirmations 路线矩阵层`：当 `payload.supportLongConfirmPlacements[]` 已经稳定返回“确认后的后账由哪层安身主体接住”后，再跑 `window.__DEMO.inspectSupportLongConfirmRouteCoverage()`。这里不再只问字段是否存在，而是继续把这层晚段承接压成主路线矩阵，显性比较哪些路线已经真实回答“谁在多年确认后把后半生继续接下去”；若某条路线还只在单层样板里成立，就优先补主链矩阵或自然回放，而不是回头细修那一句承接文案。
- `制度路线矩阵层`：当 `payload.institutionDetails[]` 本身已经具备字段层与双层覆盖后，再跑 `window.__DEMO.inspectInstitutionRouteCoverage()`。这里要统一检查**仍需父桶 `institution-detail` 承担**的主路线，是否都已经带出 route 级 `institutionDetailLabels / missingEvidenceLabels`，并确认这些路线不是只在 `unknown 台账` 里出现过，而是真的被主链矩阵与自然回放双层锁住；若某类制度边界已拆成独立 field group，则它不该继续把父桶路线统计抬高。
- `制度来源矩阵层`：当 `payload.institutionDetails[]` 仍需要作为父桶 `institution-detail` 被整局读取时，不应只停在“字段存在 + 路线存在”。还应继续跑 `window.__DEMO.inspectInstitutionDetailRecordedByCoverage()` 与 `window.__DEMO.inspectInstitutionDetailSourceArchetypeCoverage()`，把同一批制度细节再压成 `recordedBy / recordedByArchetypes` 两层，直接回答“制度细节究竟靠哪类字面来源被看见、哪些不同字面来源已经能保守压成共享来源原型”；若字面来源仍不稳定，可继续保留 `literal-source unknown`，不要为了总表好看硬并同。
- `制度缺口矩阵层`：当制度路线矩阵已经存在后，再跑 `window.__DEMO.inspectInstitutionMissingEvidenceMatrix()`。这里要把 `route -> missingEvidenceLabels` 反查成 `missingEvidenceLabel -> routes/scenarios`，优先识别哪些制度证据缺口已经横跨多条主路线；但若这些共享缺口已经被 `draft / workteam / gold` 这类独立 field group 承接，就不应继续让 `institution-detail` 父桶在 unknown 排序里重复计数，否则会误导下一轮优先级。
- `shared unknown 跨组比较层`：当 `unknown 台账` 的 top3 已经都推进到各自的 `recordedBy` 层后，不应继续只按“场景数最多的是哪组”来决定下一轮。还应再跑 `window.__DEMO.inspectSharedUnknownComparison()`，把 `地方兵役参数边界 / 金圆券风暴保全边界 / 名额转嫁承接边界` 统一比较成 `跨路线共享来源 / 跨路线可比较但来源未共享 / 单路线内共享来源` 三类成熟度；像 `地方兵役参数边界 / 金圆券风暴保全边界` 这样已经抽出来源原型矩阵的，应优先按原型层成熟度比较，同时继续保留字面 `recordedBy` 的路线差异，避免把“来源原型已共享”误写成“字面来源已完全并同”。
- `结构化 unknown 资产总表层`：当 top3 shared unknown 之外，`多年确认真相边界 / 多年确认后续去向边界 / 多年确认安身承接边界 / 名额后账回应边界` 这些字段层也已经稳定进入双层验收后，不应继续只靠几张专项表分别判断优先级。还应再跑 `window.__DEMO.inspectUnknownStructurePortfolio()`，把当前已结构化的 `unknown` 统一压成 `制度与后账边界 / 多年确认边界` 两大桶，并显性回答“哪些边界已跨路线可直接比较、哪些仍只适合 route-local 保守判断”；只有把这层总表建立起来，后续才不会继续只围着 top3 shared unknown 打转。
- `主系统闭环层`：当某组 `unknown` 已经同时具备 `字段 / 路线 / 来源` 多层结构后，还应继续回看 `window.__DEMO.inspectMajorSystemClosureCoverage()`，确认它是不是真的被整局读成 `closureReady`，而不是只在专项检查器里成立。像 `institution-detail` 这类父桶边界，只有同时进入 `字段证据层 / 主路线层 / 字面来源层 / 来源原型层` 四层闭环，才算真正能服务主家庭/主路径排序。
- `route-local 候选矩阵层`：当 `主体牵制原型矩阵 / 主体牵制回应原型矩阵 / 主体牵制角色原型矩阵 / 终局收束原型矩阵 / 结构化 unknown 资产总表` 都已经成立后，不应继续只把剩余 route-local 资产分散在几张表里分别阅读。还应再跑 `window.__DEMO.inspectRouteLocalTemplateCandidates()`，把仍停在单路线、但已双层锁住的 `主体牵制原型 / 主体牵制回应原型 / 主体牵制角色原型 / 终局收束原型 / 结构化 unknown` 统一按主路线聚合，显性比较“哪条路线已经同时挂着 2 类以上候选、哪一类证据最接近下一组整局模板”；只有把这层候选前沿做出来，后续才不容易一边说“保留 route-local”，一边又回到单枝修边。
- `route-local 类别比较层`：当 `route-local 候选矩阵` 与 `route-local 升格信号矩阵` 都已经成立后，不应继续只围着“哪条路线候选最多”打转。还应再跑 `window.__DEMO.inspectRouteLocalCategoryComparison()`，把 `主体牵制原型 / 主体牵制回应原型 / 主体牵制角色原型 / 终局收束原型 / 结构化 unknown` 这些仍在前沿的证据统一比较成 `已出现共享来源锚点 / 已出现可比较锚点 / 仍停在单路线候选` 三种成熟度；只有把“先看哪条路线”继续推进成“先看哪一类证据”，后续才更容易决定下一组整局模板究竟该从哪层冒头。
- `route-local 候选优先级层`：当 `route-local 候选矩阵 / route-local 升格信号矩阵 / route-local 类别比较层` 都已经成立后，不应继续只知道“哪条路线更前、哪一类证据更成熟”，却还要靠人工把两者拼起来决定下一步先看哪一个具体候选。还应再跑 `window.__DEMO.inspectRouteLocalCandidatePriority()`，把每个候选项保守映射到“所在路线已挂到哪类锚点、所属证据类别已成熟到哪一层”，显性区分 `路线/类别均有共享来源锚点 / 路线/类别均已有可比较锚点 / 一侧已挂锚 / 仅有原型锚点 / 仍停在单路线候选` 等优先级；只有把“先看哪条路线/哪一类证据”继续推进成“先看哪一个候选”，后续才更容易持续回答“这一步是否让整个游戏更完整”。
- `route-local 候选阻塞层`：当 `route-local 候选优先级层` 已经成立后，不应因为某个候选排得靠前，就把它直接当成下一组整局模板。还应再跑 `window.__DEMO.inspectRouteLocalCandidateBlockers()`，保守区分 `同路线已有更宽共享原型、但字面结果仍只在本路线成立`、`上位 unknown 桶已跨线、但子字段仍是单路线`、`共享类别已成立、但具体主体组合仍只在本路线成立`、`同路线已挂上共享角色原型，但这组回应姿态仍是 route-local` 等阻塞；只有把“为什么现在还不能升格”也显性结构化，后续才不会把 `苏北士兵·战后还在接日子` 这类仍缺第二条真实路线的字面收束，误写成整局共模。
- `尾部 route-local 固定层`：当 `window.__DEMO.inspectTopRouteLocalCandidateBundle()` 已经明确 `matchedCandidateMissingCount > 0`、`familyComboPeerRouteCount = 0`，且现有对照位只到共享主家庭/锚点骨架时，不应继续把这格当成“下一轮默认还要再比一次”的开放候选。此时应直接读取 `tailStabilityLabel / tailStabilitySummary / tailFreezeSuggested`，把它登记为“当前无第二路线证据”的**保守尾项**；只有未来出现新的真实 peer route，再重开比较器与升格判断。
- `主家庭/主路径 frontier 层`：当 `主家庭/主路径 atlas` 已经稳定覆盖 `苏北贫农家 / 苏北留乡主分流 / 苏北士兵多年线 / 苏北逃亡回乡线 / 江南沈家 / 上海工商家 / 纱厂女工 / 难民流亡者 / 共享海外轨` 这些行后，不应继续只返回“每棵树挂了几项 route-local / unknown”这种计数；还应让 `window.__DEMO.inspectFamilyRouteAtlasCoverage()` 直接按树/路径聚合 `topRouteLocalCandidates / frontierSummary / unknownFieldGroupLabels / unknownFrontierSummary / topRouteLocalRowLabel / topUnknownRowLabel / topUnknownFieldGroupSummary / topPathRouteLocalRowLabel / topPathUnknownRowLabel / topPathUnknownFieldGroupSummary`，显性回答“当前哪棵树 route-local 最重、哪棵树 unknown 最密、哪条主路径现在具体卡在哪几组边界”。只有把 frontier 也压回 atlas，总表层才真正能先判断“下一步该补哪整棵树，还是该补哪条主路径”，而不是再把 route-local 与 structured unknown 分散到多张表里人工拼读。
- `主家庭/主路径决策层`：当 atlas 已能同时区分 `自有主路线 unknown / 共享主路线借入 unknown / 跨路线模板型 unknown / 单路线保守 unknown / route-local 候选` 后，不应再靠人工把这些计数重新拼成“下一步先干什么”。还应继续让 `window.__DEMO.inspectFamilyRouteAtlasCoverage()` 为每一行返回 `completionPriorityLabel / completionPriorityScore / completionPrioritySummary`，并在总表层显性给出 `topCompletionPriorityRowLabel / topFamilyCompletionPriorityRowLabel / topPathCompletionPriorityRowLabel`，保守区分 `优先补整局系统 / 先收自有线内 unknown / 先去共享主路径收回借入 unknown / 暂保留 route-local，先比较证据` 四种推进方式；只有把“这一步是否让整个游戏更完整”的判断标准也做成运行时资产，后续才不会再次退回凭印象挑单枝。
- `返乡认地层级矩阵层`：当 `payload.workteamRecognitionBoundaries[]` 已经能稳定返回 `routeKind / currentRecognitionLabel / currentReckoningLabel` 后，不应只停在 route 级比较。还应继续返回 `reckoningTier / reckoningTierLabel`，并跑 `window.__DEMO.inspectWorkteamRecognitionTierCoverage()`，把返乡/归乡主路线反查成“账目讲明 / 量力认领 / 留余退身 / 两头并账”这组层级矩阵。只有把 route 差异再压成共享层级，后续才更容易判断哪一层已经跨路线复用、哪一层仍只是单线路径姿态。
- `名额后账回应矩阵层`：当 `payload.quotaRippleResponses[]` 已经能稳定返回 `rippleKind / rippleTier / currentRippleLabel / recordedBy` 后，不应只停在“字段存在 + 双层覆盖”。还应继续跑 `window.__DEMO.inspectQuotaRippleResponseKindCoverage()`、`window.__DEMO.inspectQuotaRippleResponseTierCoverage()`、`window.__DEMO.inspectQuotaRippleResponseRecordedByCoverage()` 与 `window.__DEMO.inspectQuotaRippleResponseSourceArchetypeCoverage()`，把同一组边界反查成 `闭门不问 / 递小礼 / 跑差补账` 三类回应矩阵、`首轮后账 / 二次后账` 两层共享模板、`家门里人的猜测 / 村里口风 / 对方神色 / 保长差事` 这组字面来源矩阵，以及 `门里门外的人情口风 / 差事补账后的后账口风` 两类来源原型。只有把回应差异与来源差异都压成可比较矩阵，后续才不容易把“已经字段化”误读成“已经系统比较完”。
- `苏北留乡主分流总览层`：当 `payload.quotaTransfers[]` 与 `payload.quotaRippleResponses[]` 都已经稳定结构化后，不应继续把 `名额转嫁承接边界` 与 `名额后账回应边界` 分散在两张专项表里读。还应继续跑 `window.__DEMO.inspectQuotaRippleResponseBranchCoverage()` 与 `window.__DEMO.inspectSubeiStayUnknownAtlas()`，把 `买丁 / 托情 / 病身免役 / 躲丁` 四条留乡主分流统一压成一张总表，显性比较每条分流当前是“只稳定命中名额转嫁承接边界”，还是已经同时命中 `名额转嫁承接 + 名额后账回应` 两组边界；只有把两组 structured unknown 真正接回同一张分流总览，后续才更容易判断下一步是在补整棵 `苏北贫农家`，还是只在磨某一枝留乡节点。
- `support + longConfirmations` 字段层：当 `support + longConfirmations` 已经稳定共现后，再跑 `window.__DEMO.inspectSupportLongConfirmPlacementCoverage()`。这里要统一检查 `payload.supportLongConfirmPlacements[]` 是否真的把“多年确认的后账”与“安身主体的承接”收进同一条字段记录，并在 `主链矩阵 + 自然回放` 双层同时带出 `currentPlacementLabel / confirmedFactLabel / currentTruthLabel / recordedBy / evidenceLabels / missingEvidenceLabels`；若仍需回头读 HUD 或终局文案猜“是谁接住后半生”，就继续保留 `unknown`，不要硬说已经闭环。
- `support + longConfirmations` 路线层：当上述字段层已经站稳后，再跑 `window.__DEMO.inspectSupportLongConfirmRouteCoverage()`。这里要继续把这组结果压成 route 级摘要，直接比较 `苏北士兵 / 纱厂女工 / 难民流亡者` 等主路线目前各自由哪层安身主体承接多年确认后账；只有把字段层再推进成路线矩阵，后续才更容易判断这是新的整局模板，还是仍停在单枝上的晚段句式。
- `旧路回接落点层`：当 `海外试探回身旧路 / 逃亡回乡旧屋旧路重接 / 归乡士兵旧屋家口旧地回接` 已经不止是一条路线里的晚段文案时，再跑 `window.__DEMO.inspectReturnRouteLandingEvidenceCoverage()`。这里要统一检查 `payload.returnRouteLandings[]` 是否真的在 `主链矩阵 + 自然回放` 双层同时带出 `currentLandingLabel / recordedBy / evidenceLabels / missingEvidenceLabels`，并优先回答“哪一条返身路径现在已经有第二条真实落点来源、可以拿来做整局比较”；若还没有第二条真实路线或第二组来源，就继续保留 `route-local / unknown`。
- 确认层：对 `missing / loss` 这类“并非当年立刻确认”的事实，至少做一次跨年验证，确认会从“未明”推进到“多年无准信 / 已确认”，且保留 `unknown` 边界
- 串线层：重点检查 `noneEcho / yearTo / override` 是否让后段节点正确让位，避免不同家庭/路径误并回通用主干
- 自然流程层：当主链代表快照已基本闭环后，要补一层真实 `startGame()/startFamily() + advanceToYear() + chooseSituation()` 的自然推进验证；当前运行时已新增 `window.__DEMO.inspectNaturalPlaythroughs()`，已覆盖 **83** 条代表性自然闭环脚本：原有苏北、沈家、工商家、女工、难民与共享海外轨主代表线都继续保留，并已把 `shared overseas study / kin / trade` 推成 `family / origin × mainland / hktw / overseas` 更对称的自然验收；本轮又把 `natural-shen-books-records-mixed / natural-shdaughter-shelter-workroom-return-mixed` 加进真实年份回放，并保留 `natural-shen-woman-teach-letters-mixed / natural-shdaughter-shelter-workroom-mixed`，专门验证“同一家族内的外部门路与家门回应能否在跨阶段混合态里一起站住”，而不只是在静态快照里成立。并要求脚本结束时不遗留未处理情境；为避免批量验收受随机波动干扰，这层回放现按固定随机值执行，同时允许 `advanceToYear()` 测试 helper 用 `autoChoices` 自动吃掉已知通用阻塞情境（如 `sell-for-cure / mother-death-confirm / rent-negotiate` 与共享逃亡层节点），避免“主链其实能走、却总被前置重复节点卡住”的假阴性。继续保留两条运行时约束：凡 `refugee` 直玩入口专属的高耗损流亡动作，都必须挂回真实时代门槛，至少保证 `赶路南逃 / 沿途做工 / 求助赈济 / 结伴同行 / 护送家人` 只在 `1938+`、`寻地落脚` 只在 `1940+` 之后开放；而 `江南沈家` family 模式女线若已经走到 `rebel:marriage`，也不应只因家庭开局与 `gentrywoman` 直玩入口的初始学识不同，就永远命不中 `nora-leave`，否则会形成“矩阵闭环、自然断链”的假完整。对 `苏北病身` 这条制度线，当前采用的是**脚本活法收束**而不是再放宽运行时门槛：当 `1943` 的真实自然底子仍达不到 `fakesick-longtail` 的轻手艺门槛时，自然专项改走 `injury:withdrawn -> injury:guard-body -> injury:team-guard -> injury:settle-handicraft` 这条同样闭环、且更贴近当前苏北底子的病身后路，用来回答“整局是否完整”，而不是用一条额外放水的 gate 去掩盖真实底色；而前几轮新增的 `household` 自然线也再次确认：若真实 HUD 仍停在 `归城旧账重开 / 战后残账与周转紧` 这一层，就应先收回自然断言去服从运行时输出，而不是反向硬改 `跨年余账` 的世界规则。
- 路径层：凡家庭模式会把 `origin` 从 `tenant` 切到 `soldier / refugee` 的节点，都要验证 `origins` 守门已同步放开；否则主树只在“旧直玩入口”可达，家庭模式会在中途断链
- 队列层：若同一年既有家庭/路径专属晚段节点，又有 `stay-or-go-1949`，则**必须先跑专属晚段节点，再弹最终去留**；否则 `end:*` 会直接触发 `doEnding('era')`，把同年仍可达的专属闭环截断
- 队列层补充：若一条线在 `1949` 之前已经进入“多年确认后的家庭侧收束”，也仍要显式补一个 `1949` 晚段节点，把“活着的人后来把日子安在哪一层”先写实，再进入 `stay-or-go-1949`。当前 `soldier:missing-home/natal -> soldier-missing-*-1949-settle -> stay-or-go-1949` 就是这一模板。
- 年度情境层：凡 `trigger.everyYear` 的情境，都要验证“同一年只出现一次、跨到下一年才允许再出现”；否则自然试玩会被同年重复弹出的年度情境卡住，无法判断整棵树是否真正可推进
- 主体层：凡 `subjects.father / subjects.household` 接入的家庭线，都要至少验证一次 `setState({ family, echoes }) -> buildHudFacts() -> renderEnding()`，确认“家内回应”不是只停在日志里，而是进入 HUD 与终局事实回收。
- 入口一致性层：凡某条主线同时允许 `family` 开局与 `origin` 直玩，都要至少验证一组 `startGame(origin) + setState(...)`，确认 `父亲 / 家门 / 婚家 / 账房 / 工友家口` 不会只在家庭模式下存在。
- 外部门路层：凡 `subjects.connections` 被用于非海外主轨，都要至少验证一组 `previewEnding({ echoes, firedEvents })`，确认 `门生 / 学会名帖 / 同业会馆 / 熟客旧交` 不是只写进日志，而是真的进入 HUD 与终局总账。
- 苏北从军主体层：凡 `soldier` 直玩或 `subeipoor -> soldier-path` 的代表性快照，都要至少验证一组 `previewEnding({ origin:'soldier' ... })` 与一组 `previewEnding({ family:'subeipoor', track:'soldier-path' ... })`，确认 `旧邻/搭伙` 这一层会真实出现，而不是只在家庭并账或日志里成立。
- 多主体层：凡一条主线理论上会同时存在两层以上辅助主体，都要至少验证一组 `previewEnding({ echoes, firedEvents })`，确认 HUD 能同时出现多条主体事实，而不是仍被旧的单槽位口径压成一条。
- **family 守门层**：凡情境、动作或年度事件带有 `family / families` 触发，都要至少验证一组 `startGame(origin) + setState(...)`，确认直玩入口也能命中对应 family profile 下的家族专属主干，而不是只有 HUD 在显示主家庭过程态。
- **主链矩阵层**：当主家庭/主路径已经基本闭环后，不应再只靠零散快照逐条人工记忆验证。当前运行时已新增 `window.__DEMO.inspectMainlineCoverage()`，把苏北买丁/从军/逃亡、沈家男女线、工商家男女线、纱厂女工、难民返乡与共享海外轨收成统一矩阵；此前已补进 `江南沈家女线·抗婚叠加海外安身后路`，用来检验 `婚约裂口 / 家门回应` 与 `亲眷 / 住处 / 海外后路` 是否能在同一复合主链里并账出现；本轮继续保留 `shen-male-books-records-mixed`，但把它升级成必须显性断言 `主体牵制 = 家门未先稳住，门生课脉与文书细路并着顶上来`，并新增 `biz-woman-shelter-workroom-return-mixed`，专门检验“同一家族内的外部门路 + 家门回应”是否能在 `战后几年 / 金圆券前夜 / 1949` 之前就一起进入 HUD 与终局总账。后续补新主链时，优先把代表状态追加进这个矩阵，再补文档与叙事说明；当矩阵代表面基本够用后，再优先把代表线补进 `inspectNaturalPlaythroughs()`，回答“这些事实是不是自然推着也站得住”。
- **确认后事验证层**：`病母亡故 -> 后事确认` 不应只在 `choices-data.js` 的单个情境里存在。本轮已把它继续压进统一验收：`subei-soldier-home` 与 `subei-soldier-missing-natal` 这两组矩阵状态、以及 `natural-soldier-home / natural-subei-soldier-missing-natal` 这两条真实年份脚本，都会显性断言 `病母回应 = 母亲后事已办` 与对应总账句，避免“后事确认已经发生，但辅助主体快照因 `present:false` 把它吞掉”的回归。
- 行动层：凡新增 `trigger` 型分阶段动作，都要至少验证一次 `setState(...) -> __DEMO.plan(actionId)`，确认动作只在对应年份/回响下可计划，不会提前露出或在后段继续滞留。

## E. 已接入运行时的 HUD 事实层
> 目标不是加 UI 装饰，而是把“主链已经走到哪一层闭环”显性化，方便验证主要人生线是否真的形成 `前提 → 选择 → 跨年后果 → 长期结果 → 终局回收`。

| 家庭/路径 | HUD 键 | 当前口径 |
|---|---|---|
| `苏北贫农家` | `抓丁过程态 / 佃权-地缘 / 名额压力 / 觉醒-土改路径` | 继续作为样板树，承载抓丁、逃亡、从军、土改四层状态收束 |
| `江南沈家·女线` | `婚约-出走 / 制度过程态 / 跨年活法 / 战时回应 / 跨年余账 / 战后几年 / 家门回应 / 门生-旧交 / 后半生安放` | 不只写主角姿态，也把“婚约与门内张力是否已起、课堂赈济怎样拖成年账、战后怎样重接女学与门内旧账、外头旧交与女学门路有没有继续接住、风声再紧后的收束是否已启动”显性化 |
| `江南沈家·男线` | `前战家门 / 制度过程态 / 战时先护 / 跨年余账 / 战后归城 / 风声收束 / 家门回应 / 门生-旧交-旧同事 / 1949去留` | 把课堂、家门、报馆与后半生收束写成连续阶段，同时让“前战家门责任 / 战时南迁或留城或先护契书谱册 / 归城旧账 / 风声收束 / 门生名帖与旧同事细路有没有继续牵住”这组阶段态在 HUD 里单独可见 |
| `上海工商家·男线` | `接班策略 / 接班前账 / 制度过程态 / 战时处置 / 跨年余账 / 战后残局 / 金圆券回应 / 风暴后安放 / 家内回应` | 不只写主角怎么选，也把父亲开口、顾家怎样拆家内迁，以及“接班试守 / 三类接班前账 / 战后周转 / 会馆事务线 / 合账小网 / 金圆券风暴 / 风暴后收束”这条制度阶段链显性化 |
| `上海工商家·女线` | `婚约-出走 / 前战门路 / 制度过程态 / 战时撑住 / 跨年余账 / 战后几年 / 金圆券前夜 / 家内回应 / 后半生安放` | 把租界教育、新女性路径与家业震荡后的长期结果拆成独立阶段，并让“前战门路训练 / 战时租界风雨 / 战后门路旧账 / 金圆券风暴 / 风暴后收束”进入 HUD；顾家对“先藏退路 / 先接妇孺口粮 / 先守公共门面”之外，本轮又把“先拢女工小作 / 战后重接手账与熟手零单 / 风暴后收成互帮小作”压成第四条独立后半生主轨，相关家内回应也进入可见层 |
| `共享海外轨` | `隔海回应 / 两头认账 / 后路收拢 / 非家庭主体回应 / 1949去留` | 不只把 `abroad:*` 写成后路或门票，还要把 `旧同学 / 旧门路 / 亲眷` 这层非家庭主体如何继续牵住人物显性写进 HUD 与终局 |
| `士兵多年线（直玩）` | `入伍前提 / 入伍起点 / 军中分化 / 多年阶段 / 复员-去路 / 家中回应 / 跨年余账 / 后半生安放` | 用 `soldier:entry-*` 补齐“前提→选择”层：即便直玩入口默认快进到 `1937 入伍待编`，也仍能显性写出“这一回怎么进军”的可观察事实；后续继续沿 `军中日常 → 分化 → 复员去路 → 1949` 的多年闭环推进，不写价值排名与具体制度细数 |
| `难民/流亡者（出身线）` | `家园与离散 / 路上处境 / 后方落脚 / 跨年余账 / 亲族-旧路 / 后半生安放` | 不复用苏北抓丁/土改派生状态；同时用 `refugee:*` 回响、`firedEvents` 与 `subjects.connections` 把“流亡主线 + 亲族屋檐 / 捎话旧路 / 旧街坊”补成可观察的闭环事实；此前已把 `返乡仍两头并行` 这一条 `home-twoends -> late-home-twoends` 收口正式拉进验收面，本轮再给直玩返乡线补出 `refugee-return-at-workteam -> refugee-return-after-allocation` 这层返乡后的制度再压 |
| `纱厂女工（出身线）` | `包身/自由身 / 工潮后账 / 战时活法 / 战后几年 / 跨年余账 / 熟工-字路 / 后半生安放` | 以 `labor:* / free:* / scab` 等回响为主，把 `包身契 / 罢工 / 赎身` 继续推进到战时回应、战后几年中继、跨年后账与长期结果层；并把 `熟工 / 夜校 / 里弄旧识 / 口粮票 / 递信字路 / 代写家信 / 记工账` 收进统一 `subjects.connections`，不伪造细节。本轮再把 `互帮饭碗 / 字路细活` 两组稳定收口推进成显性 `主体牵制`，使女工线也能回答“是谁把战后几年与后半生顶住”。 |

## F. 本轮验证补充
- `demo.html` 现已通过 `jsdom + window.__DEMO.inspectMainlineCoverage()` 与 `window.__DEMO.inspectNaturalPlaythroughs()` 全量回归：主链矩阵现为 **62 / 62** 通过，自然回放现为 **80 / 80** 通过。
- 这轮没有回头细修任何已闭合单枝，而是继续补 `上海工商家` 女线里此前还停在规则表、却没进入统一验收的战时 mixed 裂口：把 `屋檐 + 小作` 的**战时撑住态**正式锁进主链矩阵与自然回放。
- 运行时现在新增两组统一验收对象：  
  `1.` `biz-woman-shelter-workroom-war-mixed`：在 `1937` 战时节点里，除 `前战门路 = 先认屋檐与借住名册`、`战时撑住 = 拢住女工小作`、`家内回应 = 先拢女工小作` 外，还必须显性验收 `借住房主 / 旧校舍空屋 / 熟手旧路 / 女工小账 = 先起`，以及 `主体牵制 = 家门先让位，屋檐与小作并着撑住战时日子`；  
  `2.` `natural-shdaughter-shelter-workroom-war-mixed`：把同一条顾家女线真实年份推进脚本也锁成同口径，避免“规则表与静态快照都说成立，真实战时推进却无人验”。
- 这一步补的是“顾家女线五种主收口在早阶段也要进入统一验收”的模板面，不是给 `shelter / workroom` 单枝多加一句结论。此前 `屋檐 + 小作` 只在 `1948 重接` 与 `1949 收束` 被正式抽检，现在战时撑住态也终于被系统看见。
- 这轮真正补上的模板裂口，是共享 `主体牵制` 不应只在晚段回收才被验收；只要某条主家庭线在真实运行里已经能稳定回答“谁先让位、谁先把日子撑住”，就应尽快进入 `主链矩阵 + 自然回放` 双层显式断言。
- 既有 `苏北逃亡线 / 苏北士兵多年线 / 纱厂女工 / 江南沈家 / 上海工商家 / 难民直玩入口 / 共享海外轨` 的矩阵与自然回放保持通过，说明“先补系统代表态，再审慎决定是否升格规则层”的口径仍然成立，没有打断旧树闭环。
- 阶段守门层继续沿用上一轮结论：晚段代表快照只能留下“当前该跑的晚段节点 + \`stay-or-go-1949\`/同年通用节点”，不能把更早的中段节点重新弹出来。
- `jsdom` 下仍需在测试环境里 stub `Window.scrollTo()`，但不影响 `__DEMO` 的状态推进、HUD 断言与矩阵/自然回放结果判断。

---

# 节点 N-03：江南沈家 · 抗婚之后到后半生安放
> 目标不是再讲一次“娜拉出走”的口号，而是把 `江南沈家` 女线做成真正闭环：**婚约前提 → 抗婚/出走选择 → 几年后的活法 → 抗战时主体回应 → 风声再紧时的后半生安放 → 1949 事实回收**。

## ① 父快照 P₀
| 字段 | 值 | 说明 |
|---|---|---|
| 家庭 | `jiangnanshen` | `actions-data.js` 已定义 |
| 主路径 | `newwoman-path` | 女性默认主干 |
| 起点资源 | 学识/心智/关系账偏高，钱与处境尚可 | 已实现 |
| 先决节点 | `父母之命·婚约`、`娜拉时刻·是否出走` | 已实现 |
| 未知项 | 家门会不会重新接纳、外部饭碗能否长期撑住、战时所在城市的具体处境 | 保留 `unknown` |

## ② 已成型运行时节点
- `抗婚以后·先把日子接住`
  - 承接：`nora:out / reform:inside / nora:back`
  - 分化：`woman:self-teach / woman:self-press / woman:inside-school / woman:inside-family / shenwoman:prewar-letters`
  - 作用：把“姿态选择”沉淀成几年后的实际生计与社会位置；其中 `shenwoman:prewar-letters` 把女学名册、婚家礼帖与亲眷信札抽成沈家女线独立起手
- `抗战起来·把学识与名声用到哪`
  - 承接：上述五条活法与 `nora:*`
  - 分化：`woman:war-teach / woman:war-relief / woman:war-household / shenwoman:war-letters`
  - 作用：把 `1937` 之后的时代冲击写成主体现实回应，而不是一句“参加动员”；新增 `shenwoman:war-letters` 用来承接“续名册、递口信、保礼帖”这条沈家女线制度细路
- `战后几年·先把哪层日子重新接回来`
  - 承接：`woman:war-* / shenwoman:war-letters / woman:self-* / woman:inside-*`
  - 分化：`woman:postwar-teach / woman:postwar-public / woman:postwar-household / shenwoman:postwar-letters`
  - 作用：让沈家女线不再从战时直接跳到晚段，而是在 `1946-1948` 先分出“重接女学 / 稿费门路 / 门内旧账 / 名册礼帖细路”四种战后主收口
- `风声再紧·把后半生收在哪一层`
  - 承接：`woman:war-teach / woman:war-relief / woman:war-household / shenwoman:war-letters / woman:postwar-* / shenwoman:postwar-letters`
  - 分化：`woman:settle-teach / woman:settle-public / woman:settle-household / shenwoman:settle-letters`
  - 作用：把女线从“战时做过什么”继续推进到“后半辈子靠什么活、把自己安在哪一层”；新增 `shenwoman:settle-letters` 用来把代笔信札、礼帖与续名册收成一条真正的后半生细活

## ③ 1949 回收口径
- `demo.html` 终局现已回收：`rebel:marriage / nora:* / woman:* / woman:settle-* / stay:teach / sold:legacy / debt:inflation`
- 只写事实：
  - 你是否真的把门推开过
  - 你靠什么把日子接住
  - 你在战时把学识/名声用到哪里
  - 你在风声再紧时把后半生安到哪里
  - 你在战后通胀里卖过什么、欠过什么
- 不写价值排名，不把“留家/留守”自动贬为失败

## ④ 独立轨补口（最新）
- 为避免 `江南沈家` 女线长期停在“关键节点闭环”，运行时现已把“节点之间如何过日子”继续下沉到 `action` 层：在 `actions-data.js` 为 `gentrywoman` 保留一组 `trigger` 型行动，并在本轮把沈家女线独立主轨 `shenwoman:*` 也接入其中。
- 新增动作（均可反复计划，带边际递减）：
  - `women-write-sustain`：承接 `woman:self-press / woman:settle-public / shdaughter:gold-public / shdaughter:settle-public`，把“写稿/译稿/结社门路”做成长期饭碗。
  - `women-war-teach`：承接 `woman:war-teach / woman:settle-teach / shdaughter:war-school / shdaughter:settle-teach`，把“战时教书/识字班/女学饭碗”做成年度日常，而不是只停在情境节点。
  - `women-war-relief`：承接 `woman:war-relief / shdaughter:war-relief`，把“赈济救护/安置跑腿”做成持续年头，体现体力与心力消耗。
  - `women-war-household`：承接 `woman:war-household / woman:settle-household / shdaughter:war-household / shdaughter:settle-household`，把“守家门与家口账”做成可反复占用年头的日常。
  - `shen-public-notes`：承接 `woman:self-press / woman:war-relief / woman:postwar-public / woman:settle-public`，把沈家女线 `public` 分流进一步压成 `递稿 / 学会名帖 / 稿路旧账` 这一层更家族化的慢动作，而不再只靠通用写稿/赈济动作撑着。
  - `shen-copy-letters`：承接 `shenwoman:prewar-letters / war-letters / postwar-letters / settle-letters`，把“替婚家抄信、续名册、认旧称呼”落成可反复计划的家族动作，避免新主轨只存在于节点文案。
- `demo.html` 现已支持**带 trigger 的年度事件守门**，因此 `gentrywoman` 这轮也开始补女线专属年度事件骨架：
  - `战时课堂与门内支撑`（`1938`）：仅在 `family=jiangnanshen` 且已命中 `woman:war-teach / woman:war-relief / woman:war-household` 时触发，把课堂、赈济与家门支撑写成要按年再认的消耗账。
  - `战后女学与门内旧账`（`1947`）：承接 `woman:war-* / shenwoman:war-letters` 且未进入 `woman:settle-* / shenwoman:settle-letters`，把女学饭碗、稿费门路、门内旧账与名册礼帖细路一起写回年度层。
  - `战后名册与礼帖旧账`（`1947`）：仅在 `shenwoman:postwar-letters` 时触发，把“哪封信还能递到、哪层旧称呼还作数”压回年度层。
  - `风声再紧后的信札礼帖余账`（`1949`）：仅在 `shenwoman:settle-letters` 时触发，把代笔信札、礼帖与续名册这条后半生细活写回 HUD 与终局。
  - `战后稿路与名帖旧账`（`1947`）：仅在 `family=jiangnanshen` 且已命中 `woman:postwar-public`、尚未进入 `woman:settle-public` 时触发，把稿路、名帖、口风与婚家牵扯拆成 `public` 分流自己的中继余账。
- 这一步的目标不是再加一堆细枝文案，而是让女线也开始具备与男线更对称的 `origin/action/event` 结构厚度：不仅有关键节点与可计划日常，也开始有会被 `firedEvents` 与 `1949总账` 回收的年份骨架。

---

# 节点 N-04：上海工商家 · 接班、战时搬厂到风暴后安放
> 目标是把 `上海工商家` 男线从“背景很厚、运行时很薄”推进成闭环：**接班前提 → 接班策略 → 战时处置家业 → 金圆券前夜先保哪样 → 风暴过去后的长期安放 → 1949 去留事实回收**。

## ① 父快照 P₀
| 字段 | 值 | 说明 |
|---|---|---|
| 家庭 | `shanghaigongshang` | `actions-data.js` 已定义 |
| 主路径 | `biz-heir` | 男性默认主干 |
| 起点资源 | `money/network/fame/position` 高，`knowledge/mind` 中等 | 已实现 |
| 上游节点 | `备考庚款留学`、`是否依附洋行` | 已有运行时节点 |
| 未知项 | 哪笔资产真能保住、战时留下是否会背更重名声后账、金圆券前夜保全能否成功 | 保留 `unknown` |

## ② 本轮新增运行时节点
- `父病接班·先稳哪一笔`
  - 分化：`biz:steady-ledger / biz:expand-risk / biz:foreign-shield`
  - 作用：把“资本家之子接班”拆成账房、扩张、外部依赖三种不同路径
- `战火压厂·先把家业往哪边挪`
  - 承接：`biz:* / comprador:yes / national:industry / give-up:study`
  - 分化：`biz:move-inland / biz:stay-shanghai / biz:split-house`
  - 作用：把抗战写成“厂、人、账、家口如何拆开保”的现实层，而不是一句“厂毁”
- `金圆券风暴·先保哪一样`
  - 承接：上述战时与接班后账
  - 分化：`biz:gold-hide / biz:gold-freeze / biz:gold-burn`
  - 作用：把 `1948` 从被动清零改成“先保退路 / 先保厂壳 / 先保人和账”的三种主体回应
- `风暴过去以后·把厂人与家门收在哪一层`
  - 承接：`biz:gold-hide / biz:gold-freeze / biz:gold-burn`
  - 分化：`biz:settle-ledger / biz:settle-public / biz:settle-household`
  - 作用：把男线从“先保什么”继续推进到“最后把厂、人、账和家门安成哪一种后半生”

## ③ 1949 回收口径
- `demo.html` 终局现已回收：`give-up:study / comprador:yes / national:industry / biz:* / biz:settle-* / end:*`
- 只回收以下事实：
  - 你接班后先守什么
  - 战火来了以后你怎样拆分家业
  - 金圆券前夜你到底先保退路、保厂、还是保工人与清账
  - 风暴过去以后你把残余账房、工人与家门收成了哪一种后半生
  - 留大陆以后是“怎样把厂、人、账、体面重新拆开再接”，而不是简单一句“公私合营”

## ④ 独立轨补口（本轮）
- `actions-data.js` 已为 `capitalist` 补入 5 条更可验证的 `trigger` 型行动（重点是把“接班策略”也压回行动层，而不是只停在一句 `echo` 上）：
  - `拆机护骨架`：承接 `biz:move-inland`，把“内迁保机器”落成战时多年日常，而不是一次性姿态。
  - `盘残账找熟客`：承接 `biz:stay-shanghai / biz:split-house / biz:gold-hide / biz:gold-freeze / biz:settle-ledger`，把守残账、追旧单、找熟客补成风暴中后期的可计划活法。
  - `清账发薪安工友`：承接 `biz:gold-burn / biz:settle-public / biz:settle-household`，让“先保人工账/家口账”变成持续投入，而不只停在 `1948` 一次抉择。
  - `押贷扩机跑行情`：承接 `biz:expand-risk`，把“押注扩张冒险”落成可反复占用年头的抵押/借贷/加机台事务，让扩张分流也有自己的可玩日常与代价。
  - `跑洋行合同借外账护壳`：承接 `biz:foreign-shield / comprador:yes`，把“外部依赖与靠山”落成可计划的合同/手续/名声后账事务，让外援分流也具备可验证的年头层。
- `demo.html` 现已支持**带 trigger 的年度事件守门**，因此 `capitalist` 的年度骨架也继续补齐到能区分三条接班策略：
  - `账房现金与接班前账`（`1936`）：只在 `biz:steady-ledger` 时触发，把“先稳现金流”压成一层可跨年回收的前账。
  - `扩张押贷与机台前账`（`1936`）：只在 `biz:expand-risk` 时触发，把“抵押扩张”压成一层可跨年回收的前账。
  - `洋行合同与外账前账`（`1936`）：只在 `biz:foreign-shield / comprador:yes` 时触发，把“外部依赖”压成一层可跨年回收的前账。
  - `战时拆厂后的残账`（`1938`）：承接 `biz:move-inland / biz:stay-shanghai / biz:split-house`。
  - `战后残账与周转`（`1947`）：承接接班与战时后账，写“旧厂骨架与熟门路都要重新认账”。
  - `战后账房与熟客旧账`（`1947`）：只在 `biz:postwar-ledger` 时触发，把账房、熟客、现款与熟账拆成 `ledger` 分流自己的中继余账。
  - `战后同业与门面旧账`（`1947`）：只在 `biz:postwar-public` 时触发，把同业会馆、熟客口风与公共门面拆成 `public` 分流自己的中继余账。
  - `战后会馆差事与旧工友路数`（`1947`）：只在 `biz:postwar-route` 时触发，把会馆差事、代办跑单与旧工友介绍拆成 `route` 分流自己的中继余账。
  - `战后合账小网旧账`（`1947`）：只在 `biz:postwar-coop` 时触发，把熟客小单、账房余单与旧工友代工拆成 `coop` 分流自己的中继余账。
  - `风暴后残账`（`1949`）：承接 `biz:gold-*`，把“金圆券之后还要再摊一遍残账”接到后半生之前。
  - `风暴后会馆差事余账`（`1949`）：只在 `biz:settle-route` 时触发，把 `route` 分流正式写回后半生。
  - `风暴后合账小网余账`（`1949`）：只在 `biz:settle-coop` 时触发，把 `coop` 分流正式写回后半生。
- 这一轮继续把“顾家男线是不是仍只是节点闭环”改成可直接回归的问题：`inspectFamilyIndependentTrackCoverage()` 已把 `route / coop` 两条顾家男线独立轨接进 `主链验证 + 动作层 + 年度事件层` 同一套检查，不再只靠 README 手工判断。
- 验证口径：不仅要看 `biz-*` 情境能否出现，还要直接验证 `__DEMO.plan()` 能在对应状态下计划这些行动，且年度推进后 `firedEvents` 会真实写入上述事件，不会误在未承接前提前触发。

---

# 节点 N-05：江南沈家 · 男线从家门南迁到战后归城
> 目标是把当前仍主要复用 `scholar-path` 的 `江南沈家` 男线补出家族专属中继：不是再讲一次“读书人遇上通胀”，而是把**书香门第的家门、课堂与南迁压力**写成连续人生。

## ① 父快照 P₀
| 字段 | 值 | 说明 |
|---|---|---|
| 家庭 | `jiangnanshen` | `actions-data.js` 已定义 |
| 主路径 | `scholar-path` | 男性默认主干 |
| 起点资源 | 学识/心智/声望偏高，家门有薄产 | 已实现 |
| 通用上游 | `备考庚款留学`、`通胀下的抉择` | 仍保留为读书人通用骨架 |
| 未知项 | 是否出洋、家门能否完整南迁、战后归城时哪层关系还能接回 | 保留 `unknown` |

## ② 本轮新增运行时节点
- `家门南迁·先护哪一头`
  - 触发：`family=jiangnanshen + origin=scholar + yearFrom:1937`，且未走 `abroad:gone`
  - 分化：`shen:school-south / shen:press-stay / shen:family-escort`
  - 作用：把“读书人遇战火”拆成课堂南迁、留城守文字、先护家门三种家族化回应
- `战后归城·先把哪层日子接回来`
  - 触发：`yearFrom:1946`，承接 `shen:* / stay:teach / sold:legacy / debt:inflation`
  - 分化：`shen:postwar-lecture / shen:postwar-public / shen:postwar-household`
  - 作用：把战后收束继续拆成“先接讲席 / 先接公共说话位置 / 先修补家门旧账”三条长期活法
- `风声再紧·书箱与家门先收哪一头`
  - 触发：`yearFrom:1948`，承接 `shen:postwar-*`
  - 分化：`shen:settle-books / shen:settle-public / shen:settle-household`
  - 作用：把 `1948-1949` 再压来的一轮风声继续拆成“守书脉 / 收窄公开位置 / 收回家门日常”三种长期结果层，而不是从 `1946` 直接跳到终局

## ③ 1949 回收口径
- `demo.html` 终局现已回收：`stay:teach / sold:legacy / debt:inflation / shen:* / end:*`
- 只回收以下事实：
  - 你在 1937 后先护的是课堂、文字，还是家门老小
  - 战后先接回来的是讲席、公共位置，还是一家人的旧账
  - 风声再紧以后，你最后把后半辈子收成了守书脉、收窄公开位置，还是门内修补者
- 不把 `江南沈家` 男线再压回“通用读书人遭逢通胀”的一句话里

## ④ 独立轨补口（本轮）
- `actions-data.js` 已为 `scholar` 补入 8 条 `trigger` 型行动：
  - `带着讲义续课堂`：承接 `shen:school-south / shen:postwar-lecture / shen:settle-books / stay:teach`
  - `守报馆抄旧稿`：承接 `shen:press-stay / shen:postwar-public / shen:settle-public`
  - `修家账接族亲`：承接 `shen:family-escort / shen:postwar-household / shen:settle-household`
  - `抄讲义续门生`：承接 `shen:postwar-lecture / shen:settle-books`，把守书脉继续压成年头层
  - `裁旧稿跑公事`：承接 `shen:postwar-public / shen:settle-public`，把公开位置收窄后的晚段活法写成日常
  - `走亲访旧稳家门`：承接 `shen:postwar-household / shen:settle-household`，把门内修补继续写成年头账
- `晒书箱抄旧目录 / 替家门抄帖回门信`：分别把 `books / household` 两条家族分流压到更细的沈家日常
- `替报馆校样问纸路 / 借学会名帖探风声`：专门补厚此前偏通用的 `public` 分流，把“守文字/跑公事”落到 `校样 / 纸路 / 名帖 / 口风` 这一层可反复计划的日常
- `demo.html` 本轮继续把 `subjects.connections` 接到沈家**非海外**主轨：`shen:prewar-books / shen:postwar-lecture / shen:settle-books` 与 `woman:*teach` 现在会把 `门生 / 课脉` 显性写进 HUD 与终局；`shen:prewar-public / shen:postwar-public / shen:settle-public` 与 `woman:*public` 则会把 `报馆旧交 / 学会名帖 / 公事旧路` 一并收回，不再只靠主角一句“我还在做事”。
- `demo.html` 现已支持**带 trigger 的年度事件守门**，因此 `scholar` 又补入 7 条男线专属年度事件：
  - `流亡课堂与留城文字`（`1938`）：只在 `shen:school-south / shen:press-stay` 且未 `abroad:gone` 时触发，避免把出洋线误判成南迁线
  - `留城报馆与纸路余账`（`1938`）：只在 `shen:press-stay` 时触发，把纸张、校样、口风与署名取舍写成留城文字线的专属后账
  - `归城旧账重开`（`1947`）：承接 `shen:postwar-*`，把讲席、报馆、人情与家门旧账一起写回年度层
  - `归城讲席与门生旧账`（`1947`）：只在 `shen:postwar-lecture` 时触发，把讲义、束脩、借课室与旧门生拆成 `books` 分流自己的中继余账
  - `归城公事与名帖旧账`（`1947`）：只在 `shen:postwar-public` 时触发，把 `public` 分流继续从通用“归城旧账”里拆细出来
  - `守书脉后的讲义余账`（`1949`）：承接 `shen:settle-books`，把讲义、门生与借来的课室继续压成余账
  - `收窄公开位后的旧稿余账`（`1949`）：承接 `shen:settle-public`，把旧稿、人情与地方公事继续压成年头消耗
  - `门内修补后的家计余账`（`1949`）：承接 `shen:settle-household`，把族亲、家计与门内人心继续写回年度层
- 这一步的目标不是再加一句结尾，而是把“南迁/守报/修家门”从关键情境之间，再补回可反复计划、可跨年触发、可被 HUD 与终局同时回收的一层日常；其中 `public` 分流现在也必须像 `books / household` 一样，拥有自己的家族化日常与中继余账，而不再只靠通用 `scholar` 动作撑住；`books` 分流也要像 `public` 一样，在 `1947` 就先长出自己的中继细账，而不是一直等到 `1949` 才露出专属余账。
- 验证口径：要同时验证 `__DEMO.plan('copy-class-ledger'|'trim-public-notes'|'walk-kin-doors'|'shen-proof-route'|'shen-public-calls')` 的门控，以及年度推进后 `firedEvents` 中是否真实出现 `shen-lecture-return-remnant / shen-public-return-remnant / shen-public-remnant`，避免新增动作只存在于动作池、没真正写回跨年后果。

---

# 节点 N-06：上海工商家 · 女线从租界风雨到金圆券前夜
> 目标是把当前主要复用 `newwoman-path` 的 `上海工商家` 女线补出家庭专属中继：不是只写“新女性抗婚”，而是把**租界教育、家业震荡、工人家口与个人退路**真正接成一条线。

## ① 父快照 P₀
| 字段 | 值 | 说明 |
|---|---|---|
| 家庭 | `shanghaigongshang` | `actions-data.js` 已定义 |
| 主路径 | `newwoman-path` | 女性默认主干 |
| 起点资源 | 钱财/人脉/声望较高，受洋学堂教育 | 已实现 |
| 通用上游 | `父母之命·婚约`、`娜拉时刻·是否出走`、`抗婚以后·先把日子接住` | 通用新女性主干仍保留 |
| 未知项 | 战时租界与家门哪一头先塌、金圆券前夜哪条关系真保得住 | 保留 `unknown` |

## ② 本轮新增运行时节点
- `租界风雨·先把哪一头撑住`
  - 触发：`family=shanghaigongshang + origin=gentrywoman + yearFrom:1937`
  - 分化：`shdaughter:war-relief / shdaughter:war-school / shdaughter:war-household`
  - 作用：把“工商家女儿的战时回应”拆成安顿工人家口、跟学校撤往内地、留守租界家门三条专属路径
- `金圆券前夜·先替自己保哪条退路`
  - 触发：`yearFrom:1948`，承接 `shdaughter:war-*` 与 `woman:war-*`
  - 分化：`shdaughter:gold-hide / shdaughter:gold-household / shdaughter:gold-public`
  - 作用：把 `1948` 前夜拆成“先保个人退路 / 先接家中与女工口粮 / 先守公共门面”三种不同主体回应
- `风暴过去以后·先把后半生安在哪一层`
  - 触发：`yearFrom:1949`，承接 `shdaughter:gold-*`
  - 分化：`shdaughter:settle-teach / shdaughter:settle-household / shdaughter:settle-public`
  - 作用：把风暴后的长期结果明确成“自立饭碗 / 持续接住家口 / 收窄公共门面”三条后半生，不再从 `1948` 直接跳到终局

## ③ 1949 回收口径
- `demo.html` 终局现已回收：`nora:* / woman:* / shdaughter:* / end:*`
- 只回收以下事实：
  - 你在战时先接住的是工人家口、女学饭碗，还是租界家门
  - 金圆券前夜你先保的是个人退路、身边人的口粮，还是女学/赈济这点公共门面
  - 风暴过去以后，你最后把后半生安成了自立饭碗、持续接住家口，还是收窄后的公共位置
  - 留大陆以后，这条线不再被伪装成通用“新女性”结局，而会带着工商家家门与厂务震荡的后账一起收束

## ④ 独立轨补口（本轮）
- `上海工商家` 女线仍主要复用 `newwoman-path` 的主干节点；为让它不再只靠节点串联，本轮同样把“战时与后半生的过法”下沉到可计划行动层：`actions-data.js` 为 `gentrywoman` 新增的一组 `women-*` 与家族专属 `trigger` 行动，会在 `family=shanghaigongshang` 且命中 `shdaughter:* / woman:*` 对应事实态时自动可用。
- 新增更贴近工商家女线 `public` 分流的家族动作：
  - `shdaughter-public-calls`：承接 `shdaughter:prewar-public / shdaughter:war-relief / shdaughter:postwar-public / shdaughter:gold-public / shdaughter:settle-public`，把 `同业会馆 / 熟客旧交 / 赈济联系人 / 公共门面` 压成可反复计划的慢动作，而不再只靠通用 public 行动承重。
- 已落地的两条女线专属独立轨动作也继续按完整多年线理解，而不是再当作战时侧枝：
  - `shdaughter-shelter-ledger / shdaughter-shelter-routes`：承接 `prewar -> war -> postwar -> gold -> settle shelter`，把屋檐、空屋、旧校舍与接应名册持续写成年头层。
  - `shdaughter-workroom-ledger / shdaughter-workroom-routes`：承接 `prewar -> war -> postwar -> gold -> settle workroom`，把女工散工、熟手零单与门里小账持续写成年头层。
- `demo.html` 现已支持**带 trigger 的年度事件守门**，因此 `gentrywoman` 这轮也为 `上海工商家` 女线补入 2 条 family 专属年度事件：
  - `租界门第的前战门路账`（`1936`）：现已同时承接 `prewar-teach / prewar-public / prewar-household / prewar-shelter / prewar-workroom`，不再漏掉 `workroom` 这条前战多年起手。
  - `租界风雨与女学后账`（`1938`）：仅在 `family=shanghaigongshang` 且命中 `shdaughter:war-*` 时触发，把工人家口、撤校求学、留守家门、借住屋檐与互帮小作一起写成多年余账。
  - `战后同业与门面旧账 / 战后借住与接应旧账 / 战后散工与手账旧账`（`1947`）：分别承接 `public / shelter / workroom`，把三条顾家女线分流都拆成自己的中继余账。
  - `风暴前夜的门路与口粮账 / 风暴后门内与女学余账`（`1948-1949`）：共同承接 `gold-*`，把风暴前夜与风暴后的承压都回收成年度层。
  - `风暴后屋檐与安身名册余账 / 风暴后互帮小作余账`（`1949`）：分别只在 `settle-shelter / settle-workroom` 时触发，把两条顾家女线独立轨正式写回后半生。
- `demo.html` 本轮继续把 `subjects.connections` 接到工商家**非海外**主轨：`shdaughter:prewar-public / shdaughter:war-relief / shdaughter:postwar-public / shdaughter:gold-public / shdaughter:settle-public` 会把 `同业门路 / 赈济联系人 / 公共旧交` 写进 HUD 与终局；`biz:postwar-public / biz:gold-burn / biz:settle-public` 则会把 `同业会馆 / 熟客旧交 / 风暴里替你续门面的那层关系` 一并接回运行态。新补的 `shdaughter-public-calls + 战后同业与门面旧账` 进一步保证顾家女线的 `public` 分流，在 `action/event/HUD/终局` 四层都不再只靠一句“继续安顿场面”概括。
- 这一步让工商家女线在“租界风雨 → 金圆券前夜 → 风暴后安放”之间，不只插入可反复的年头日常（教书/赈济/守家门/写稿），也让 `shelter / workroom` 两条独立轨都具备从前战起手的年份骨架。`inspectFamilyIndependentTrackCoverage()` 现在会直接验证顾家女线的 `shelter / workroom` 是否同时具备 `主链验证 + 动作层 + 年度事件层`，避免它们再次退回“节点写过，但年头层缺口仍在”的灰区。

---

# 节点 N-07：苏北贫农家 · 士兵线从 1948 后续到 1949 余生安放
> 目标不是再给 `soldier` 线多写一句结局，而是把它补成真正的闭环：**被抓丁 / 从军 / 复员或伤退 / 归乡或留外 / 1949 前把余生收成一种可见活法 / 终局事实回收**。

## ① 父快照 P₀
| 字段 | 值 | 说明 |
|---|---|---|
| 家庭 | `subeipoor` | `actions-data.js` 已定义 |
| 主路径 | `soldier-path` | `draft:soldier` 后强制切入 |
| 上游连续节点 | `soldier-camp-life / soldier-battle / soldier-after-battle-years / soldier-rear-years / soldier-demobilize / soldier-home-after-allocation / soldier-outside-late-settle` | 已实现 |
| 当前分岔 | `归乡接地` 与 `退伍在外` | 两条都已进入 `1948` 层 |
| 未知项 | 退伍兵更细的地方差事结构、再婚/改嫁、地方兵役数值口径 | 继续保留 `unknown` |

## ② 本轮新增运行时节点
- `1949 到来·归乡兵把后半生收成哪一种人`
  - 触发：`yearFrom:1949`，承接 `soldier:settle-ledger / soldier:settle-mutual / soldier:settle-watch`
  - 分化：`soldier:late-home-ledger / soldier:late-home-mutual / soldier:late-home-twoends`
  - 作用：把“分田以后能不能接住”继续落成“后半辈子究竟收成一户日子、互帮慢守，还是门里门外两头并行”
- `1949 到来·退伍在外把余生收在哪一头`
  - 触发：`yearFrom:1949`，承接 `soldier:outside-settle-labor / soldier:outside-settle-watch / soldier:outside-settle-drawn`
  - 分化：`soldier:late-outside-labor / soldier:late-outside-watch / soldier:late-outside-drawn`
  - 作用：让“退伍在外”不只在 `1948` 停成一句说明，而是继续长成常年饭碗 / 带退路活法 / 地方差事牵连三种长期结果

## ③ 1949 回收口径
- `demo.html` 终局现已回收：`soldier:* / life:* / wife:* / end:*`
- 只回收以下事实：
  - 你是把归乡后的新地和家口收成了一户日子、互帮慢守，还是门里门外两头并行
  - 你是把退伍在外的苦力门路熬成常年，继续边干边看留退路，还是仍被地方差事牵住
  - 不把 `soldier` 线重新压扁成“当过兵，后来留在大陆/离开大陆”的一句终局摘要

## ④ 独立轨补口（本轮）
- `actions-data.js` 现已新增 `soldier` 专属年度事件骨架：`camp-attrition / front-chaos / camp-transfer-remnant / army-bond-remnant / front-watch-remnant / front-rotation-remnant / army-hunger / rear-route-remnant / rear-manifest-remnant / demob-remuster / demob-paper-backlog / soldier-home-reledger / soldier-outside-remnant`。
- 这组事件不再只覆盖“被带走 / 会战 / 复员”三个大钉子，还继续把两段此前偏薄的年月补成可验证骨架：
  - `camp-transfer-remnant`（`year=1939`）：承接 `draft:soldier / soldier:drilled / soldier:camp-support / soldier:camp-bond`，把“补员、转营、烂鞋与拉长脚程”写成新兵营之后仍持续存在的军中年头。
  - `army-bond-remnant`（`year=1940`）：承接 `draft:soldier / soldier:drilled / soldier:camp-support / soldier:camp-bond / soldier:survive / soldier:rear / soldier:front-* / soldier:rear-*`，把“旧鞋、欠饷、同乡互保和军中搭伙账”写成服役中段的真实年头。
  - `front-watch-remnant`（`year=1941`）：承接 `soldier:survive / soldier:front-* / soldier:rear-*`，把“守哨、雨夜、半睡半醒的兵年”补成大战之间那层不显眼却很磨人的年头。
  - `front-rotation-remnant`（`year=1942`）：承接 `soldier:front-hold / soldier:front-learned`，把“前线换防、补土窝、守残班与一遍遍认地形”从“还在前线”这句概括里再拆出来，避免前线多年线继续只靠大战名词承重。
  - `rear-route-remnant`（`year=1943`）：承接 `soldier:front-shift-rear / soldier:front-learned / soldier:rear / soldier:rear-*`，把“后方转运、后送线与路耗”写成后勤长年的年度后账。
  - `rear-manifest-remnant`（`year=1944`）：承接 `soldier:front-shift-rear / soldier:rear / soldier:rear-*`，把“抄单、认路、押运、交接与赶脚程”写成后勤年头更细的一层旧账，让后方线也拥有与前线换防对应的普通生活骨架。
  - `army-years-loosen`（`year=1944`）：承接 `soldier:front-* / soldier:rear-*`，把“胜利前夜老兵心气与去路先松动”写成复员前就已发生的长期后账，而不是到 `1945` 才突然出现。
  - `demob-paper-backlog`（`year=1946`）：承接 `soldier:demob-* / life:* / soldier:outside-*` 的早段退伍态，把“路条、证明、铺位与回乡盘缠继续拖账”落成复员后第一两年的制度余波。
  - `soldier-home-reledger`（`year=1947`）：承接 `soldier:return-* / soldier:team-*`，把“归乡以后仍要重认旧屋、家口与乡里风声”写成归乡兵线的年度后账，而不只停在情境节点上。
  - `soldier-outside-remnant`（`year=1947`）：承接 `soldier:demob-stay / soldier:demob-drift / soldier:outside-*`，把“退伍留外以后铺位、短工、旧袍泽和地方差事一样都不稳”写成留外兵线的年度后账。
- `actions-data.js` 现也已新增按阶段守门的 `soldier` 专属动作，并把原先未守门的基础现役动作一并收回军中阶段：
  - 新兵营 / 军中年头：`drill / march / rear-duty / write-home / trade-ration / rest-army / camp-buddy / patch-boots / night-watch / front-dugout-shift / army-ledger / salvage-kit / rear-manifest-run / escort-wounded / seek-demob / demob-station-wait`
  - 退伍在外：`dock-labor / keep-route-open / bunk-rest`
  - 归乡接地：`repair-home / swap-light-work / home-rest`
- 新增动作里，`front-dugout-shift` 与 `rear-manifest-run` 这组前后对应，专门把“前线多年硬熬”和“后方多年押运”压回可反复计划的普通年头；`demob-station-wait` 则把“复员待遣”从一条年度拖账，再下沉成站台、路条与借宿这层年头事务。
- 其中 `drill / march / rear-duty / trade-ration / rest-army` 现已不再是一旦切进 `soldier-path` 就终身常驻；一旦人物进入 `复员 / 归乡 / 留外` 晚段状态，就必须由 `trigger + noneEcho` 自动退场，让“现役动作”和“退伍后的日子”在运行时真正分层。
- 这一步不是单纯多写几项按钮，而是把“士兵线已经走到哪一层生活”从情境与终局之间，再补回一层可操作的日常，避免 `soldier-path` 只有情境节点厚、行动层仍像一张全年通用表。
- 这一步的目标不是替代既有情境节点，而是把 `soldier-path` 真正补成 `origin / action / event` 三层同时存在的轨道：
  - `origin/action`：继续由 `soldier-path -> soldier` 复用独立行动表；
  - `situation`：继续承担“新兵营里怎么熬 / 会战后怎么分化 / 复员后去哪”这类主体现实回应；
  - `event`：补上“军中年头本身的时代骨架”，让待编、会战、拖长的兵年与胜利后待遣散，不再只靠情境与终局文案存在。
- `demo.html` 的 `1949总账 / 事实回收` 现也会把这些 `firedEvents` 写回总账，避免 `soldier` 线明明已有独立年度事件，却在终局里重新失声。
- HUD 这一轮也把 `soldier-path` 的 `跨年余账` 单独显出来：当人物已切进从军线时，第五段事实不再继续沿用通用 `觉醒/土改路径`，而会优先显示 `新兵营欠饷疫病 / 军中搭伙旧鞋账 / 前线换防与残班旧账 / 后方抄单与押运旧账 / 复员纸面拖账 / 归乡旧屋与新风声 / 退伍在外铺位差事` 这类更贴近从军线的跨年事实。
- 本轮再把士兵线的**外部门路主体层**补进统一模板：`demo.html` 里 `subeipoor.connections` 不再只服务逃亡/难民并账，而会按已有 echoes 自动收成五类关系账
  - 军中：`军中搭伙 / 旧鞋欠账`
  - 归乡：`旧乡风声 / 邻里认人`
  - 留外：`铺位差事 / 旧袍泽旧路`
  - 顺势：`门里口粮 / 外头跑差`
  - 潜躲：`藏身旧识 / 退身旁路`
- 约束：这些对象必须都能回溯到现有节点、动作或年度事件文本；不得新增具体地名、团体名称或人物身份。无仓库证据的更细组织层继续保留 `unknown`。
- 继续按统一模板补齐 `1949` 晚段余账层：
  - `soldier-home-late-remnant`：承接 `soldier:late-home-ledger / soldier:late-home-mutual / soldier:late-home-twoends`，把“归乡后半生已经收成一种活法”继续压回“旧屋、家口与兵年脾气仍要反复认账”的年头层。
  - `soldier-outside-late-remnant`：承接 `soldier:late-outside-labor / soldier:late-outside-watch / soldier:late-outside-drawn`，把“退伍在外已经收成常年饭碗 / 留退路 / 被差事牵住”继续压回“铺位、熟路与退身口仍要年年再认”的年头层。
- 这一步的意义不是再多写一句 `1949` 结局，而是把 `归乡晚段 / 留外晚段 / 顺势晚段 / 逃兵晚段` 四类结果都补成同一模板：`晚段结果态 + 1949 firedEvent 余账 + HUD / 终局 / 总账同步回收`，避免 `soldier-path` 内部不同支路厚薄不一。

---

# 节点 N-08：苏北贫农家 · 逃亡线到 1949 后半生安放
> 目标不是把 `C4 flee` 再写成一串“回去/不回去”的姿态，而是把它补成真正闭环：**被抓丁而逃 / 路上过关或受阻 / 后方落脚 / 战后探路或停留 / 返乡回接或留在后方 / 1949 前把余生收成一种可见活法 / 终局事实回收**。

## ① 父快照 P₀
| 字段 | 值 | 说明 |
|---|---|---|
| 家庭 | `subeipoor` | `actions-data.js` 已定义 |
| 主路径 | `flee-refugee` | `draft:flee` 后切入 |
| 上游连续节点 | `flee-with-mother / flee-road-tenant / flee-resettle-tenant / flee-war-end-reckoning / flee-return-probe-aftermath / flee-return-wind-1948 / flee-return-at-workteam / flee-return-after-allocation` | 已实现 |
| 当前分岔 | `留在后方` 与 `返乡回接` | 两条都已进入 `1948` 层 |
| 未知项 | 旧乡哪一层关系还能真正接回、返乡后具体哪块地能不能长久守住、后方哪处落脚最终最稳 | 继续保留 `unknown` |

## ② 本轮新增运行时节点
- `1949 到来·留在后方的逃亡者把余生收在哪一头`
  - 触发：`yearFrom:1949`，承接 `flee:stay-away / flee:still-drifting / flee:return-hear / flee:return-cut`
  - 分化：`flee:late-away-root / flee:late-away-kin / flee:late-away-drifting`
  - 作用：把“后方落脚”从临时过渡推进成后半生安放，区分“后方扎根 / 依亲互帮 / 继续留脚程退路”三种长期结果
- `1949 到来·返乡后的逃亡者把后半生收成哪一种人`
  - 触发：`yearFrom:1949`，承接 `flee:settle-ledger / flee:settle-edge / flee:settle-two-ends`
  - 分化：`flee:late-home-ledger / flee:late-home-edge / flee:late-home-twoends`
  - 作用：把“返乡以后接到一点地和屋”继续推进成“返乡成户 / 守细过活 / 两头并行”的长期结果层，而不把逃亡前史抹平

## ③ 1949 回收口径
- `demo.html` 终局现已回收：`flee:* / mother:* / wife:* / end:*`
- 只回收以下事实：
  - 你是把后方那处棚屋、亲族与零碎饭碗慢慢过成了真正的家，还是仍把脚程留成退路
  - 你是把返乡后的旧屋、新地与家口重新收成了一户日子，还是继续把后方与返乡两头一起过
  - 不把 `C4 flee` 再压扁成“后来有没有回乡”的一句摘要，也不把它自动伪装成留乡主干

## ⑬ 仍未完成的设计内容（明确保留，不编造）
- `C4` 现已继续长到“后方安身 / 探路回接 / 重新站进丈量场 / 返乡后把地接回手 / 1949 后半生安放”这几层，但**仍未**长成完整难民专属行动表与事件线；目前还是以专属情境节点串联为主，而不是独立新 track。
- `soldier-path` 现已补到更连续的独立 `action/event` 骨架：服役中段、归乡后账、留外残账都已有年度事件承接；但军中服役段仍主要靠情境节点串联，`待编 / 新兵营普通生活 / 会战后续 / 后勤长年 / 复员去路` 还没有完全下沉成更细的年度生活流与更多独立事件层。
- `江南沈家` 与 `上海工商家` 现都已补到“战时 / 战后 / 长期结果 / 终局回收”四层；其中 `上海工商家` 男线又进一步把 `expand-risk / foreign-shield` 两类接班底色拉进了 HUD 与自然回放。本轮起两家已不再只是“关键节点闭环”：`沈家 records / letters` 与 `顾家 route / coop / shelter / workroom` 已接进 `inspectFamilyIndependentTrackCoverage()`，能逐条验证是否具备 `主链验证 + 动作层 + 年度事件层`。目前仅剩更细制度参数与更多主体长期牵制变体仍未系统化展开，保留 `unknown`。
- 苏北地方**兵役具体数值**（抽丁比例、壮丁费银元数）——`需核实`，暂 unknown。
- 病母/妻的**独立回应表**（各自目标+触发条件+去留轨迹）与**死亡确认状态机**，目前已做病母亡故确认，且病母后事的两种确认事实 `mother:buried / mother:buried-plain` 已接入终局事实回收；妻在逃亡/从军失联下的局部回应与“多年无准信后按亡故确认”也已有，但完整回应表仍未成系统层规则。
- 名额容量的随机模型参数（配额/宗族权重/贿赂弹性）。
- 引擎侧：父快照冻结、多阶段随时间展开、`endingVerdict` 由评分改为事实陈述、信息时点校验(INV-7)。

---

# 节点 N-09：难民/流亡者（出身线）· 1937—1949 主链闭环
> 目标不是再加一条“背景介绍”，而是把 `refugee` 出身线补成可玩的闭环：**家园将塌 → 主体回应选择 → 路上处境 → 后方落脚 → 战后回望 / 留在后方后的再接一层 → 1949 前后半生安放 → 终局事实回收**。这一条线不复用苏北抓丁/土改派生状态；只写“流亡/落脚/安放”的事实链，未知处明确保留 `unknown`。

## ① 本轮新增运行时节点（已落地）
- `家园沦陷·先护哪一头`（`refugee-homeland-choice`，`year=1937`）：分化 `refugee:flee-with-family / refugee:send-family / refugee:stay-risk`
- `流亡路上·先拿什么去换一段活路`（`refugee-road-choice`，`year=1938`）：分化 `refugee:road-survived / refugee:road-stalled / refugee:road-loss`
- `后方落脚·先把家安在哪`（`refugee-resettle-choice`，`year=1940`）：分化 `refugee:settle-shack / refugee:settle-kin / refugee:settle-split`
- `战后回望·旧地与新落脚处怎么认`（`refugee-postwar-choice`，`yearFrom=1946`）：分化 `refugee:return-probe / refugee:stay-away / refugee:still-drifting`
- `回望旧地后·先把哪一层消息接回后半辈子`（`refugee-return-probe-aftermath`，`yearFrom=1947`）：分化 `refugee:return-listen / refugee:return-cautious / refugee:return-yield`
- `旧地风声再起·回望后的再记一笔`（`refugee-return-wind-1948`，`yearFrom=1948`）：分化 `refugee:return-hear / refugee:return-bridge / refugee:return-cut`
- `回望以后·旧屋旧路先接哪一头`（`refugee-return-home-reentry`，`yearFrom=1948`）：分化 `refugee:home-ledger / refugee:home-edge / refugee:home-twoends`
- `返乡以后·重新站进丈量场`（`refugee-return-at-workteam`，`yearFrom=1948`）：分化 `refugee:team-facts / refugee:team-edge / refugee:team-twoends`
- `返乡以后·重新接回手的屋与地怎么过`（`refugee-return-after-allocation`，`yearFrom=1948`）：分化 `refugee:settle-ledger / refugee:settle-edge / refugee:settle-two-ends`
- `后方风声再紧·留在这头的人先把哪层日子接牢`（`refugee-away-wind-1948`，`yearFrom=1948`）：分化 `refugee:away-rooted / refugee:away-kin-linked / refugee:away-route-held`
- `1949 到来·留在后方的流亡者把余生收在哪一头`（`refugee-1949-settle`，`yearFrom=1949`）：分化 `refugee:late-root / refugee:late-kin / refugee:late-drift`
- `1949 到来·把旧屋重新接回手的人把余生收成哪一种活法`（`refugee-home-1949-settle`，`yearFrom=1949`）：现改为承接 `refugee:settle-ledger / refugee:settle-edge / refugee:settle-two-ends` 后再分化 `refugee:late-home-ledger / refugee:late-home-kin / refugee:late-home-twoends`

## ② 与年度事件骨架的边界
- `actions-data.js` 仍保留 `homeland-fall / road-crisis / resettle` 三个年度事件作为时代骨架；本节点组不替代它们，而是补上“主体回应/长期结果”的可选分化层。
- 现在 `refugee` 已不再只是“节点闭环”，而开始具备一层真正的 `action + year event` 双骨架；并且本轮把这层共享守门从“只认 `track:flee-refugee`”收回成“统一按 `origin=refugee` 复用”，因此它现在会同时服务 `难民/流亡者` 出身线与苏北 `flee-refugee` 逃亡线：
  - 行动层：`road-barter / patch-shelter / split-remit / trace-old-home`，分别把“路上拿物件换口粮”“搭棚补檐接落脚”“两头送粮跑脚程”“托人打听旧地消息”下沉成可反复计划的年头日常；此前已补入 `keep-neighbor-roof / mend-old-yard`，把“后方把街坊过成家”“返乡后把旧屋与田边重新接回手”继续压成年头层；本轮又新增 `rear-kin-rations / rear-route-oddjob`，把“后方亲族互帮重新接牢”“后方脚程与零碎活继续留旁路”也压回 `1948+` 年头动作，并把 `split-remit / keep-neighbor-roof` 的守门继续扩到 `refugee:away-* / flee:away-*` 这层新中继回响。
  - 年度事件层：`refugee-road-remnant / refugee-shelter-remnant / refugee-return-probe-remnant / refugee-postwar-remnant / refugee-away-reledger-remnant / refugee-return-home-remnant / refugee-return-workteam-remnant / refugee-away-late-remnant / refugee-home-late-remnant`，分别把“路上余粮与病气”“棚屋借檐与两头口粮”“探路往返与旧屋口风”“旧地消息与现今余账”“后方屋檐与活路后账”“返乡补屋与旧街坊”“返乡站场与两头脚程”“后方街坊与脚程”“返乡旧屋与两头活路”写成跨年的余账，而不只靠节点或终局一句带过。
- 事件成败细节与离散具体人名，不在本轮硬写；必要处保持 `unknown`，只允许用资源变化与回响标记记账。

## ③ 主体推进与多年确认（已落地）
- 当 `refugee:road-loss` 命中后，运行时不再把它立刻写死成“谁已经死亡”；当前会按 `途中折损未明 → 多年无准信 → 折损已确认` 三层推进：
  - 初次命中时，只确认“这一路确实发生过折损”，不补写具体是谁失散；
  - 拖过两年仍无准信，自动写入 `refugee:loss-prolonged`；
  - 再拖过四年，自动写入 `refugee:loss-confirmed`，但仍不补写具体人名与地点，继续保留 `unknown`。
- 这层规则与 `soldier:missing-prolonged / soldier:missing-confirmed` 共用同一设计口径：**确认的是“多年无准信后事实被怎样记账”，不是突然获得完整真相。**
- 运行时实现（`demo.html`）已把 `refugee` 折损的“2 年拖长 / 4 年确认”从散落的线内 `if` 收敛为统一的多年确认规则表 `LONG_CONFIRM_RULES` + `advanceLongConfirmations()`；这一步的目的不是新增设定，而是让后续把更多“多年确认”事实（例如更多 `refugee` 的离散、或其它线的类似拖长）并入同一层时，不必重复写一套特判逻辑。当前 `soldier:missing` 仍保留专属逻辑（因牵涉妻子去留与名分确认），尚未并入该表。

## ④ 1949 事实回收口径（已落地）
- `demo.html` 的 `buildFinalLedgerFacts()` 现已同时从 `refugee:*` 回响与 `firedEvents` 骨架写回事实句，避免“出身线没有 echo 就无法回收”的结构性缺口。
- HUD 现已新增 `refugee` 出身线的五段事实层（家园与离散 / 路上处境 / 后方落脚 / 跨年余账 / 后半生安放）；其中“跨年余账”现已显性读取 `refugee-road-remnant / refugee-shelter-remnant / refugee-return-probe-remnant / refugee-postwar-remnant / refugee-away-reledger-remnant / refugee-return-home-remnant / refugee-return-workteam-remnant / refugee-away-late-remnant / refugee-home-late-remnant`，用于验证这条线是否真的长出“路上之后 / 落脚之后 / 留在后方后重新接牢一层 / 回望旧地之后 / 真把旧屋接回手之后 / 返乡后重新站进制度场 / 1949 晚段余账”的年头层；“后半生安放”也开始区分 `留后方` 与 `返乡后` 两组收束回响，不再只剩 `late-root / late-kin / late-drift` 一套。本轮又把 `connections` 继续拆成可分项回收的长期结果：留后方时显性区分 `后方街坊 / 借檐屋檐 / 亲族互帮 / 脚程活路 / 后方去路`，返乡后显性区分 `旧屋账 / 旧屋亲邻 / 回乡照应 / 两头活路`；并且只要当前状态已经明确落在 `away-route-anchored / home-kin-returned / home-twoends-returned` 这些代表态，HUD 与终局还必须继续显性回收 `主体牵制`，回答“是屋檐先顶住，还是脚程/回乡照应/两头活路先顶到前面”。
- `endingVerdict()` 与 `buildFinalLedgerFacts()` 现都已写回这组年度余账，避免“动作与年度事件已经存在，但第一页主叙事和 1949 总账看不见”的断层。
- 本轮除了保留 `refugee:home-twoends -> refugee:late-home-twoends` 这条“返乡后仍两头并行”的长期结果，还新增把 `refugee:late-home-ledger` 与 `refugee:late-drift` 正式纳入统一验收：前者代表“返乡先把旧屋账重新接回手”，后者代表“留后方时把脚程活路留成后路”，因此返乡/留后方两组主收口都不再只靠单一代表态站住。
- 本轮还新增 `natural-refugee-away-route / natural-refugee-home-ledger`，并保留 `natural-refugee-kin-root / natural-subei-flee-away-root` 的真实年份推进，明确验证“留在后方”与“返乡以后”都必须经过各自中继层，再进入 `1949` 收束，而不是从 `1946` 直接跳到晚段；随后又把 `natural-refugee-away-route / natural-refugee-home-kin / natural-refugee-home-twoends / natural-subei-flee-home` 一并拉进 `主体牵制` 验收，防止“结果事实已回收，但让位关系只停在规则表或终局文本里”。

---

# 节点 N-10：纱厂女工（出身线）· 包身、工潮到后半生安放
> 目标不是只把 `包身契 / 罢工 / 赎身` 当成年轻时的几枚标签，而是把 `millworker` 出身线补成真正闭环：**包身前提 → 工潮站位 → 跨年后账 → 战时活法 → 战后几年中继 → 1948-1949 后半生安放 → 终局事实回收**。这一条线继续复用现有 `millworker` 的独立 `origin/action/event` 骨架，不编造具体工会组织结构、厂名或个人传奇。

## ① 父快照 P₀
| 字段 | 值 | 说明 |
|---|---|---|
| 出身 | `millworker` | `actions-data.js` 已定义 |
| 起点前提 | `contract / big-strike / own-wage` | 包身契、工潮风波、赎身念头均已有年度事件骨架 |
| 已有显性选择 | `参与罢工`、`赎身自立` | `choices-data.js` 已实现 |
| 未知项 | 具体厂名、具体工头/工会人物、战时哪一处厂最终能长期留下 | 继续保留 `unknown` |

## ② 本轮新增运行时节点
- `工潮过去·先把日子接在哪一层`
  - 触发：`hasEvent:contract + yearFrom:1926`
  - 分化：`labor:line-core / labor:line-edge / labor:blacklist-drift`
  - 作用：把 `1925` 前后的单次工潮推进成几年后的现实活法，区分“留厂抱团熬班”“缩回机台攒钱”“离厂转小厂/零工”
- `战火压厂·先把命和饭碗护哪一头`
  - 触发：`yearFrom:1937`，承接 `labor:line-* / free:redeemed`
  - 分化：`labor:war-shift / labor:war-mutualaid / labor:war-hide / labor:war-ledger`
  - 作用：把抗战写成女工自己的主体回应，而不是只靠年度事件一笔带过
- `战后几年·先把工友、字路和饭碗接回哪一层`
  - 触发：`yearFrom:1946`，承接 `labor:war-* / labor:line-* / free:redeemed`
  - 分化：`labor:postwar-smalltrade / labor:postwar-mutualaid / labor:postwar-household / labor:postwar-ledger`
  - 作用：把女工线正式补出“战后几年怎么接”的独立中继，不再从战时直接跳到 `1948-1949`
- `风声再紧·自由身和饭碗先收在哪一头`
  - 触发：`yearFrom:1948`，承接 `labor:postwar-*`
  - 分化：`labor:settle-smalltrade / labor:settle-mutualaid / labor:settle-household / labor:settle-ledger`
  - 作用：把 `1948-1949` 从“活下来了”继续推进成“后半辈子靠什么活、把人情和家口收在哪一层”

## ③ 主体推进与多年确认（已落地）
- `参与罢工` 的出头选项，运行时现已区分成功/失败回响：
  - 成功：`labor:leader`（留下“出头/组织”的事实）
  - 失败：`labor:blacklisted`（留下“被列黑名单”的事实）
- `demo.html` 已把 `millworker` 的“工伤/黑名单”补进可复用的多年确认层（与 `soldier/refugee` 同口径）：
  - 工伤：当运行中健康长期见底（`health<=24`）且此前尚未记账时，先写入 `labor:injury-unconfirmed`（伤病未明）；拖过两年写入 `labor:injury-prolonged`；再拖过四年写入 `labor:injury-confirmed`（病根已确认）。确认的是“多年之后如何记账”，不是突然补写完整诊断；具体伤处继续保留 `unknown`。
  - 黑名单：当写入 `labor:blacklisted` 或已走到 `labor:blacklist-drift` 后，运行时会按年推进 `labor:blacklist-prolonged / labor:blacklist-confirmed`，表示“黑名单后账拖长/钉住”。具体名单如何流转、是否有明文制度，继续保留 `unknown`。
- 为减少“多年确认”在不同线里各写一套特判导致的口径漂移，运行时（`demo.html`）现已把女工工伤与黑名单的推进也一并并入 `LONG_CONFIRM_RULES`：未来若要把更多“未明→拖长→确认”事实纳入同一口径，可以先加规则表项，再补 HUD/终局回收映射，而不是继续扩展长 `if-else`。
- 这层规则的目标是让 `纱厂女工` 不再只靠一次性 `echo` 叙述“伤病/黑名单/转业”，而能在 HUD 与终局按“未明→拖长→确认”的节奏显性验证闭环。
- 为避免 `纱厂女工` 仍停在“关键节点闭环”，本轮继续把“节点之间的很多年”下沉成独立 `action/event` 年头层，而不补写具体厂名、工头或组织人物：
  - `actions-data.js` 已新增并继续延长 6 条阶段化动作：
    - `跟熟工转机台接活`：承接 `labor:war-shift / labor:postwar-smalltrade / labor:settle-smalltrade`，把“跟着机器和熟工换地方做活”做成长期日常。
    - `借工友路数互帮接活`：承接 `labor:war-mutualaid / labor:postwar-mutualaid / labor:settle-mutualaid`，把“互帮与熟关系”做成持续饭碗。
    - `躲风头接零碎手工`：承接 `labor:blacklist-drift / labor:blacklist-* / labor:war-hide`，把“黑名单后怎么活、躲风头怎么熬”落成可反复计划的手工与散活。
    - `给家口张罗口粮`：承接 `labor:war-mutualaid / labor:postwar-mutualaid / labor:postwar-household / labor:settle-household`，把“后半生把重心收回家口”下沉成真正要年年去做的动作。
    - `替工友记工账认票据 / 替里弄递信续字路`：现在都继续承接 `labor:postwar-ledger / labor:settle-ledger`，把字路细活真正拖进战后几年。
  - `actions-data.js` 已新增并继续压实 5 条女工专属年度事件：
    - `黑名单与散工余账`（`1931`）：承接 `labor:blacklisted / labor:blacklist-drift / labor:blacklist-*`，把原厂门槛、小厂散工与不稳工位写成跨年后账。
    - `战时机台与口粮后账`（`1938`）：承接 `labor:war-*`，把机台、住处、口粮与病气拖成长年余账。
    - `战后宿舍与熟工旧账`（`1947`）：承接 `labor:postwar-smalltrade / labor:postwar-mutualaid / labor:postwar-household`，把战后几年如何再接熟工、宿舍、家口与里弄稳饭落回年度层。
    - `战后工账与口粮票旧账`（`1947`）：承接 `labor:postwar-ledger`，把战后几年谁还肯继续认这手字、认这层票据与口信落回年度层。
    - `代写信与记工账余账`（`1949`）：承接 `labor:settle-ledger`，把细活收成饭碗以后仍未结清的票据与托付继续压回。
  - `demo.html` 现已把这组 fired event 接入 HUD `跨年余账` 与终局事实回收，避免动作和年度事件只存在于年终日志里。

## ④ HUD 与终局回收口径
- HUD 现已把 `纱厂女工` 显性拆成六段事实层：`包身/自由身 / 工潮后账 / 战时活法 / 战后几年 / 跨年余账 / 后半生安放`
- 运行时主体层继续补齐：`labor:postwar-ledger / labor:settle-ledger` 现已不只在 `workers / connections` 留痕，而会正式写入 `subjects.ledger`，并参与 `AUX_SUBJECT_TENSION_RULES`。因此女工线的“工账票据 / 代写家信 / 记工账细活”终于能和 `上海工商家` 的账房线放进同一套 `ledger + workers + connections` 模板里比较。
- 本轮再把此前已经存在、但还没进入牵制矩阵的 `labor:postwar-household / labor:settle-household` 晚段也正式接进 `AUX_SUBJECT_TENSION_RULES`：`工友宿舍没有独自把战后几年接稳，家口稳饭与里弄旧路并着重接`、`工友互帮没有独自收住后半生，家口稳饭与里弄熟路一起把日子接住`。这意味着女工线不再只提供 `工友/家口 + 外部门路`、`账房/工友/家口 + 外部门路`，也开始提供 `安身主体/工友/家口 + 外部门路` 这一层真实第二路线证据。
- 为避免这层 mixed 态仍只停在代码里，运行时本轮同步新增 `millworker-household-settle` 与 `natural-millworker-household` 两组代表态/自然试玩，用真实 `labor:postwar-household -> labor:settle-household -> end:mainland` 去锁定 HUD 与终局口径；验证时应直接看到 `战后几年 = 接回家口与里弄稳饭`、`主体牵制 = 工友互帮没有独自收住后半生，家口稳饭与里弄熟路一起把日子接住`、`后半生安放 = 收回家口稳饭`。
- `demo.html` 终局现已回收：`contract / big-strike / own-wage / labor:* / free:* / scab / end:*`
- 只回收以下事实：
  - 你在工潮之后把日子接成了留厂抱团、缩回机台，还是转小厂/零工
  - 战火压厂之后你是跟着手艺换厂做活、先靠互帮接住人、先认票据递信，还是把锋芒收起做零工
  - 战后几年你先把哪一层重新接回手里：熟工门路、工友宿舍、家口稳饭，还是工账票据与字路细活
  - 到 `1948-1949`，你把手艺收成了小工小卖、把工友路数过成饭碗网、把重心收回家口与一口稳饭，还是把代写记账收成细活
- 不把 `纱厂女工` 再压扁成“参加过罢工/后来赎身了”的一句摘要，也不把具体组织细节硬写死

---

# 节点 N-11：苏北贫农家 · `life:into-tide` 从“顺势再走”到 1949 收束
> 目标不是把 `life:into-tide` 继续停在一句“后来又顺着时局往前闯”，而是把这条此前偏薄的尾支补成真正闭环：**复员归乡 → 不再守旧田埂 → 顺势再走 → 顺势站位 → 1949 后半生收束 → 终局事实回收**。本轮不编造具体编制、头衔、地方制度细目，只写运行时已真实落地的“做事实位 / 留边路 / 两头并行”三种活法。

## ① 父快照 P₀
| 字段 | 值 | 说明 |
|---|---|---|
| 家庭 | `subeipoor` | `actions-data.js` 已定义 |
| 主路径 | `soldier-path` | 承接 `draft:soldier` 后的复员归乡线 |
| 上游连续节点 | `soldier-demobilize / soldier-return` | 已实现 |
| 起手回响 | `life:into-tide` | 仅表示“复员后没有停在旧田埂上”，此前尚未拆开多年后果 |
| 未知项 | 具体归属哪一类地方组织、正式头衔、制度身份 | 继续保留 `unknown` |

## ② 本轮新增运行时节点
- `复员之后·顺着时局先走哪一层路`
  - 触发：`hasEcho:life:into-tide + yearFrom:1946`
  - 分化：`soldier:tide-facts / soldier:tide-watch / soldier:tide-twoends`
  - 作用：把“顺势再走”从一句姿态拆成三种中段活法：做事实位、场边留边路、门里门外两头并行
- `几年之后·顺势走的人把自己收在哪一层`
  - 触发：`yearFrom:1948`，承接 `soldier:tide-*`
  - 分化：`soldier:tide-settle-facts / soldier:tide-settle-watch / soldier:tide-settle-twoends`
  - 作用：把 1948 前后的长期结果落下来，而不是只写“还在闯”
- `1949 到来·顺势走的人把后半生收成哪一种活法`
  - 触发：`yearFrom:1949`，承接 `soldier:tide-settle-*`
  - 分化：`soldier:late-tide-facts / soldier:late-tide-watch / soldier:late-tide-twoends`
  - 作用：让这条线也像 `归乡 / 留外 / 逃兵潜躲` 一样，在 1949 前落到可验证的长期结果

## ③ 动作 / 事件下沉
- `actions-data.js` 已新增 3 条 `soldier-path` 专属动作：
  - `替人跑差认旧账`：承接 `life:into-tide / soldier:tide-facts / soldier:tide-twoends`，把“跑差、认人、认旧账”写成年头层。
  - `在场边听风声`：承接 `life:into-tide / soldier:tide-watch / soldier:tide-twoends`，把“留退路、听口风”落成持续日常。
  - `门里门外两头照应`：承接 `soldier:tide-twoends / soldier:tide-settle-twoends`，把“家口与场面两头跑”压回行动层。
- `actions-data.js` 已新增 3 条年度事件：
  - `soldier-tide-remnant (1947)`：承接 `life:into-tide / soldier:tide-*`，把跑差、人情、脚程拖成中段余账。
  - `soldier-tide-settle-remnant (1948)`：承接 `soldier:tide-settle-*`，把“已经站住一层位置”后的余账继续写回年度层。
  - `soldier-tide-late-remnant (1949)`：承接 `soldier:late-tide-*`，让顺势线的晚段余账不只存在于终局句子。

## ④ HUD 与终局回收口径
- HUD 现已把这条线接入 `苏北贫农家` 的 `跨年余账`：
  - 事件优先读取：`soldier-tide-remnant / soldier-tide-settle-remnant / soldier-tide-late-remnant`
  - 回响兜底读取：`life:into-tide / soldier:tide-* / soldier:tide-settle-* / soldier:late-tide-*`
- `demo.html` 终局现已回收：
  - `endingVerdict()`：补入三段中段与三段 `1949` 结果的主叙事句；
  - `buildFinalLedgerFacts()`：补入 `life:into-tide / soldier:tide-* / soldier:tide-settle-* / soldier:late-tide-*` 与对应 `firedEvents`；
  - `currentSubeiLateOutcome()`：从“只有一句 `life:into-tide` 总结”推进到可区分 `顺势后续已落 / 顺势收住后半生`。
- 不把这条线伪装成已经自动并回土地账，也不把它虚构成具体头衔、具体组织履历。

---

## 统一主链覆盖矩阵（2026-08-07 本轮抽取）
> 这一节不新增虚构系统，只把当前 `README.md / assets/choices-data.js / demo.html` 已真实存在的主链闭环重新收成一张总表，便于后续每轮先判断“是否让整局更完整”。当前运行时矩阵已稳定到 **36** 组代表状态、自然回放已稳定到 **57** 条主链：此前已把 `苏北托情旧账 / 苏北病身旧账 / 苏北士兵失联多年确认` 与 `江南沈家 / 上海工商家` 叠加共享海外轨这些复合态纳进统一验收；最近几轮又把两大家族女线 `household / teach / workroom / shelter`、`难民折损确认后返乡守住 / 返乡仍两头并行`、`女工工伤/黑名单多年确认` 一并纳入，而本轮再把 `江南沈家` 女线新增 `letters` 主轨并进矩阵与自然回放，确认这些链路不只“规则成立”，也都能被统一矩阵或真实自然回放看见。

| 主家庭/主路径 | 前提与入口 | 跨年后果 | 时代冲击回应 | 主体回应 | 终局回收 | 当前剩余缺口 |
|---|---|---|---|---|---|---|
| `苏北贫农家` | `C1-C6` 抓丁入口、`rent-negotiate` | `quota:* / bought:* / patronage:* / injury:* / soldier:* / flee:* / land:*` | 抗战、复员、工作队进村、分田、`1949` 去留 | 病母/妻已局部落地，`flee-refugee` 现也能与 `亲族/旧路` 双层并账显示；本轮又把 `买丁 / 托情 / 病身免役` 这三条留乡主分流的 `旧账口风 / 短工去路 / 旧情门路 / 轻手艺门路` 接进同一个 `subeipoor.connections` 主体 | `endingVerdict + buildFinalLedgerFacts` 已双接线 | 仍缺更统一的死亡确认总规则；兵役地方细数继续 `unknown` |
| `soldier-path` | `draft:soldier → 新兵营/会战/后勤/复员` | `soldier:drilled / rear / missing / demob-* / tide-* / settle-* / late-*` | 会战、军中搭伙账、后勤长年、`1945` 复员、`1947` 归乡/留外/顺势走动残账、`1949` 余生安放 | 妻在 `missing` 下已有“等消息/回娘家”独立回应；本轮又把 `军中搭伙 / 旧乡风声 / 铺位差事 / 藏身旧识` 接进同一个 `connections` 主体 | 已回收到终局第一页与 `1949总账` | `life:into-tide` 现已补齐；剩余缺口主要转向主体规则层统一与兵役地方细数 `unknown` |
| `flee-refugee`（苏北逃亡线） | `draft:flee → 病母上不上路` | `road:* / flee:settle-* / flee:return-* / flee:late-*` | 战后探路、工作队进村、`1949` 后半生安放 + `refugee-return-probe-remnant / refugee-return-workteam-remnant / refugee-away-late-remnant / refugee-home-late-remnant` | 病母/妻去留已有局部独立推进，`亲族屋檐 / 捎话旧路 / 旧街坊` 也已接进 `connections` | 已回收到终局第一页与 `1949总账` | 逃亡返乡线已厚；下一步更值得统一的是“病母/妻主体回应 + 外部门路”在苏北并账口径下怎样长期共存，而不是回头重写已闭合节点 |
| `江南沈家·女线` | 抗婚/出走/留门改良 | `woman:self-* / woman:inside-* / shenwoman:prewar-letters` | `woman:war-* / woman:settle-* / shenwoman:war-letters / postwar-letters / settle-letters` + `shen-women-war-remnant / shen-women-postwar-ledger / shen-women-letters-return-remnant / shen-women-settle-letters-remnant` | `father / household / marriage / connections` 已接入 | 已回收到 HUD 与终局，且 HUD 已显性显示 `跨年余账 + 家门回应 + 代笔信札/礼帖细路` | 女线已独立长出 `letters` 新轨；下一步更值得补的是更广义的族亲/旧同事长期规则与更多可反复计划的沈家制度日常 |
| `江南沈家·男线` | 南迁前的课堂/文字/家门分流 | `shen:postwar-*` | `shen:school-south / press-stay / family-escort / settle-*` + `shen-exile-year / shen-return-old-ledger` | `father / household` 已接入 | 已回收到 HUD 与终局，且 HUD 已显性显示 `跨年余账` | 仍偏关键节点闭环，尚未独立成完整新轨 |
| `上海工商家·男线` | `biz-takeover-choice` | `biz:move-inland / stay-shanghai / split-house / postwar-* / gold-* / settle-*` | 战火压厂、金圆券、风暴后收束 + `biz-war-remnant / biz-credit-crunch / biz-coop-return-remnant / biz-gold-aftershock / biz-settle-coop-remnant` | `father / household / ledger / workers / connections` 已接入，且 `熟客小单 / 账房余单 / 旧工友代工` 已能并账显示 | 已回收到 HUD 与终局，且 HUD 已显性显示 `跨年余账` | 现已具备第五条独立后半生主枝 `coop`；下一步更值得补的是女线 `public / household / teach / workroom` 之间更厚的互相牵制日常，而不是回头重修已闭合男线节点 |
| `上海工商家·女线` | 抗婚/出走后进入租界风雨专属段 | `shdaughter:war-* / gold-* / settle-* / workroom-* / shelter-*` | 战火压进上海、金圆券前夜、风暴后安放 + `shdaughter-war-remnant / shdaughter-postwar-ledger / shdaughter-shelter-return-remnant / shdaughter-workroom-return-remnant / shdaughter-gold-remnant / shdaughter-settle-shelter-remnant / shdaughter-settle-workroom-remnant` | `household / father / ledger / workers / connections` 已接入，且 `借住屋檐 / 接应名册 / 安身去路` 与 `互帮小作 / 熟手零单 / 门里手账` 都已能并账显示 | 已回收到 HUD 与终局，且 HUD 已显性显示 `跨年余账` | 已具备 `teach / public / household / workroom / shelter` 五种代表收口；仍与 `newwoman-path` 共用部分上游骨架，后续更值得补的是这五条主收口之间更厚的阶段内互相牵制 |
| `纱厂女工` | 包身/工潮/赎身 | `labor:line-* / blacklist-* / injury-*` | `labor:war-* / labor:settle-*` + `mill-blacklist-remnant / mill-war-remnant / mill-postwar-remnant` | 主要以多年确认层推进，具体人名继续 `unknown` | 已回收到 HUD 与终局，且 HUD 已显性显示 `跨年余账` | 未扩写具体组织人物与厂名，保持 `unknown` |
| `难民/流亡者` | 家园沦陷 | `refugee:road-* / loss-* / settle-* / late-*` | 战后回望、返乡站场、`1949` 安放 + `refugee-road-remnant / refugee-shelter-remnant / refugee-return-probe-remnant / refugee-postwar-remnant / refugee-return-workteam-remnant / refugee-away-late-remnant / refugee-return-home-remnant` | 主要以“折损未明→拖长→确认”推进，但本轮 `refugee:home-twoends -> refugee:late-home-twoends` 也已进入代表验收 | 已回收到 HUD 与终局 | 已开始具备独立 `action + year event` 双骨架，并与 `flee-refugee` 共享守门；主体推进/多年确认更接近统一规则层，剩余重点转向更广义外部门路 profile 与制度边界的 `unknown` 固定 |

## 统一验证口径补充（本轮）
- `demo.html` 现新增 `window.__DEMO.inspectMajorChainDimensionCoverage()`：把文档总表里的 `前提/制度过程态/跨年后果/时代冲击/主体回应/长期结果/终局回收` 七维主链，直接压成运行时矩阵。当前 **10 / 10** 条主路线都已 `fullyCovered`，且 **10 / 10** 都已 `dualLayerCovered`；其中 `时代冲击回应 = 10 / 10`、`制度/过程态 = 10 / 10`，以后判断“这一步是否让整局更完整”时，不必再只靠人工对照长表。
- `demo.html` 现新增 `window.__DEMO.inspectTemplateLayerReuseCoverage()`：不再只说“模板矩阵已接上”，而是直接按主路线桶列出这条线当前已经复用了哪些 `状态字段 / 规则层 / 节点模式 / 验证方式`，并把这四层再反查成共享矩阵。后续若要继续补主家庭/主路径，应先看这张四层矩阵，优先补“整局还缺哪一层模板”，而不是只在某条闭合分支上继续添句。
- `demo.html` 现新增 `window.__DEMO.inspectSubjectRuleSchemaCoverage()`：把 `FAMILY_SUBJECT_RULES / AUX_SUBJECT_RULES / AUX_SUBJECT_TENSION_RULES` 与 `家门主体槽 / 亲缘婚家槽 / 营生工友槽 / 安身承接槽 / 外部门路槽` 一起压成主体规则 schema 矩阵。当前 **10 / 10** 条主路线都已 `schemaReady`，其中 **7 / 10** 已进入“锚点或营生 + 安身承接 + 外部门路”的三槽结构；以后再判断 `江南沈家 / 上海工商家 / 纱厂女工` 的 route-local 前沿时，不必再靠手工猜“是不是整棵树还没迁模板”，而能先看这层矩阵直接区分“共享 schema 已成”与“规则层仍缺槽位/规则表”。
- `demo.html` 现把这四层模板复用结果继续接回 `window.__DEMO.inspectFamilyRouteAtlasCoverage()`：主家庭树与用户点名的主路径不只会返回 `route-local / unknown` 前沿，也会直接列出该行当前最稳定复用的 `topTemplateStateFieldLayers / topTemplateRuleLayers / topTemplateNodePatterns / topTemplateValidationWays`。本轮又把依赖 `共享海外多年轨` 的家庭树进一步拆成 `ownUnknownFrontierSummary / sharedDependencyUnknownFrontierSummary` 两层，专门区分“这棵树自己还缺什么”与“它只是借入了共享路线尚未收回的余账”。后续判断“这一步是否让整个游戏更完整”时，应先看这棵树/这条路径究竟还缺哪一层模板迁移，以及当前的 unknown 到底是自有缺口还是共享路径借入，而不是只看闭环计数。
- `demo.html` 现新增 `window.__DEMO.inspectGlobalMainlineBoard()`：它不再新增另一套验证口径，而是把 `inspectFamilyRouteAtlasCoverage()` 已经稳定返回的 `completionPriority / ownUnknown / sharedDependencyUnknown / route-local frontier / templateLayerSummary` 继续压成一张真正面向整局推进的工作看板。以后每轮若要回答“现在先补哪一整棵树/哪一条主路径最能让整局更完整”，应先看这张看板，而不是重新翻长篇推进记录手工整理优先级。
- `demo.html` 现新增 `window.__DEMO.inspectFamilyPhaseAtlasCoverage()`：它不另造新标准，而是把已经通过 `inspectRouteLoopCoverage()` 的五段闭环，继续压回 `苏北贫农家 / 苏北留乡主分流 / 苏北士兵多年线 / 苏北逃亡回乡线 / 江南沈家 / 上海工商家 / 纱厂女工 / 难民/流亡者 / 共享海外多年轨` 这 **9** 行 atlas，直接列出每一行五段当前分别由哪些真实节点承接。当前 `fullyCoveredRowCount = 9 / 9`、`dualLayerCoveredRowCount = 9 / 9`；这意味着后续再问“这一步是否让整个游戏更完整”时，不必只看 route bucket 是否过线，也可以直接看主家庭/主路径的五段槽位是不是已经由真实节点撑住。
- `demo.html` 现新增 `window.__DEMO.inspectWholeGameClosureAtlas()`：它把 `inspectFamilyPhaseAtlasCoverage()` 的五段槽位、`inspectMajorChainDimensionCoverage()` 的七维闭环、`inspectWholeGameCompletionLedger()` 的模板层与样例证据重新收成同一张 9 行总览。以后若先要回答“这一步是不是让整个游戏更完整”，默认应优先看这张 atlas；因为同一行会同时告诉你 `前提→选择→跨年后果→长期结果→终局回收` 的节点承接、七维双层是否齐、当前有哪些主链/自然样例在支撑，以及这一整行还剩什么整局级缺口，而不用再把五段、七维、模板和样例拆成四张表人工拼。
- `demo.html` 现新增 `window.__DEMO.inspectWholeGameEvidenceDensityMatrix()`：它不新增任何剧情或制度细目，而是把 `inspectWholeGameClosureAtlas()` 已经确认闭环的 9 行，再按 `主链样例 / 自然样例 / 五段槽位样例 / 七维样例` 重新排序，显性区分 `证据较厚 / 证据均衡 / 证据偏薄`。后续若下一轮的判断题是“哪一整行虽然闭环了，但证据仍偏薄、最值得继续加厚”，应先看这张矩阵，再决定是补代表态、补自然回放，还是继续保守保留 `unknown / route-local`。
- `demo.html` 现新增 `window.__DEMO.inspectTensionAnchorArchetypeCoverage()`：在细粒度 `主体牵制原型矩阵` 与更粗的 `主体牵制角色原型矩阵` 之间，再补一层**主体牵制锚点原型矩阵**，专门比较“哪类家门锚点 + 哪簇安身主体 + 外部门路”已经跨路线站稳。当前 `anchorCount = 13`、`crossRoute = 5`、`dualLayer = 13 / 13`、`routeLocal = 8`；这让 `江南沈家 / 上海工商家` 不再只能在“过细主体组合”和“过粗角色分工”之间跳读，而能先看 `父辈/家门 + 工友 + 外部门路`、`婚家 + 安身 + 外部门路` 这类锚点层是否已具备整局模板资格。
- `demo.html` 现新增 `window.__DEMO.inspectTensionFamilyComboDetailCoverage()`：当 `inspectTopRouteLocalCandidateBundle()` 已经把 blocker 收窄到 `shared-family-combo-template-but-local-detail` 时，不再只知道“粗主家庭模板已共享”，还会直接回答这组 `exact 细主体组合` 当前到底是 `已跨路线`，还是 `粗模板已跨路线、细组合仍只在单路线`。后续若继续推进 `江南沈家·女线`、`上海工商家` 或其他主家庭树，应先读这张细模板账板，再决定是核对现成对照位，还是继续如实保留 `route-local`。
- `demo.html` 本轮再把 `window.__DEMO.inspectRouteLocalContrastEvidence()` 的 compare-ready provenance 补全：每条对照路线现在会显式返回 `comparisonBasisLabel / subjectDeltaSummary / matchedCandidateMissingCount / compareRouteGapSummary`。若当前只命中 `共享主家庭比较模板` 或 `共享锚点门路骨架`、却没有命中同类 `candidate` 细签名，就必须把这层差额直接写成“粗模板可比，但第二路线细签名仍不存在”；不得把它误记成“第二路线已经成立，只是比较器没读到”。
- `demo.html` 现新增 `window.__DEMO.inspectRouteLocalPromotionSignals()`：在既有 `window.__DEMO.inspectRouteLocalTemplateCandidates()` 基础上，再把 `主体牵制原型矩阵 / 主体牵制锚点原型矩阵 / 终局收束原型矩阵 / shared unknown 跨组比较器` 叠进同一张**升格信号矩阵**，显性区分“候选很多”与“已经挂上跨路线锚点、值得优先比较是否升成下一组整局模板”。当前 `anchored = 10 / 10`、`sharedSourceAnchor = 5`、`compareOnlyAnchor = 0`、`multiCategoryAnchored = 1`；因此后续若继续补整局模板，应先比较这些已挂锚点的主路线里，哪类 route-local 资产开始出现跨线复用，而不是只按单路线候选数量排优先级。
- `demo.html` 现把 `window.__DEMO.inspectTensionAnchorArchetypeCoverage()` 与 `window.__DEMO.inspectTensionResponseArchetypeCoverage()` 一并接回 `window.__DEMO.inspectGlobalCompleteness()`：整局总表不再只汇总 `主体牵制原型 / 主体牵制角色原型`，也会显性返回 `tensionAnchorArchetypeCoverage / tensionResponseArchetypeCoverage`，并把“已有 5 组主体牵制锚点原型跨路线复用”直接写进 `recommendedNextFocus`。这样以后判断“这一轮是否让整局更完整”时，不会再出现“route-local 前沿已补到家门锚点层，但整局总览仍漏看这一层”的口径裂口。
- `demo.html` 本轮再把“最后一格是否已该停止反复修边”也接成统一口径：`window.__DEMO.inspectTopRouteLocalCandidateBundle()` 现会显式返回 `tailStabilityLabel / tailStabilitySummary / tailFreezeSuggested`，`window.__DEMO.inspectWholeGameCompletionLedger()` 与 `window.__DEMO.inspectWholeGameProgressBundle()` 也会同步把这格写成“可固定为保守尾项”或“仍需继续保守核对”。后续若 `江南沈家·女线：婚家 + 外部门路` 继续保持 `matchedCandidateMissingCount > 0` 且 `familyComboPeerRouteCount = 0`，默认做法就不再是继续翻成熟库存，而是把它固定为“当前无第二路线证据”的 route-local 尾项；同时 `inspectTemplateLayerReuseCoverage()`、`inspectFamilyRouteAtlasCoverage()`、`inspectWholeGameMainlineBlueprint()`、`inspectWholeGameTemplateMigrationBoard()` 不应再把这格继续计作 `active route-local`，而应改记为 `保守尾项`。
- `demo.html` 现把 `inspectWholeGameClosureAtlas()` 继续接回 `window.__DEMO.inspectWholeGameProgressBundle()`：整局汇总除 `模板迁移前沿 / 主链蓝图前沿 / 整局总账前沿 / 尾部 route-local` 外，还会显性给出 `closureAtlasReadyRowCount` 与当前 atlas 首行摘要。这样后续每轮先读整局汇总时，就能立刻判断“现在是某一行的五段/七维还没齐，还是只是尾部 route-local 还需守口”，不再只靠 completion ledger 单表推断。
- `demo.html` 现把 `inspectWholeGameEvidenceDensityMatrix()` 也继续接回 `window.__DEMO.inspectWholeGameProgressBundle()`：整局汇总现在不只会说“哪一行仍是模板迁移/主链蓝图/总账前沿”，还会继续显性返回当前 `证据偏薄` 的首行与摘要。这样后续每轮先读整局汇总时，就能同时判断“哪一行还没闭环”与“哪一行虽已闭环，但样例支撑仍偏薄”，避免又凭直觉回到成熟库存附近修边。
- `demo.html` 现把 `金圆券风暴保全边界` 再往前推进成四层共享矩阵：`window.__DEMO.inspectGoldPreservationKindCoverage()` 统一汇总七种已进代表态的保全姿态，`window.__DEMO.inspectGoldPreservationTierCoverage()` 再把同一批条目反查成 `退路暗账层 / 外壳苦撑层 / 口粮安身重摊层`，`window.__DEMO.inspectGoldPreservationRecordedByCoverage()` 继续把同一批 `payload.goldPreservationBoundaries[]` 反查成 `顾家家门与账房在风暴里的口风 / 金圆券后残账与工友安置留下的后账 / 顾家家门在风暴里的口风 / 女工家口与安身网络在风暴里留下的后账` 四类字面证据来源，而 `window.__DEMO.inspectGoldPreservationSourceArchetypeCoverage()` 再把这四类来源抽成 `顾家家门口风 / 风暴后家口工友安身后账` 两类共享来源原型。当前 `kindCount = 7 / 7`、`tierCount = 3 / 3`、`literalSourceCount = 4 / 4`、`sourceArchetypeCount = 2 / 2` 都已被主链矩阵 + 自然回放双层锁住；仍继续保留 `unknown` 的只是“哪些资产/门路最终真保住、怎样折着算”的证据缺口，而不是 `gold-hide` 或来源层本身还缺统一代表态。
- `demo.html` 现把 `地方兵役参数边界` 也继续推进成“字面来源 + 来源原型”双层矩阵：在既有 `window.__DEMO.inspectDraftInstitutionBoundaryCoverage()`、`window.__DEMO.inspectDraftInstitutionRouteCoverage()`、`window.__DEMO.inspectDraftInstitutionTierCoverage()` 与 `window.__DEMO.inspectDraftInstitutionRecordedByCoverage()` 之外，再新增 `window.__DEMO.inspectDraftInstitutionSourceArchetypeCoverage()`，把 `村里传到门口的告示与口风 / 保甲点验桌前后账 / 一家离乡前仓促应对后账` 继续抽成 `村里门口口风 / 抓丁临门后的应对后账` 两类共享来源原型。当前 `literalSourceCount = 3 / 3`、`sourceArchetypeCount = 2 / 2` 都已被主链矩阵 + 自然回放双层锁住；仍继续保留 `unknown` 的只是“本地抽丁比例 / 壮丁费或买丁礼价 / 保甲摊派与免役认定口径”的证据缺口，而不是来源层本身还缺统一代表态。
- `demo.html` 本轮继续把 `workteam-recognition-boundary` 从“三层已可比较”推进成真正的**四层折算矩阵**：在既有 `window.__DEMO.inspectWorkteamRecognitionRouteCoverage()`、`window.__DEMO.inspectWorkteamRecognitionRecordedByCoverage()`、`window.__DEMO.inspectWorkteamRecognitionSourceArchetypeCoverage()` 之外，把 `留余退身层` 的主链样板与自然回放样板一并补入 `苏北留乡主分流 / 苏北士兵多年线`。当前 `window.__DEMO.inspectWorkteamRecognitionTierCoverage()` 已达到 `tierCount = 4`、`sharedTierCount = 3`、`dualLayerCoveredTierCount = 4 / 4`；后续再判断这组 shared unknown 是否值得继续升层时，应先看“四层都已真实进表”这个前提，而不是只因某一层以前没代表态就回去细修单一路线。
- `demo.html` 现把 `地方兵役参数边界` 再往前推进成“字面层级 + 回应原型”双层回应矩阵：在既有 `window.__DEMO.inspectDraftInstitutionTierCoverage()` 之外，再新增 `window.__DEMO.inspectDraftInstitutionResponseArchetypeCoverage()`，把 `缓过一回 / 周旋未稳` 保守收成 `留在门口周旋型`，把 `被带走 / 离乡避征` 保守收成 `被逼离场改线型`。这层不改写现有 route-local 层级，只回答“同一拨抓丁压力什么时候仍停在门口周旋，什么时候已经把人生从原路上逼开”；当前 `archetypeCount = 2 / 2`、`sharedArchetypeCount = 1`、`dualLayer = 2 / 2`，说明这组回应层级已不再只是“四层都存在但互不共享”，而开始具备更高一层的整局比较模板。
- `demo.html` 现把 `名额转嫁承接边界` 再往前推进成第三层共享矩阵：在既有 `window.__DEMO.inspectQuotaTransferBranchCoverage()` 与 `window.__DEMO.inspectQuotaTransferTierCoverage()` 之外，再新增 `window.__DEMO.inspectQuotaTransferRecordedByCoverage()`，把同一批 `payload.quotaTransfers[]` 反查成 `村里名册 / 村里口风 / 保长差事` 三类证据来源。当前 `sourceCount = 3 / 3`，其中 `sharedSourceCount = 1` 已跨多条留乡分流稳定复用；这说明这组 shared unknown 现在不只知道“落在哪条分流、停在第几次转嫁”，也能回答“究竟靠哪类来源被系统看见”。
- `demo.html` 现把 `名额转嫁承接边界` 继续补齐到“字面来源 + 来源原型”双层来源模板：新增 `window.__DEMO.inspectQuotaTransferSourceArchetypeCoverage()`，把 `村里名册 / 村里口风 / 保长差事` 保守抽成 `村里名目与口风 / 保长差事后账` 两类共享来源原型，并同样在主链矩阵 + 自然回放双层锁住；当前 `sourceCount = 2 / 2`、`sharedSourceCount = 1`、`dualLayer = 2 / 2`。这层只抽结构，不把“来源原型可共模”误写成“每一笔名额转嫁都靠同一种字面来源被看见”。
- `demo.html` 现把 `返乡认地折算边界` 也继续推进成第三层共享矩阵：在既有 `window.__DEMO.inspectWorkteamRecognitionBoundaryCoverage()`、`window.__DEMO.inspectWorkteamRecognitionRouteCoverage()` 与 `window.__DEMO.inspectWorkteamRecognitionTierCoverage()` 之外，再新增 `window.__DEMO.inspectWorkteamRecognitionRecordedByCoverage()`，把同一批 `payload.workteamRecognitionBoundaries[]` 反查成 `工作队进村后的丈量/认人口风 / 家门旧账重新摆上桌后的后账 / 丈量场边重新认人的口风 / 旧街坊与回乡路上续回的消息 / 归乡以后家口与旧地重新接线的后账` 五类证据来源。当前 `sourceCount = 5 / 5`、`sharedSourceCount = 2`，说明这组跨 **4** 条返乡主路线桶复用的制度边界，已经不只知道“站在哪层认账姿态”，也能回答“究竟靠哪类来源被系统看见”。
- `demo.html` 现再新增 `window.__DEMO.inspectMajorSystemClosureCoverage()`：把 `地方兵役参数边界 / 返乡认地折算边界 / 金圆券风暴保全边界 / 名额转嫁承接边界 / 名额后账回应边界 / 多年确认边界` 六组主系统统一压成一张总表，逐项比较它们是否已经具备 `字段证据层 / 主路线层 / 层级或姿态层 / 字面来源层 / 来源原型层 / 安身承接层` 这些闭环层。以后主路线已经 10 / 10 闭环时，应先看这张系统总表再决定下一步，而不是退回单枝修边。
- `demo.html` 的 HUD 验证口径现进一步统一为：主线不仅要显示“过程态/时代冲击”，也要显式显示“主体回应 / 后半生安放”。本轮已把 `苏北贫农家` 从原先四项事实层补到六项：`抓丁过程态 / 佃权地缘 / 名额压力 / 主体回应 / 觉醒土改路径 / 后半生安放`，与 `江南沈家 / 上海工商家` 的验证口径更接近。
- `纱厂女工` 这轮也被补到同类验证口径：除 `包身/自由身 / 工潮后账 / 战时活法 / 后半生安放` 之外，HUD 新增 `跨年余账`，只读取已真实落地的 `mill-blacklist-remnant / mill-war-remnant / mill-postwar-remnant` 与 `labor:blacklist-* / labor:injury-*`，不补写不存在的组织细节或具体事故真相。
- 对 `江南沈家 / 上海工商家`，本轮新增统一口径：HUD 里必须把 `跨年余账` 单独显性列出，而不是只把它留到 `buildFinalLedgerFacts()`。当前只读取已真实落地的年度事件与已存在 echoes 的保守回退：
  - `江南沈家女线`：`shen-women-war-remnant / shen-women-postwar-ledger / shen-women-letters-return-remnant / shen-women-settle-letters-remnant`
  - `江南沈家男线`：`shen-exile-year / shen-return-old-ledger`
  - `上海工商家男线`：`biz-war-remnant / biz-credit-crunch / biz-gold-aftershock`
  - `上海工商家女线`：`shdaughter-war-remnant / shdaughter-postwar-ledger / shdaughter-shelter-return-remnant / shdaughter-workroom-return-remnant / shdaughter-gold-remnant / shdaughter-settle-shelter-remnant / shdaughter-settle-workroom-remnant`
- 若只是注入了晚段 echo、尚未真实命中对应 `firedEvents`，HUD 允许回退显示“战时余账仍在拖长 / 战后旧账开始压回 / 风暴后残账待摊”这类保守阶段句，不补写不存在的细节账目。
- 对 `苏北贫农家`，`主体回应` 当前不再只读取病母/妻状态，也会按 `subeipoor.connections` 读取已真实落地的外部门路事实：除 `军中搭伙 / 旧乡风声 / 铺位差事 / 藏身旧识` 这些从军分化外，本轮又补入 `名额后账 / 村里口风 / 旧账口风 / 短工去路 / 旧情门路 / 村里风声 / 轻手艺门路 / 零碎活路`。没有对应运行时状态的，一律仍写 `未显性写出`，不补写不存在的心理或制度结论。
- 对 `苏北贫农家` 的 `flee-refugee` 分支，当前运行时已把这条口径收成权威并账函数：HUD 会固定保留一条来自 `subeipoor` profile 的 `主体回应`，再按 `refugee` profile 额外显性显示一条 `亲族/旧路`。只读取已真实落地的 `flee:settle-* / flee:return-* / flee:team-* / flee:late-*` 回响所对应的 `connections` 判读，不把“有人情接应”夸大成稳定保底，也不覆盖病母/妻的独立主体性。
- 对 `苏北贫农家`，`后半生安放` 当前只读取已真实存在的晚段回响：`soldier:late-* / flee:late-* / land:settle-* / bought:settle-* / patronage:state-* / injury:settle-* / end:*`。若只走到战后接日子、还没进入明确晚段，则保守显示带路径向的阶段句，如 `归乡还在接日子 / 在外还在接日子 / 顺势还在接日子 / 离队后还在接日子 / 返乡还在接日子 / 后方还在接日子`，或当前 `1949` 去留；不要把“已留在大陆”自动写成“已把后半辈子完全走实”。
- 对 `refugee / flee-refugee` 共享线，本轮新增的 `refugee-away-late-remnant / refugee-home-late-remnant` 只负责把 `1949` 的晚段安身继续压回年头余账；它们不补写额外制度细节，也不覆盖既有 `late-*` 结果态，只补“安身以后仍要继续认账”的那一层现实。
- `demo.html` 的验证接口现补成统一快照口径：`window.__DEMO.inspect()` / `previewEnding()` 都应能直接返回 `hudFacts / ledgerFacts / firedEvents / subjects / echoes`，这样同一条主线就能用一套脚本同时断言“是否真实触发年度事件、HUD 是否真显示、总账是否真回收”，避免只靠读源码猜接线。
- `demo.html` 现进一步补出 `window.__DEMO.inspectMainlineCoverage()`：会按统一规则批量返回 `total / passed / failed / allPassed / results`，每条 `result` 都对应一组主链代表状态与其 HUD/总账断言。当前 **36** 组代表状态除原有 `苏北买丁 / 从军 / 逃亡、沈家男女线、工商家男女线、纱厂女工、难民返乡、共享海外轨` 外，近几轮又补进 `苏北托情旧账 / 苏北病身旧账 / 苏北士兵失联多年确认`、两大家族女线 `household / teach / workroom / shelter`、本轮新增 `江南沈家女线 letters`、`难民折损确认后返乡守住 / 返乡仍两头并行`、`女工工伤多年后小工自立 / 女工黑名单多年后小工自立`。它的目标不是替代细查，而是先回答“这一轮改动后，整局主链有没有倒退、有没有哪条主路径被落下”，尤其是那些规则层已经成立、但若没有矩阵断言就容易被后续改动悄悄压扁的确认链与主体回应链。
- `demo.html` 现在同时新增 `window.__DEMO.inspectNaturalPlaythroughs()`：用真实年份推进脚本回放代表性自然路径，并批量断言 HUD/总账事实是否仍能落地。当前自然回放已扩到 **57 条脚本全部通过**：除苏北、沈家、工商家、女工、难民与共享海外轨的既有代表线外，近几轮又补进 `natural-shen-woman-household / natural-shen-woman-teach / natural-shen-woman-letters / natural-shdaughter-household / natural-shdaughter-teach / natural-millworker-blacklist-settle / natural-refugee-home-loss-confirmed` 等关键闭环，使 `household / teach / letters` 与“多年确认后如何安放余生”都不再只停在矩阵层。继续保留前述方法论：后续补其他主家庭/主路径时，不要只补快照矩阵，也要逐步把自然闭环脚本补齐；若遇到断口，应先判断是规则层真缺桥，还是脚本在强行要求一条并不贴近当前底子的活法，再决定该修 gate 还是该改自然脚本。
- 后续凡新增主树或主路径，至少要同时满足四项验证：`HUD 事实层可见`、`endingVerdict()` 已接线、`buildFinalLedgerFacts()` 已接线、`目标性脚本验证能读到代表性事实句`；否则只能算“结构稿已补”，不能算“主链已闭环”。
