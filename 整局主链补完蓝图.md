# 整局主链补完蓝图

> 这份蓝图不是再写一遍长剧情，而是把“苏北贫农家样板已经抽出了什么”“九行主家庭树/主路径现在各处于什么角色”“下一轮什么动作才算真的让整局更完整”收成同一份执行文档。
>
> 口径约束：
> - 只基于当前项目内已真实存在的 `README.md`、`设计规范-分支节点.md`、`主链闭环模板.md`、`整局主链系统看板.md`、`整局剩余主链路总表.md`、`持续推进记录.md` 与现有运行时字段/路线命名整理。
> - 不补写不存在的剧情、制度细数、接口或内部状态。
> - 任何当前项目里尚未被真实运行时或文档锁定的细节，一律继续保留 `unknown`。

## 使用顺序

1. 先看 `window.__DEMO.inspectWholeGameProgressBundle()`，确认当前是不是还在补整局，而不是只在修单枝。
2. 若当前环境暂时跑不动重聚合，先看 `window.__DEMO.inspectWholeGameScenarioDeck()`，确认 10 条主路线桶各自已有多少 `主链代表态 / 自然回放`，先回答“这一步有没有补到双层样例层”。
3. 再看 `window.__DEMO.inspectWholeGameDesignPlaybook()`，决定现在该补哪一整行。
4. 若当前问题已经缩到“最后一格为什么仍只到 compare-only”，先读 `window.__DEMO.inspectTopRouteLocalCandidateBundle()`，再读 `window.__DEMO.inspectTopRouteLocalCompareGapBoard()`；前者回答“卡在哪一格”，后者回答“这几条对照位到底差哪层细签名”。
5. 只有当作战板明确指向某一整行时，才下钻 `主链闭环模板.md`、对应路线节点、或 `route-local` 单表。
6. 若没有新增第二路线、第二类共享来源、代表态或自然回放双层样例，就继续保留 `route-local / unknown`，不要为了整齐感补新枝。

## 苏北样板抽取

### 状态字段

| 层 | 当前已复用字段 | 用法 |
| --- | --- | --- |
| 基础快照 | `family / track / origin / year / age / gender` | 明确“出身家庭、当前主轨、借用 origin”三层不是一回事 |
| 人物能力与资源 | `attrs.*`、`res.*` | 同一套门槛、代价、长期损耗规则供所有家庭/路径复用 |
| 过程记忆 | `echoes[]`、`firedEvents[]` | 负责跨年后果、战时回应、后半生收束的守门与回收 |
| 主体槽位 | `subjects.mother / wife / father / household / marriage / ledger / workers / support / connections` | 把家内主体、安身主体、营生主体、外部门路拆开，不再只靠主角自述 |
| 派生 HUD 事实 | `draftStatus / tenantRight / quotaPressure / landStage` | 苏北样板先落地，其他主树借同类“过程态 HUD”思路，而不是只写终局 |
| 结构化边界 | `quotaTransfers[] / quotaRippleResponses[] / institutionDetails[] / workteamRecognitionBoundaries[] / longConfirmations[] / longConfirmFollowups[] / supportLongConfirmPlacements[] / returnRouteLandings[]` | 把制度边界、多年确认、回乡落点等缺口做成字段层，而不是藏在句子里 |

### 规则层

- `入口分层`：`family -> track -> origin` 分离，出生家庭不等于人生路径，路径也不等于行动表来源。
- `家庭优先`：只要显式写入 `family`，HUD、终局、主体规则就先服从家庭，不回落到默认 `origin`。
- `主体独立回应`：病母、妻子、婚家、家门、账房、工友家口、安身网络、外部门路都按各自规则推进，不由玩家动作自动代签。
- `多主体并账`：同一路线允许同时保留多层主体事实，不能再压成一个辅助主体槽。
- `主体牵制`：当多主体同场时，必须显性回答“谁先让位、谁先把日子接住”；没有第二路线或第二来源时继续保留 `route-local`。
- `多年确认`：统一按“发生 → 多年无准信 → 确认 → 后续去向/安身承接”推进，不把确认后的真相补成伪确定。
- `制度边界结构化`：兵役、名额、金圆券、返乡认地等统一先做字段层、路线层、来源层，再决定是否升成共享模板。

### 节点模式

| 宏观闭环 | 当前工程化七段 | 作用 |
| --- | --- | --- |
| 前提 | `前提/入口抉择` | 回答“这条命为什么会从这里起手” |
| 选择 | `入口抉择 / 同年中继` | 把姿态与现实结果拆开，不让选择直接跳成命运 |
| 跨年后果 | `跨年活法 / 战时回应 / 战后几年中继` | 把几年后的真实活法显性写出来 |
| 长期结果 | `风声再紧或制度冲击 / 1949 去留或后半生安放` | 回答“后半生最后收在哪一层” |
| 终局事实回收 | `endingVerdict / buildFinalLedgerFacts` | 只回收事实链，不做价值排名 |

### 验证方式

- `整局先读`：`window.__DEMO.inspectWholeGameProgressBundle()`
- `逐行决策`：`window.__DEMO.inspectWholeGameDesignPlaybook()`
- `主链代表态`：`window.__DEMO.inspectMainlineCoverage()`
- `自然闭环`：`window.__DEMO.inspectNaturalPlaythroughs()`
- `五段/七维`：`window.__DEMO.inspectFamilyPhaseAtlasCoverage()`、`window.__DEMO.inspectMajorChainDimensionCoverage()`
- `主系统闭环`：`window.__DEMO.inspectMajorSystemClosureCoverage()`
- `尾部保守判断`：`window.__DEMO.inspectTopRouteLocalCandidateBundle()`

## 九行主链矩阵

| 行 | 当前角色 | 现有完整闭环骨架 | 已迁入的模板重点 | 下一轮只允许的动作 |
| --- | --- | --- | --- | --- |
| `苏北贫农家·主家庭树` | 整局模板母树 | 抓丁起手 → 留乡/从军/逃亡分流 → `1946-1949` 制度冲击 → 终局总账回收 | `draftStatus / tenantRight / quotaPressure / landStage`、`mother / wife / support / connections`、结构化制度边界 | 只继续作为模板母树供别行借用，不回头修已闭合单枝 |
| `苏北贫农家·留乡主分流` | 模板库存来源位 | 抓丁缓过 → 二次摊派/名额后账 → 工作队进村/分田后账 → `1949` 事实回收 | `quotaTransfers[] / quotaRippleResponses[] / institutionDetails[] / workteamRecognitionBoundaries[]` | 只在别行需要制度模板时借用，不再单独扩写新剧情 |
| `苏北贫农家·士兵多年线` | 对照库存位（`compare-ready`） | 被带走从军 → 军中/复员/留外/顺势/潜躲 → 多年确认或归乡再接 → `1949` 回收 | `longConfirmations[] / longConfirmFollowups[] / supportLongConfirmPlacements[] / returnRouteLandings[]`、`support + connections` | 只在需要第二路线或第二来源时补代表态/自然样例，不再长同类晚段 mixed |
| `苏北贫农家·逃亡回乡线` | 共享晚段样板 | 离乡避征/逃荒 → 后方落脚 → 回乡试探旧屋旧账 → `1949` 回收 | `support + connections` 双 profile 并账、`returnRouteLandings[]`、返乡认地边界 | 只继续作为回乡/返乡 shared 模板位，不并回留乡主干 |
| `江南沈家·主家庭树` | 保守尾项所在主树 | 男线：前战家学/家门责任 → 战时南迁/留守 → 战后归城 → 风声收束 → `1949`；女线：婚约/抗婚 → 战时回应 → 战后 years → 后半生安放 → `1949` | `marriage / support / connections / records / letters`、家门与外部门路模板、shared overseas 借入 | 只有出现新的真实第二路线、第二来源或明确的证据偏薄行时，才补代表态/自然样例；否则继续保留 `婚家 + 外部门路` 为保守尾项 |
| `上海工商家·主家庭树` | 对照库存位（`compare-ready`） | 男线：接班 → 战时拆家业 → 战后残局 → 金圆券风暴 → `1949`；女线：前战门路训练 → 战时租界去留 → 战后 years → 金圆券前夜/后半生 → `1949` | `ledger / workers / support / connections`、金圆券边界、女线 `workroom / shelter` 多年轨 | 只做现成对照库存或补会改变整局支撑强度的样例，不再扩同类城市 years mixed |
| `纱厂女工·主路径` | 城市路径证据位 | 包身起手 → 工潮/战时活法 → 战后几年中继 → 后半生安放 → `1949` | `workers + support + connections + ledger`、多年确认边界、城市劳动线牵制模板 | 只在被作战板列为证据偏薄时补代表态/自然样例，不回头细修措辞 |
| `难民/流亡者·主路径` | 共享回应原型来源位 | 家园沦陷 → 路上处境 → 后方落脚 → 回望/返乡两头活路 → `1949` | `support + connections`、多年确认来源模板、返乡落点模板 | 继续作为 shared response/source 库存位；没有新第二来源前不再单独扩 late-line |
| `共享海外多年轨` | 共享借入路径 | 出海/隔海维系 → 战后两头认账 → 回探旧路 → 后路收束 → `1949` | `connections + support`、shared overseas family profile、回身旧路落点模板 | 只为 `江南沈家 / 上海工商家` 提供借入模板与第二来源，不单独争当前前排 |

## 当前整局剩余工作

### 还需要继续做的

1. 用 `inspectWholeGameDesignPlaybook()` 判断九行里哪一行现在最值得推进。
2. 若某一行被判为 `证据偏薄`，优先补它的 `主链代表态`、`自然回放` 或 `系统层双层样例`。
3. 若某一行只是 `对照库存位` 或 `共享来源位`，优先拿它当第二路线/第二来源，不要把库存误写成新主战场。
4. 若某一格已经满足 `tailFreezeSuggested = true`，就固定为保守尾项，等待未来新证据，而不是继续围着它打磨。

### 现在不该做的

- 不回头细修 `苏北贫农家` 已闭合节点。
- 不为了对称感给 `江南沈家 / 上海工商家` 新编没有第二路线证据的 marriage-only 或 city-only 晚段。
- 不把 `unknown` 擅自补成看起来更完整的具体资产、具体人名、具体制度细数。
- 不把“能比较”误写成“已经跨路线共享”；尤其是 `compare-only`、`shared skeleton` 与 `exact second route` 必须分开。

## 下一轮默认提问

- 这一步补的是哪一整行？
- 补的是 `代表态`、`自然回放`、`系统层双层样例`，还是只是多写了几句？
- 有没有新增第二路线、第二来源、或四层模板迁移？
- 如果没有，为什么不继续保留 `route-local / unknown`？
