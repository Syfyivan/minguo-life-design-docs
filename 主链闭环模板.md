# 主链闭环模板（从“苏北贫农家”样板抽取）

> 本文件用于“持续做完整个游戏设计”时的复用：新增/补全任意家庭树、主路径、制度线、时代冲击线时，都尽量按同一口径回答：`前提 → 选择 → 跨年后果 → 长期结果 → 终局事实回收`。
>
> 说明：
> - 本模板只抽取当前项目 `actions-data.js / choices-data.js / demo.html` **已经真实共用**的做法，不补写不存在的系统。
> - 涉及史实细数、制度条文、具体人名/地名等，只要运行时证据不足，一律显式保留 `unknown`，禁止编造。
> - 从本轮起，先看 `window.__DEMO.inspectGlobalClosureChecklist()`：它会先把 9 行主家庭/主路径按 `五段闭环 / 七维主链 / 模板迁移 / 独立轨` 压成轻量总表，并继续返回 `wholeGameStageLabel / wholeGameStageSummary / remainingMainlineLabels / nextWholeGameQuestion`，避免每次一上来就钻进最重的 atlas，结果仍无法回答“这一步是否让整个游戏更完整”。
> - 若当前只是要快速判断“这一步是否真的让整个游戏更完整”，优先先看 `window.__DEMO.inspectWholeGameProgressBundle()`：它会把 `整局阶段 / 九行模板迁移分布 / 九行主链蓝图分布 / 整局完工账本硬指标 / 尾部 route-local` 压成同一份共享预计算结果，再决定要往哪张细表下钻。
> - 从本轮起，这份模板不再只靠静态文档维护；`window.__DEMO.inspectTemplateLayerReuseCoverage()` 会把每条主路线已经复用到的 `状态字段 / 规则层 / 节点模式 / 验证方式` 四层直接列出来，方便判断“这一步是在补整局模板，还是只在磨单枝”。
> - 若已经确认 `wholeGameStageLabel = 整局模板收口阶段`，再直接看 `window.__DEMO.inspectWholeGameTemplateLedger()`：它会把整局阶段、四层模板复用矩阵与当前 route-local 前沿并成一份按主路线直读的台账，直接回答“这条主路线现在是 active route-local、模板库存，还是成熟样板；它还缺哪一层，下一步该问什么”。
> - 若当前先要回答“这一步是否让整个游戏更完整”，优先看 `window.__DEMO.inspectWholeGameMainlineBlueprint()`：它按 9 行主家庭树/主路径直接并出 `当前角色 / 五段骨架 / 模板四层 / unknown 焦点 / 下一问`，先判断整棵树或整条主路径的闭环语义，再决定要不要继续钻 route-local 候选。
> - 若当前先要把“苏北样板抽取 + 九行主链当前角色 + 下一轮允许动作”先收成一份静态执行口径，再看 `整局主链补完蓝图.md`：它不新增任何运行时结论，只把现有模板抽取、九行矩阵与“现在不该做什么”集中到同一处，方便在正式下钻运行时细表前先排除单枝修边冲动。
> - 若当前先要回答“苏北样板迁到了哪一行、下一步该补哪一行更值”，再看 `window.__DEMO.inspectWholeGameDesignPlaybook()`：它会把 `inspectGlobalClosureChecklist()`、`inspectFamilyRouteAtlasCoverage()`、`inspectWholeGameProgressPlan()`、`inspectWholeGameEvidenceDensityMatrix()` 与 `inspectFamilyIndependentTrackCoverage()` 并成一张九行作战板，逐行返回 `当前角色 / 四层模板 / 证据密度 / 独立轨 / 下一步`，直接把“整局是否更完整”翻成可执行的逐行判断。
> - 若当前环境跑不动重聚合，但又需要先判断“这一轮有没有补到主路线样例分布”，先看 `window.__DEMO.inspectWholeGameScenarioDeck()`：它只统计现有 `主链代表态 / 自然回放` 样例账，不重算终局事实或 route-local 排序，适合先回答“哪一条主路线样例更薄、这一步是否至少补到了双层样例层”。
> - 若当前先要回答“苏北样板迁到九行主链的哪一步了”，再看 `window.__DEMO.inspectWholeGameTemplateMigrationBoard()`：它直接按 9 行主家庭树/主路径，汇总每一行当前已经稳定迁入的 `状态字段 / 规则层 / 节点模式 / 验证方式`，并继续区分 `四桶待补 / 四桶已迁入，但仍有行内分布差 / 四桶共享稳定`，优先把“整局模板迁移余账”与“单条 route-local 余账”拆开。
> - `window.__DEMO.inspectWholeGameCompletionLedger()` 现默认只用 `candidatePriority + blockers` 轻量推导尾部 `route-local`，优先保证九行总账本身稳定可读；只有确实需要候选级 `同键/同骨架/compare-only` 证据阶段时，才继续显式下钻 `window.__DEMO.inspectTopRouteLocalCandidateBundle()`。
> - 从本轮起，`window.__DEMO.inspectTopRouteLocalCandidateBundle()` 还会继续返回 `tailStabilityLabel / tailStabilitySummary / tailFreezeSuggested`。当 `matchedCandidateMissingCount > 0`、`familyComboPeerRouteCount = 0` 且现有对照位只到共享主家庭/锚点骨架时，就应把这格登记为“当前无第二路线证据”的**保守尾项**，停止围着成熟库存反复修边；只有未来真出现新的 peer route，再重开判断。
> - 若当前已经知道“哪一格是最前候选”，但还要继续回答“为什么它仍只到 compare-only、具体缺哪层细签名”，优先看 `window.__DEMO.inspectTopRouteLocalCompareGapBoard()`：它默认复用 `inspectTopRouteLocalCandidateBundle()` 已算出的当前候选，只把前几条真实对照位压成 `comparisonStage / comparisonBasisLabel / subjectDeltaSummary / sharedHudKeys / example ids` 的轻量清单，避免为了读差额再重跑整批 candidate。
> - 本轮对 `inspectWholeGameTemplateLedger()` 与 `demo-regression.mjs` 的 smoke 入口继续做了“同一批聚合结果只算一次”的去重：`inspectTemplateLayerReuseCoverage()` 里已经产出的 `route-local template candidates / promotion signals / category comparison / candidate priority / candidate blockers`，现在会沿预计算结果复用，避免“先算四层模板，再算整局台账”时把同一组 route-local 聚合链整段重跑。当前语法层已通过，但轻量 smoke 仍未在合理时间内稳定返回，因此真实运行结果继续保守记 `unknown`；后续若继续压轻运行时，应优先沿这条预计算链再拆。
> - 当主路线本身已经 10 / 10 闭环后，还要继续看 `window.__DEMO.inspectMajorSystemClosureCoverage()`：它把 `地方兵役 / 返乡认地 / 金圆券 / 名额转嫁 / 名额后账 / 多年确认` 六组主系统压成一张总表；目前六组都已闭环，而 `多年确认` 也已补进来源比较层，因此后续更容易直接判断“这一步是在补整局系统，还是只在磨单枝”。
> - 当前真实默认轻量直读结果已推进到：`inspectTemplateLayerReuseCoverage() = 10 / 10`、`inspectMajorChainDimensionCoverage().fullyCovered = 10 / 10`、`inspectFamilyPhaseAtlasCoverage().fullyCovered = 9 / 9`、`inspectFamilyPhaseAtlasCoverage().dualLayerCovered = 9 / 9`、`inspectGlobalClosureChecklist().closureReady = 9 / 9`。闭环清单现在会把这一阶段显性写成 `wholeGameStageLabel = 整局模板收口阶段`，并把 `remainingMainlineLabels` 收成空数组，提醒后续真正的问题已不再是“主树/主路径本身缺闭环”，而是“route-local 候选到底只是有样例，还是已经拿到足够稳的第二路线骨架”。
> - 现在还要继续看 `window.__DEMO.inspectFamilyRouteAtlasCoverage()`：它把 `苏北贫农家 / 苏北留乡主分流 / 苏北士兵多年线 / 苏北逃亡回乡线 / 江南沈家 / 上海工商家 / 纱厂女工 / 难民/流亡者 / 共享海外多年轨` 压成同一张“主家庭/主路径 atlas”，显性回答每一整棵树或整条路径是否已经同时具备 `主路线闭环 / 七维主链 / 模板迁移`；本轮又把 `inspectTemplateLayerReuseCoverage()` 的四层结果一并接回 atlas，使每一行还能直接返回 `topTemplateStateFieldLayers / topTemplateRuleLayers / topTemplateNodePatterns / topTemplateValidationWays / templateLayerSummary`，避免路线桶已经很满，却还缺“这棵树到底迁进了哪几层模板”的视角。只有在轻量闭环清单已经告诉我们“最缺的是哪一行”之后，再继续钻 atlas。
> - 当前主家庭树继续往前推进时，还要专门看 `window.__DEMO.inspectTensionSupportCoreCoverage()`：它把 `江南沈家 / 上海工商家` 这些家树里已经真实存在的 `父辈/家门 / 婚家 + 安身簇 + 外部门路` 压成一层“主体牵制支撑簇矩阵”，用来判断这一步是在补整树共享模板，还是仍只在保守保留某个细主体组合的 route-local 差异。
> - 在 `主体牵制支撑簇矩阵` 之下、`安身簇子类/细主体组合` 之上，现在还要继续看 `window.__DEMO.inspectTensionAnchorConnectionCoverage()`：它把“家门/婚家锚点是否已经与外部门路形成共享骨架”单独压成一层 `主体牵制锚点门路骨架矩阵`。当前 `父辈/家门 + 外部门路` 已横跨 `江南沈家·男线 / 上海工商家·男线 / 上海工商家·女线`，`婚家 + 外部门路` 已横跨 `江南沈家·女线 / 上海工商家·女线`，因此后续看板若还留着 route-local 候选，应优先理解成“共享骨架已成，只剩安身细分或家庭组合仍保守留在线内”，而不是回头把整棵树当成没有共同模板。
> - 现在又要继续看 `window.__DEMO.inspectTensionSupportClusterCoverage()`：它位于 `主体牵制支撑簇矩阵` 与 `主体牵制锚点原型矩阵` 之间，专门把 `安身 / 工友 / 账房/工友 / 工友/安身` 这些安身簇子类压成中矩阵，用来判断“这批主家庭树前沿究竟只是细主体组合还缺第二条真实路线，还是连安身簇子类本身都还没有共享”。
> - 从这轮起，再补一层 `window.__DEMO.inspectTensionRuleCoverageDrift()`：它不看“代码里有没有这句话”，而是直接检查 `AUX_SUBJECT_TENSION_RULES` 里哪些牵制规则在 `inspectMainlineCoverage()` 与 `inspectNaturalPlaythroughs()` 里都零命中。只要某条 slimmer 规则还没进主链或自然回放，就先按“模板/文档口径漂移”处理，避免把尚未落地的句子误判成已经能支撑 `江南沈家 / 上海工商家 / 纱厂女工` 比较的真实证据。
> - 当 `route-local` 前沿已经进入“同类候选很多、锚点也已经挂上，但仍不能升格”的阶段时，再看 `window.__DEMO.inspectRouteLocalContrastPlan()`：它不会硬抬模板，而是把每个候选已经拿到的 `同类路线 / 可比较路线 / 共享来源路线` 直接压成对照位总表，先回答“下一步该拿哪条第二路线做真实对照”。
> - 从本轮起，这张对照表不再满足于“给一条路线级例子”；若对照路线本身也有同类候选，它会优先返回**候选级** `mainline/natural` 最小样例，再退回路线锚点样例。这样 `上海工商家 / 江南沈家 / 苏北士兵多年线 / 纱厂女工` 的比较，不会再被过粗的路线级例子误导成“整条线差不多”，而能直接复现到当前正在比较的那组细组合。
> - 再下一步不是立刻升格，而是看 `window.__DEMO.inspectRouteLocalContrastEvidence()`：它会把上一步拿到的候选级 `mainline/natural` 样例真正跑一遍，保守区分 `同键双层对照 / 同骨架双层对照 / 已有双层样例但结构偏粗 / 样例未双层跑通`。只要仍停在“同骨架”或“结构偏粗”，就继续明确 keep `route-local`，不要为了整齐把 `上海工商家 / 江南沈家 / 苏北士兵多年线` 的细组合硬抬成共模。
> - 本轮再把“苏北样板抽出的主体规则层到底迁到了哪几棵树/哪几条主路径”显式压成 `window.__DEMO.inspectSubjectRuleSchemaCoverage()`：它会把 `FAMILY_SUBJECT_RULES / AUX_SUBJECT_RULES / AUX_SUBJECT_TENSION_RULES` 与 `家门主体槽 / 亲缘婚家槽 / 营生工友槽 / 安身承接槽 / 外部门路槽` 一起比较。当前 `schemaReadyRouteCount = 10 / 10`，其中 **7 / 10** 已具备“锚点或营生 + 安身承接 + 外部门路”三槽结构；因此后续若再看到 `沈家 / 工商家 / 女工` 的 route-local 前沿，先问“是不是已有共享 schema 只剩 local combo”，不要先默认整棵树仍缺主体模板。
>
> 来源：
> - 根据根目录 `设计规范-分支节点.md` 的「主家庭复用模板（本轮抽取）」节整理；
> - 与根目录 `README.md` 的运行时分析入口保持一致。

---

## 状态字段

| 层 | 字段 | 作用 |
|---|---|---|
| 基础快照 | `family / track / origin / year / age / gender` | 家庭、当前主路径、借用的 origin 行动表与年份年龄 |
| 人物能力 | `attrs.{body,knowledge,craft,mind,network,fame}` | 六维累计值，供 `gate / chanceBy / optionEnabled` 共用 |
| 有限资源 | `res.{money,health,relation,position}` | 钱财/健康/关系账/处境，共用同一人生资源池 |
| 过程记忆 | `echoes[]` | 最通用“已发生事实”存储层；主链多靠它跨年传递 |
| 事件记账 | `firedEvents[]` | 年度事件命中记录，与情境节点相互守门 |
| 主体状态 | `subjects.*` | 统一承载家庭主体（如 `father/household/marriage/ledger/workers`）、外围支撑主体（如共享海外轨、`难民/流亡者` 共享 profile、`纱厂女工` 的 `宿舍/稳饭`、`江南沈家 / 上海工商家` 的门内家计与借住接应，以及本轮 `苏北留乡 workteam / 分田后账` 里从 `买丁 / 托情 / 病身免役` 晚段抽出的 `家口/安身` support）与外部门路主体（`connections`），以及苏北样板的 `mother/wife` 等 |
| 派生事实 | `draftStatus / tenantRight / quotaPressure / landStage` | 用于 HUD/终局可见的事实收束层（当前以苏北为主） |
| 结构化 unknown 边界 | `quotaTransfers[] / quotaRippleResponses[] / draftInstitutionBoundaries[] / goldPreservationBoundaries[] / institutionDetails[] / workteamRecognitionBoundaries[] / longConfirmations[] / longConfirmFollowups[] / supportLongConfirmPlacements[] / returnRouteLandings[]` | 把“只确认到哪一层、还缺哪类制度/真相证据”从终局句抽成统一字段层，供主链矩阵、自然回放与 route 级比较共用；其中 `quotaRippleResponses[]` 与 `longConfirmations[]` 现都已稳定带出 `recordedByArchetypes`，`supportLongConfirmPlacements[]` 继续回答“多年确认后的后账由哪层安身主体接住”，而 `returnRouteLandings[]` 现已不只覆盖 `海外试探回身旧路 / 逃亡回乡旧屋旧路重接`，也把 `归乡士兵旧屋、家口与旧地能不能真接牢` 一并收成同字段的晚段模板 |

---

## 规则层

- `入口层`：`family -> track -> origin` 三层分离。出生家庭不等于人生路径，路径再借用 origin 的行动表与年度事件。
- `守门层`：`triggerSatisfied()` 同时用于情境级与选项级触发；`yearFrom / yearTo / age / hasEcho / noneEcho / sex` 在 UI 与执行层一致生效。
- `家庭守门层`：`triggerSatisfied()` 支持 `family / families` 守门；并与 `family profile` 回退打通，使 `scholar / capitalist / gentrywoman` 等直玩入口在未显式指定 `family` 时，也能命中家族专属主干。
- `家庭判定优先级层`：一旦状态里已经显式写入 `family`，后续 `HUD / 终局 / 主体规则 / family helper` 必须先服从该家庭，再考虑 `origin` 默认 profile。
- `路径兼容层`：家庭模式从 `tenant` 切到 `soldier-path / flee-refugee` 后，后续专属情境不应只认旧入口；应对 `tenant + soldier` 或 `tenant + refugee` 等组合开放，避免中途断链。
- `主体层`：
  - 主体独立回应：玩家决定 ≠ 他人同意；他人状态不由玩家动作直接“签字”。
  - 规则表化：把已经真实存在的主体推进沉淀为 `FAMILY_SUBJECT_RULES / AUX_SUBJECT_RULES` 式规则表，减少散落特判。
  - 触发口径对齐：`applySubjectRules()`（病母/妻）已与 `applyFamilySubjectRules()/applyAuxSubjectRules()` 对齐支持 `yearFrom/yearTo + anyEcho/noneEcho + onceKey/noteKey + logText/tone` 这组通用触发字段；仍允许保留 `when()/apply()` 的最小自定义，不为对称强改旧规则。
  - `多主体并账`：HUD 与终局不再只显示一个辅助主体快照，而是稳定回收多条 `currentAuxSubjectFacts()`。
  - `外围 profile 继续拆层`：当一条主路线长期把两类不同性质的外围事实都压进同一个桶时，优先拆成新的辅助主体层，而不是继续往同一 `connections` 里塞句子；例如共享海外轨现已把 `旧同学/旧门路` 与 `家书/汇路/安身` 分成 `connections + support` 两层，`难民/流亡者` 共享 profile 已把 `后方屋檐/亲族互帮/回乡照应` 与 `旧街坊/捎话旧路/两头消息/脚程活路` 分成 `support + connections` 两层，`纱厂女工` 已把长期挤在外部门路里的 `里弄屋檐/宿舍稳饭/借檐续口粮` 抽成新的 `support`，形成 `workers + support + connections` 三层，而 `苏北士兵多年线` 在本轮也把 `留外 years` 里原先只挂在 `铺位差事/旧袍泽旧路` 上的“借铺稳饭、临时落脚与还能把家口先托住的一层安身底盘”单独抽成 `support`；`江南沈家 / 上海工商家` 则把原先长期只挂在 `household / father` 与零散 echo 上的 `家门修补 / 妇孺口粮 / 借住接应` 抽成独立 `support`，让主家庭线也能显性分开“谁先把人和家门接住”与“谁再把外部门路续下去”。
- `主体牵制层`：当多主体已并账成立时，用 `AUX_SUBJECT_TENSION_RULES` 显性回答“谁先让位、谁把后半生先顶住”；若主体状态仍 `unknown / n/a`，不得硬写成已发生事实。
- `主体牵制层补充`：晚段若已经真实进入“家门/婚家退后，只剩门内安身先把日子接住”的纯收束态，也应单独写成牵制规则与代表态，而不是只留在 `家门回应 / 后半生安放` 的概述里；但这类纯收束规则必须支持 `noneEcho` 守门，避免反过来遮掉 `public/teach/workroom + household` 这类更细的 mixed 牵制样板。
- `主体牵制回应原型归并层`：当多条主路线已经真实出现“门内接回一点、外头活路还没退净、两头日子先并着走”这一类 years mixed 姿态时，不应继续把 `苏北士兵多年线`、`苏北逃亡回乡线` 里的字面差异各记成新的 route-local 回应句。应先在 `tensionResponseArchetypeMeta()` 里把这组姿态并成同一个共享回应原型，再让 `inspectTensionResponseArchetypeCoverage()` / `inspectRouteLocalCandidatePriority()` / `inspectRouteLocalCandidateBlockers()` 按共享 archetype 回收。只有在运行时还没有第二条真实路线或第二个自然回放样板时，才继续保留 literal route-local。
- `主体牵制拆分边界`：若某条主路线在真实规则表里一旦进入某个晚段 echo，就**必然**同时带出 `workers / ledger / support` 中的更重支撑簇，就不要为了让矩阵看起来更细，硬把它再拆成一个“只剩 support + connections”的更瘦规则。像 `上海工商家` 这类城市家树里，`biz:settle-public / shdaughter:settle-public / shdaughter:settle-household` 会稳定把 `工友关系收窄保留` 一并带出来，因此 `门内安身 + 外部门路` 若没有真实脱开 `workers` 的第二条路线，就应继续并回 `安身主体/工友/家口 + 外部门路`，或明确保留 `route-local / unknown`，不要编造一条运行时永远走不到的 slimmer 组合。
- `主体规则 schema 层`：当主路线已经不再缺闭环，而 `support / connections / familySubjects / AUX_SUBJECT_RULES` 也都真实存在时，不应继续只靠文档印象判断“主体模板迁到哪一层”。还应运行 `window.__DEMO.inspectSubjectRuleSchemaCoverage()`，直接比较 `家门主体槽 / 亲缘婚家槽 / 营生工友槽 / 安身承接槽 / 外部门路槽` 与 `家庭主体规则表 / 辅助主体规则表 / 主体牵制规则表` 的覆盖度，先回答“整树已有共享 schema，只剩局部组合差异”还是“规则层本身仍缺槽位/规则表/双层验证”。
- `主体牵制安身簇子类层`：当 `AUX_SUBJECT_TENSION_RULES` 已经能稳定识别“有没有安身簇”后，不应继续把 `安身 / 工友 / 账房/工友 / 工友/安身` 这些中层差异压扁；还应继续用 `window.__DEMO.inspectTensionSupportClusterCoverage()` 把它们压成中矩阵。这样 `江南沈家 / 上海工商家 / 纱厂女工` 当前仍停在 route-local 的多组细主体组合，就能先回答“共享的是哪类安身簇子类”，再决定是否继续保留 `route-local / unknown`。
- `主体牵制支撑簇层`：当 `AUX_SUBJECT_TENSION_RULES` 已经能稳定识别 `父辈/家门 / 婚家 / 妻子病母` 这些家庭锚点后，不应继续只在“细主体组合”与“回应/角色原型”之间跳读；还应继续用 `window.__DEMO.inspectTensionSupportCoreCoverage()` 把它们保守压成 `家庭锚点 + 安身簇 + 外部门路` 这层中矩阵。这样 `江南沈家 / 上海工商家` 当前仍停在 route-local 的多组 `父辈/家门 + 安身主体/工友/家口 + 外部门路`、`婚家 + 安身主体/工友/家口`，就能先回答“共享支撑簇是否已经成立”；若这层已共享，而更细主体组合仍缺第二条真实路线证据，就继续明确保留 `route-local / unknown`，不要为了整齐感硬并同。
- `多年确认层`：对 `missing / loss / injury / blacklist` 等“不能当年立刻确认”的事实，统一按 `发生 → 多年无准信 → 事实确认` 推进；并用 `payload.longConfirmations[]` 结构化呈现证据层与真相边界层（仍保留 `unknown`）。若这条主线还能稳定回答“确认以后，活下来的人先把后半生安在哪一层”，则继续落到 `payload.longConfirmFollowups[]`，把后续去向边界也收成字段层，而不是只留附带布尔标记。
- `多年确认来源共模层`：当 `payload.longConfirmations[]` 已经稳定带出 `recordedBy` 后，不应继续只把这层来源埋在证据数组里；还应先用 `window.__DEMO.inspectLongConfirmRecordedByCoverage()` 把 `活下来继续上路的人 / 家里活着的人 / 你自己 / 厂门口风 / 工头与稳工位的认人后账` 这组字面来源反查成可比较矩阵，再继续用 `window.__DEMO.inspectLongConfirmSourceArchetypeCoverage()` 保守抽成 `活着继续把后账记下去的人 / 门口口风与认人后账` 两类来源原型。这里仍不补失联真相、折损名单、诊断细节或黑名单流转机制，只把“这层多年后账究竟靠谁被看见”结构化。
- `多年确认 × 安身承接字段层`：当一条主路线已经同时具备 `support` 与 `payload.longConfirmations[]` 时，不应只停在“能看见共现”这一层；应继续把“多年后账后来是由哪层安身主体接住”的答案收成 `payload.supportLongConfirmPlacements[]`，至少显性给出 `currentPlacementLabel / confirmedFactLabel / currentTruthLabel / recordedBy / evidenceLabels / missingEvidenceLabels`。若只能确认多年后账、却还不能确认是哪层安身底盘在承接，就继续保留 `unknown`，不要为了对称硬补承接句。
- `unknown 字段层`：对制度参数边界与证据不足的长期缺口，只登记边界与缺失证据，不补细节；优先以字段组（field group）方式共模，并进入专门覆盖检查。若同一字段组会在“前期入口 / 晚段回场”重复出现，必须继续做成**同字段多阶段覆盖**，避免只在前段可见。若某个 `institution-detail` 子类已经稳定拆成独立 field group（如 `draft / workteam / gold`），则 `unknown 台账` 的优先级排序应由子组承担，不再让父桶继续重复占位。
- `名额转嫁承接共模层`：当 `quota:shifted / quota:shifted-again` 这组边界已经不只是在苏北留乡里“出现过”，而是稳定横跨 `躲丁 / 买丁 / 托情 / 病身免役` 四条留乡主分流时，不应继续只看全局场景数；应进一步把 `payload.quotaTransfers[]` 显性补到 `branchKind / branchKindLabel / transferTier / transferTierLabel / currentTransferLabel`，用来回答“是哪一条缓过分流把名额转给了别家、当前停在首轮还是二次再转嫁”。这里仍不补承接户身份与地方摊派细数，只把分流/层级结构化。
- `名额转嫁证据来源共模层`：当 `payload.quotaTransfers[]` 已经稳定带出 `recordedBy`，就不应继续只把这层证据来源埋在字段数组里；还应把它反查成 `村里名册 / 村里口风 / 保长差事` 这组共享来源矩阵，用来回答“这笔名额转嫁目前究竟是靠哪类可观察来源被系统看见、哪些来源已经跨分流复用”。这里仍不补承接户身份、摊派细数或保长心证，只把证据来源结构化。
- `名额后账回应共模层`：当 `quota:quiet / quota:small-gift / quota:run-errand` 这组边界已经不只是在苏北留乡里“出现过”，而是稳定覆盖首轮/二次两层后账时，不应继续只把它当成 `quotaRippleResponses[]` 的证据字段存在；应进一步显性补到 `rippleKind / rippleKindLabel / rippleTier / rippleTierLabel / currentRippleLabel`，用来回答“这笔后账目前是压成了闭门不问、递小礼，还是跑差补账，以及当前停在首轮还是二次后账”。这里仍不补对方心账、村里口风或差事抵账口径的具体细节，只把回应/层级结构化。
- `名额后账证据来源共模层`：当 `payload.quotaRippleResponses[]` 已经稳定带出 `recordedBy`，就不应继续只把这层证据来源埋在字段数组里；还应先把它反查成 `家门里人的猜测 / 村里口风 / 对方神色 / 保长差事` 这组字面来源矩阵，再继续保守抽成 `门里门外的人情口风 / 差事补账后的后账口风` 两类来源原型，用来回答“同一组名额后账回应如今究竟靠哪类来源被系统看见、哪些来源已经跨回应姿态共享”。这里仍不补对方心账、村里最终口风或差事抵账细则，只把来源结构化。
- `地方兵役参数共模层`：当 `苏北抓丁/兵役` 这组未知边界已经同时横跨 `留乡缓过 / 被带走从军 / 举家离乡避征` 三条主路线时，不应继续只停在通用 `institutionDetails[]` 里；应进一步拆成 `payload.draftInstitutionBoundaries[]`，至少显性给出 `routeKind / currentPressureLabel / currentResponseLabel / responseTier / recordedBy / evidenceLabels / missingEvidenceLabels`，用来回答“同一拨抓丁压力最后在三条主路线里各被接成了什么”。
- `地方兵役回应层级共模层`：当 `payload.draftInstitutionBoundaries[]` 已经不只够回答“落到哪条主路线”，而是稳定带出 `responseTier / responseTierLabel` 时，不应继续只看 route 摘要；应再把它反查成 `缓过一回 / 被带走 / 离乡避征 / 周旋未稳` 这组共享回应层级，用来回答“同一拨抓丁压力如今能被哪些层级接住、哪一层仍只停在局部样板里”。这里仍不补抽丁比例、礼价和免役细数，只把回应层级结构化。
- `地方兵役回应原型共模层`：当 `payload.draftInstitutionBoundaries[]` 已经稳定带出 `responseTier`，但四层字面层级仍各自停在不同 route bucket 上时，不应把“shared = 0”直接误读成“这组回应还不能再共模”。还应继续把它保守抽成 `responseArchetype / responseArchetypeLabel`，至少先区分 `留在门口周旋型` 与 `被逼离场改线型`，用来回答“同一拨抓丁压力究竟仍把人留在门口周旋，还是已经把人生从原路上逼开”。这里仍保留 `缓过一回 / 周旋未稳 / 被带走 / 离乡避征` 的字面差异，不为了共享原型而抹平 route-local 层级。
- `地方兵役证据来源共模层`：当 `payload.draftInstitutionBoundaries[]` 已经稳定带出 `recordedBy`，就不应继续只把这层证据来源埋在 route 摘要里；还应把它反查成 `村里传到门口的告示与口风 / 保甲点验桌前后账 / 一家离乡前仓促应对后账` 这组共享来源矩阵，用来回答“同一拨抓丁压力目前究竟靠哪类可观察来源被系统看见、哪些来源已经跨路线复用”。这里仍不补抽丁比例、礼价、免役细数或具体承办人，只把证据来源结构化。
- `地方兵役来源原型共模层`：当 `payload.draftInstitutionBoundaries[]` 的字面 `recordedBy` 已经稳定横跨三条主路线，但仍保留 `桌前后账 / 离乡后账` 这类 route-local 差异时，不应把“字面不同”误写成“来源层还不能共模”。还应继续把它抽成 `村里门口口风 / 抓丁临门后的应对后账` 这组共享来源原型，用来回答“这组边界是否已经共享同一种可观察来源结构，同时仍保留每条路线各自怎么记账的字面差异”。这里仍不补抽丁比例、礼价、免役细数或具体承办人，只把来源原型结构化。
- `金圆券保全边界共模层`：当 `上海工商家` 男女线的 `金圆券风暴：保全、清账与退路折算口径` 已经稳定横跨两条主路线时，不应继续只停在通用 `institutionDetails[]` 里；应进一步拆成 `payload.goldPreservationBoundaries[]`，至少显性给出 `routeKind / currentPreservationLabel / currentReckoningLabel / recordedBy / evidenceLabels / missingEvidenceLabels`，用来回答“顾家男女线各自先守哪一层、后账又卡在哪一层折算”。
- `金圆券保全种类/层级共模层`：当 `payload.goldPreservationBoundaries[]` 已经不只够回答“落到哪条主路线”，而是稳定带出 `preservationKind / preservationKindLabel / reckoningTier / reckoningTierLabel` 时，不应继续只看 route 摘要；应再把它反查成“藏退路 / 先清账发薪 / 先死保厂壳 / 先接家口 / 先守公共门面 / 先守借住去路 / 先守熟手小作”这组保全姿态，以及 `退路暗账层 / 外壳苦撑层 / 口粮安身重摊层` 这组共享后账层级，用来回答“工商家男女线当前分别靠哪种保全姿态接风暴、哪些后账层级已经跨路线站稳”。这里仍不补金圆券后真实折价口径、保住的资产明细与门路真相；`unknown` 只保留在“资产/门路到底真保住哪一条、后续怎样折着算”的证据缺口上，而不是让“藏退路姿态”继续停在代表态缺失。
- `金圆券保全证据来源共模层`：当 `payload.goldPreservationBoundaries[]` 已经稳定带出 `recordedBy`，就不应继续只把这层证据来源埋在字段数组里；还应先把它反查成 `顾家家门与账房在风暴里的口风 / 金圆券后残账与工友安置留下的后账 / 顾家家门在风暴里的口风 / 女工家口与安身网络在风暴里留下的后账` 这组**字面来源矩阵**，再继续抽成 `顾家家门口风 / 风暴后家口工友安身后账` 这组**来源原型矩阵**，用来回答“同一组金圆券保全边界目前究竟靠哪类可观察来源被系统看见、哪些来源已经跨路线复用，而哪些仍应保留男女线各自记账的字面差异”。这里仍不补资产明细、折价细数或门路真相，只把证据来源结构化。
- `制度参数共模层`：像 `苏北抓丁六分支` 里反复出现的 `本地抽丁比例 / 壮丁费/买丁礼价 / 保甲摊派与免役认定口径`、`工作队进村 / 返乡重站丈量场时的认人认地与旧账折算口径`，以及 `上海工商家` 男女线共享的 `金圆券风暴：保全、清账与退路折算口径`，都应先进入 `payload.institutionDetails[]` 与 route 级制度摘要，再按证据成熟度继续拆成独立 field group；一旦子组已独立站稳，就不应继续靠父桶 `institution-detail` 在 unknown 排序里重复记分。
- `制度缺口反查层`：当 `payload.institutionDetails[]` 已经能按主路线桶汇总后，不应继续只盯 route 视角；还应把这些 route 级制度摘要反查成 `missingEvidenceLabel -> routes` 的共享缺口矩阵，优先看哪些缺口横跨多条主路线，再决定下一轮补哪一层制度边界最能提升整局完整度。
- `support × unknown 联动层`：当一条主路线已经同时具备 `support` 与 `institutionDetails[] / longConfirmations[]` 时，不应让“谁先把人接住”与“制度/多年确认只确认到哪一层”分散在不同输出面里；应保证同一 payload 能同时读到这两层，并进入专门验收。当前 `上海工商家女线·金圆券风暴`、`难民/苏北逃亡返乡重站丈量场`、`归乡士兵重站丈量场`，以及本轮新增的 `苏北留乡 workteam / 分田后账` 已形成这类联动样板。

---

## 节点模式

| 模式 | 作用 | 例子（仅示意） |
|---|---|---|
| `入口抉择节点` | 把“会不会发生”拆成玩家可选入口 | 抓丁/婚约/接班等起手节点 |
| `同年中继节点` | 把“想这样做”与“现实真成了什么”拆开 | `买丁桌上·收钱算不算数`、`托情桌上·这回缓不缓得下` |
| `跨年后账节点` | 把一时选择沉淀成几年后的活法 | `买丁之后·几年后的亏空`、`抗婚以后·先把日子接住` |
| `时代冲击节点` | 把抗战/通胀/工作队进村写成各线自己的回应层 | `战火压厂·先把家业往哪边挪` 等 |
| `多年确认节点` | 把“失联/折损”推进成“多年无准信 → 后来如何记账” | `soldier:missing-*`、`refugee:loss-*` 等 |
| `终局回收节点` | 把 1949 去留与前史挂回事实句 | `stay-or-go-1949` + `endingVerdict / buildFinalLedgerFacts` |

---

## 验证方式

- `全量回归（Node）`：运行 `node demo-regression.mjs`，以低内存参数调用 `window.__DEMO` 的统一验收。默认先走轻量检查链，并显式跳过最重的 `route-local-promotion-verdict`；若只想补跑这层升格裁决，再用 `ROUTE_LOCAL_VERDICT=1 node demo-regression.mjs`；若要整包重算 atlas / board / whole-game-plan，则用 `FULL_DEMO_REGRESSION=1 node demo-regression.mjs`。
- `整局汇总`：若当前目标只是回答“这一步是不是在补整局”，先读 `window.__DEMO.inspectWholeGameProgressBundle()`；若在允许的环境里需要轻量命令口径，再用 `WHOLE_GAME_PROGRESS_BUNDLE_SMOKE=1 node demo-regression.mjs`。它会优先返回 `闭环 / 模板四层 / 主体 schema / 主系统 / 模板迁移前沿 / 主链蓝图前沿 / 尾部 route-local`，避免每次都手工串四张表。
- `九行作战板`：若当前目标是决定“下一步该补哪一整行才让整局更完整”，先读 `window.__DEMO.inspectWholeGameDesignPlaybook()`。它会逐行返回 `当前角色 / 四层模板 / 证据密度 / 独立轨 / 下一步`，适合在主链已闭环、整局进入模板收口阶段后，继续判断哪一行仍值得优先推进。
- `当前前沿束`：若当前目标不是重跑整包，而是快速回答“这一步会不会改变整局排序”，先跑 `window.__DEMO.inspectTopRouteLocalCandidateBundle()`，或直接用 `TOP_ROUTE_LOCAL_CANDIDATE_SMOKE=1 node demo-regression.mjs`。它会只汇总当前最前 `route-local` 候选的 `优先级 / 阻塞 / 对照位 / 样例 / 候选级证据阶段 / 下一问`，避免每次都靠最重的 `route-local-promotion-verdict` 才知道最后一格 blocker 卡在哪。
- `compare-only 差额板`：若当前已经知道最前候选是谁，但还要继续回答“现成对照位为何仍只到 compare-only”，先读 `window.__DEMO.inspectTopRouteLocalCompareGapBoard()`。它只把前三条真实对照位压成 `comparisonStage / comparisonBasisLabel / subjectDeltaSummary / sharedHudKeys / example ids`，适合直接回答“这一步该 keep 还是还值得继续核对”，而不必再整批下钻所有对照证据。
- `主链矩阵`：`window.__DEMO.inspectMainlineCoverage()` 用代表态抽检“闭环是否成立”。
- `自然回放`：`window.__DEMO.inspectNaturalPlaythroughs()` 用真实年份推进验证“自然推着也站得住”。
- `主路线样例账板`：`window.__DEMO.inspectWholeGameScenarioDeck()` 只按现有 `mainline / natural` 场景定义统计 10 条主路线桶的双层样例分布，适合在重聚合暂时跑不动时，先判断哪一条主路线样例更薄、这轮是否真的补到了代表态或自然回放层。
- `阶段成对验收`：若一条 mixed 牵制规则同时跨 `战后几年 / 后半生安放` 两段，例如 `纱厂女工·postwar-household / settle-household`，则不得只补晚段 `settle` 代表态；必须同时补 `postwar-*` 的主链代表态与自然试玩，确保“跨年后果层已落地”不再只靠 `1949` 收束反推。
- `五段闭环`：`window.__DEMO.inspectRouteLoopCoverage()` 统一按“起手/主体回应/跨年后果/长期结果/终局回收”聚合各主路线桶是否闭环。
- `主家庭/主路径五段槽位 atlas`：当 route bucket 层已经确认五段闭环后，再跑 `window.__DEMO.inspectFamilyPhaseAtlasCoverage()`。这里不再只问“闭环有没有过线”，而是把 `苏北贫农家 / 苏北留乡主分流 / 苏北士兵多年线 / 苏北逃亡回乡线 / 江南沈家 / 上海工商家 / 纱厂女工 / 难民/流亡者 / 共享海外多年轨` 这 **9** 行 atlas 的五段，直接反查成真实 `mainline + natural` 代表节点；只有把“整棵树/整条路径的五段到底靠哪些节点承接”也做成运行时资产，后续才更容易判断是在补整局模板，还是又回到单枝修边。本轮起，这层 atlas 还会优先回收各行预设的**阶段代表态**，避免再被某一条晚段收束场景把五段全部顶满，导致整棵树看起来“已闭环”，却读不出前提、主体回应与跨年后果究竟分别靠哪组真实节点在承接；同时这些 `phase summary / phase gap` 结果也已正式并回 `inspectFamilyRouteAtlasCoverage()`、`inspectGlobalMainlineBoard()` 与 `inspectWholeGameProgressPlan()`，让总表在排优先级时能同时回答“这棵树现在卡在 route-local / unknown”，以及“它的五段事实锚点到底落在哪几组真实节点上”。
- `七维主链矩阵`：`window.__DEMO.inspectMajorChainDimensionCoverage()` 再把每条主路线是否同时具备 `前提/制度过程态/跨年后果/时代冲击/主体回应/长期结果/终局回收` 收成统一矩阵。它不替路线补内容，只负责把 README 里的整局总表推进成可回归的运行时资产，帮助先判断“这一步是不是在补整局”。
- `模板矩阵`：`window.__DEMO.inspectTemplateExtractionCoverage()` 统一按主路线桶汇总“是否已完整复用苏北样板抽出的模板层”，至少显性返回 `五段闭环 / 外围主体层 / 制度层 / unknown 字段组 / support × institution / support × longConfirm` 这几层 route 级摘要。它不负责替路线补剧情，只负责让“模板已经迁移到哪里、还缺哪类整局字段层”变成可检查结果。
- `整局模板台账`：`window.__DEMO.inspectWholeGameTemplateLedger()` 会继续把 `inspectTemplateLayerReuseCoverage()` 的四层模板矩阵，和 `inspectGlobalClosureChecklist()` 的整局阶段、`inspectRouteLocalCandidatePriority()` / `inspectRouteLocalCandidateBlockers()` 的当前前沿并成一份**按主路线直读**的台账。它适合在“主链已齐、整局进入模板收口阶段”之后回答：哪条主路线仍会改变整局排序、哪些路线只该留在模板库存、以及每条路线的当前缺口究竟属于证据层、模板层还是成熟样板维护。
- `九行模板迁移账板`：当 `整局完工账本` 已经能回答“九行哪一行最重要”后，再跑 `window.__DEMO.inspectWholeGameTemplateMigrationBoard()`。这里不新增任何剧情或制度事实，只继续把 9 行主家庭树/主路径的四层模板迁移状态压成 `仍有 route-local 前沿 / 仍有自有模板余账 / 仍有共享借入余账 / 成熟样板` 四类角色，并显性比较每一行当前是 `四桶待补`、`四桶已迁入，但仍有行内分布差`，还是 `四桶共享稳定`。只有把这层账板做出来，后续才更容易判断“这轮到底是在补整局模板迁移，还是只让某一枝更细”。
- `四层模板复用矩阵`：当“苏北样板抽取”已经不是一句口号后，再跑 `window.__DEMO.inspectTemplateLayerReuseCoverage()`。这里要统一检查每条主路线已经复用到哪些 `状态字段 / 规则层 / 节点模式 / 验证方式`，并把这四层再反查成共享矩阵，直接看“哪几层已经跨路线站稳、哪几层仍主要停在某一家/某一路径里”。只有把这四层都显性列出来，后续才不容易误把“单枝越来越细”当成“整局越来越完整”。
- `主体规则 schema 矩阵`：当上面两层已经能回答“模板迁了没”后，再跑 `window.__DEMO.inspectSubjectRuleSchemaCoverage()`。这里不新增剧情事实，只继续比较“哪些主路线已经共享 `家门主体槽 / 亲缘婚家槽 / 营生工友槽 / 安身承接槽 / 外部门路槽` 的主体 schema、哪些规则表已经成共模、哪些路线只剩局部组合仍应保守 route-local”；同时这层结果也应回流到 `inspectTemplateLayerReuseCoverage()` 与 `inspectGlobalCompleteness()`，让“共享 schema 已成”成为运行时可回归结论，而不是推进记录里的人工解释。
- `主体牵制支撑簇矩阵`：当 `主体牵制原型矩阵` 已经能稳定区分 `家庭锚点` 与 `支撑主体` 后，再跑 `window.__DEMO.inspectTensionSupportCoreCoverage()`。这里不替任何路线补新事实，只继续比较“哪组家庭锚点已经挂上共享安身簇、哪些主家庭树还只是细主体组合 route-local”；同时这层也应回流到 `inspectRouteLocalCandidateBlockers()` 与 `inspectTemplateLayerReuseCoverage()`，让 `shared-support-core-template-but-local-combo` 与 `主体牵制支撑簇矩阵` 成为可回归的模板语言，而不是继续靠人眼读 `沈家 / 工商家` 的几条牵制句手工判断。
- `主体牵制安身簇子类矩阵`：当 `主体牵制支撑簇矩阵` 已经证明“有安身簇”后，再跑 `window.__DEMO.inspectTensionSupportClusterCoverage()`。这里不替任何路线补新事实，只继续比较“哪类安身簇子类已经跨路线复用、哪些主家庭树只是家庭锚点或外部门路组合仍保守 route-local”；同时这层也应回流到 `inspectRouteLocalCandidateBlockers()` 与 `inspectTemplateLayerReuseCoverage()`，让 `shared-support-cluster-template-but-local-combo` 与 `主体牵制安身簇子类矩阵` 成为可回归的模板语言。
- `主体牵制漂移矩阵`：当推进记录、README 或人工判断声称“某条 slimmer 牵制规则已经存在”时，再跑 `window.__DEMO.inspectTensionRuleCoverageDrift()`。这里不新增剧情事实，只继续比较“哪些牵制句已经进入 `mainline + natural` 可验证运行时、哪些仍只是模板或文档层口径”；如果某条规则 `coveredByMainline = 0` 且 `coveredByNatural = 0`，就应优先把它视为漂移，而不是继续拿它给 `route-local` 候选补虚假的第二条来源。
- `route-local 对照位矩阵`：当 `route-local 候选矩阵 / 锚点矩阵 / 阻塞矩阵` 都已经告诉你“这批候选值得继续比较，但还不能升格”后，再跑 `window.__DEMO.inspectRouteLocalContrastPlan()`。这里不替候选补第二条路线，而是保守列出每个候选当前已经拿到的 `同类路线 / 可比较路线 / 共享来源路线`，直接回答“下一轮该拿哪条第二路线做真实对照”；如果这张表仍给不出第二路线，就继续保留 `route-local`。
- `候选级对照复现`：若 `inspectRouteLocalContrastPlan()` 已经给出第二路线，不要只看路线名；还要优先读取 `contrastRoutes[*].matchedCandidateLabel / exampleMainlineId / exampleNaturalId`。规则是：先复现**同类候选自己的最小样例**，只有该路线当前没有候选级样例时，才退回共享锚点样例。这样比较 `父辈/家门 + 安身主体/工友/家口 + 外部门路`、`妻子/病母 + 安身主体 + 外部门路` 这类细组合时，才能真正回答“这组细组合有没有第二路线证据”，而不是只确认“该路线某处曾出现过更粗模板”。
- `主系统闭环矩阵`：当 `五段闭环 / 七维主链矩阵 / 四层模板复用矩阵` 都已经稳定后，再跑 `window.__DEMO.inspectMajorSystemClosureCoverage()`。这里不再按路线看“谁还缺一层”，而是把 `地方兵役参数边界 / 返乡认地折算边界 / 金圆券风暴保全边界 / 名额转嫁承接边界 / 名额后账回应边界 / 多年确认边界` 六组主系统统一比较成一张总表，显性回答它们是否已经具备 `字段证据层 / 主路线层 / 层级或姿态层 / 字面来源层 / 来源原型层 / 安身承接层` 等闭环层。只有把系统层也纳入回归，后续才不容易在主路线闭环以后又回到单枝视角。
- `主家庭/主路径 atlas`：当路线桶和系统桶都已经稳定后，再跑 `window.__DEMO.inspectFamilyRouteAtlasCoverage()`。这里不再只按 `route bucket` 判断，而是把 `苏北贫农家 / 苏北留乡主分流 / 苏北士兵多年线 / 苏北逃亡回乡线 / 江南沈家 / 上海工商家 / 纱厂女工 / 难民/流亡者 / 共享海外多年轨` 统一压成一张 atlas，显性比较每一整棵树或整条路径是否已经同时具备 `主路线闭环 / 七维主链 / 模板迁移`，并把 `共享海外轨` 这类跨树共享路线一并算回家庭树；本轮起，这张 atlas 还要直接带出每一行已复用的 `状态字段 / 规则层 / 节点模式 / 验证方式` 四层模板摘要，以及 `ownUnknownFrontierSummary / sharedDependencyUnknownFrontierSummary` 这组“自有缺口 / 共享借入缺口”口径；同时继续带出 `primaryPhaseSummary / phaseGapSummary / topPhaseSlotSummaries / topNaturalPhaseSlotSummaries`，帮助判断“这轮是在补哪棵树的模板迁移”，还是只是在收共享多年线的借入余账，以及这棵树当前五段闭环究竟由哪组真实节点在承接。
- `整局主链系统看板`：当 atlas 已经足够稳定后，再跑 `window.__DEMO.inspectGlobalMainlineBoard()`。这里不再新增验证维度，而是把 atlas 现有的 `completionPriority / ownUnknown / sharedDependencyUnknown / route-local frontier / templateLayerSummary` 继续翻成一张可直接排全局优先级的看板，显性回答“现在先补哪一整棵树/哪一条主路径更值、哪些仍只该回共享路径处理、哪些仍应 keep route-local”。如果没有这张看板，后续很容易再次回到翻长文档手工排优先级的旧工作流。
- `主家庭/主路径决策层`：当 atlas 已经能同时区分 `自有主路线 unknown / 共享主路线借入 unknown / 跨路线模板型 unknown / 单路线保守 unknown / route-local 候选` 后，不应再靠人工把这些信息重组为下一步计划；还应继续让同一个 `window.__DEMO.inspectFamilyRouteAtlasCoverage()` 返回 `completionPriorityLabel / completionPriorityScore / completionPrioritySummary`，并在总表层显性给出 `topCompletionPriorityRowLabel / topFamilyCompletionPriorityRowLabel / topPathCompletionPriorityRowLabel`，保守回答“当前最该补哪一整棵树/哪一条主路径、属于补整局系统还是只该继续保守挂账”。
- `主家庭/主路径 frontier`：当上述 atlas 已经稳定后，再继续用同一个 `window.__DEMO.inspectFamilyRouteAtlasCoverage()` 读取 `topRouteLocalCandidates / frontierSummary / unknownFieldGroupLabels / unknownFrontierSummary / unknownMaturitySummary / topRouteLocalRowLabel / topUnknownRowLabel / topUnknownFieldGroupSummary / topOwnUnknownRowLabel / topOwnUnknownFieldGroupSummary / topSharedDependencyUnknownRowLabel / topSharedDependencyUnknownFieldGroupSummary / topCrossRouteUnknownRowLabel / topCrossRouteUnknownFieldGroupSummary / topSingleRouteUnknownRowLabel / topSingleRouteUnknownFieldGroupSummary / topPathRouteLocalRowLabel / topPathUnknownRowLabel / topPathUnknownFieldGroupSummary / topPathCrossRouteUnknownRowLabel / topPathSingleRouteUnknownRowLabel`。这里不再只问“哪棵树挂了几个 route-local / unknown”，而要继续区分“哪些 unknown 已经成熟到跨路线模板层、哪些仍应保守留在线内判断”，以及“这组 unknown 到底是主家庭树自身缺口，还是共享多年线借入的余账”，直接回答哪棵树/哪条主路径现在更适合补整局系统，哪一行更适合继续保留 `unknown`；只有把 frontier 也并回 atlas，总表层才真正能优先回答“这一步是否让整个游戏更完整”。
- `整局推进规划层`：当 `四层模板复用矩阵 / 主系统闭环矩阵 / atlas / route-local 候选优先级与阻塞` 都已经存在后，再跑 `window.__DEMO.inspectWholeGameProgressPlan()`。这里不再新增验证，而是把“苏北样板抽出的模板层、当前最该补的主家庭/主路径、该先比较哪几组 route-local 候选、为什么暂时还不能升格”压成同一份运行时结果，直接服务每轮的全局推进判断，避免再次回到手工拼多张表的旧工作流。默认它先复用 `inspectRouteLocalDecisionBoard()` 的轻量保守判定，不自动触发最重的 `route-local-promotion-verdict`；只有在显式传入 `includePromotionVerdict:true` 或预先传入 `routeLocalPromotionVerdict` 时，才继续把升格裁决并回整局规划层。
- `多年确认来源矩阵`：当 `payload.longConfirmations[]` 已经稳定带出 `recordedBy` 后，再跑 `window.__DEMO.inspectLongConfirmRecordedByCoverage()`。这里要把同一组多年确认继续反查成 `活下来继续上路的人 / 家里活着的人 / 你自己 / 厂门口风 / 工头与稳工位的认人后账` 这组字面来源，并确认这些来源不只是字段数组里出现过，而是都被主链矩阵与自然回放双层锁住。
- `多年确认来源原型矩阵`：当上述字面来源已经稳定后，再跑 `window.__DEMO.inspectLongConfirmSourceArchetypeCoverage()`。这里要继续把它们保守抽成 `活着继续把后账记下去的人 / 门口口风与认人后账` 两类共享来源原型，并确认这些原型同样被主链矩阵与自然回放双层锁住；只有这样，后续才不会把“多年确认已有证据层”误读成“这组长期边界的来源结构也已经比较完了”。
- `多年确认总表成熟度层`：当 `window.__DEMO.inspectLongConfirmSourceArchetypeCoverage()` 已经站稳后，不应让 `window.__DEMO.inspectUnknownStructurePortfolio()`、`window.__DEMO.inspectFamilyRouteAtlasCoverage()` 与 `window.__DEMO.inspectGlobalMainlineBoard()` 继续只按字面 `recordedBy` 来判断 `long-confirm-truth` 的成熟度。若 `活着继续把后账记下去的人` 这组来源原型已横跨 `苏北士兵多年线 / 难民流亡者 / 纱厂女工`，总表就应把 `多年确认真相边界` 提升成 `跨路线共享来源`，避免 `难民 / 女工` 在 atlas 里继续被误判成“只到字段化”。
- `主体牵制原型矩阵`：当 mixed frontier 已全部停在 `keep-unknown`、而外围主体也已形成多组覆盖后，再跑 `window.__DEMO.inspectTensionArchetypeCoverage()`。这里不再只看某条 `主体牵制` 句有没有被锁住，而是先把 `父辈/家门 / 婚家 / 妻子病母` 这类**家庭锚点**从 `familySubjects` 与 `subjects` 两侧统一归并，再继续按 `具体安身主体簇 + 是否带外部门路` 压成整局矩阵，直接比较 `病母/妻子 + 外部门路`、`父辈/家门 + 账房/工友 + 外部门路`、`婚家 + 安身主体 + 外部门路`、`外部门路内部牵制` 这类原型究竟跨了几条路线、哪些已双层锁住。只有把“句子级锁定”推进成“家庭锚点先归并、再比较支撑主体”的原型级比较，后续才不容易因为 `household` 写在不同槽位里，就继续围着某一句牵制文案局部修边。
- `主体牵制回应原型矩阵`：当细粒度主体原型已经稳定后，再跑 `window.__DEMO.inspectTensionResponseArchetypeCoverage()`。这里继续把现有牵制压成“门内未稳以前外部门路先顶上来 / 门内接稳以后外部门路退成后手 / 原有底盘未独自撑住时别路先顶上来 / 前一层牵扯退后，现成活路把日子接住”这类回应姿态，只比较回应层是否跨路线站稳，不替任何路线补新事实；同时要吸收已经由真实运行时证明只是字面变体的写法，例如 `退成门外后手` 仍并回“门内接稳以后，外部门路退成后手”，`先给……让位` 仍并回“原有底盘未独自撑住，另一层活路先顶上来”，`婚家退到门后，……把门内日子接住` 也应并回“前一层牵扯退后，现成活路把日子接住”，不要让主家庭树因为措辞差一格就平白多出 literal route-local。这样后续判断 `route-local` 时，就不再只有“细粒度主体组合 / 粗角色分工”两层，而能看见中间这层更贴近主家庭树推进的比较口径。
- `主体牵制角色原型矩阵`：当上一步已经能稳定得到细粒度主体原型后，再跑 `window.__DEMO.inspectTensionRoleArchetypeCoverage()`。这里继续把现有牵制结构压成 `家内/家门主体 / 安身主体 / 外部门路` 三层角色矩阵，只比较角色分工是否跨路线站稳，不替任何路线补新事实；只有把“主体组合比较”再推进成“角色分工比较”，后续才更容易判断某条主家庭树是在补整局模板，还是只在补局部句式。
- `名额转嫁承接分流/层级矩阵`：当 `payload.quotaTransfers[]` 已经具备 `branchKind / transferTier` 这两层结构后，再跑 `window.__DEMO.inspectQuotaTransferBranchCoverage()` 与 `window.__DEMO.inspectQuotaTransferTierCoverage()`。前者把同一组边界反查成 `躲丁 / 买丁 / 托情 / 病身免役` 四条留乡主分流的比较矩阵，后者再继续反查成 `首轮转嫁 / 二次再转嫁` 两层共享模板；只有这样，后续才不会把“苏北留乡里有 17 条相关场景”误读成“这组 unknown 已经被系统性比较完了”。
- `苏北留乡主分流总览`：当 `payload.quotaTransfers[]` 与 `payload.quotaRippleResponses[]` 都已经稳定后，再跑 `window.__DEMO.inspectQuotaRippleResponseBranchCoverage()` 与 `window.__DEMO.inspectSubeiStayUnknownAtlas()`。前者把 `名额后账回应边界` 也压回 `买丁 / 托情 / 病身免役 / 躲丁` 四条留乡主分流，后者再把它与 `inspectQuotaTransferBranchCoverage()` 叠成一张总表，直接看每条留乡分流当前是“只稳定命中名额转嫁承接边界”，还是已经同时命中 `名额转嫁承接 + 名额后账回应`；只有把两组 structured unknown 真正放进同一张分流总览，后续才更容易判断 `苏北贫农家` 这棵树剩下的到底是哪一层。
- `名额转嫁证据来源矩阵`：当 `payload.quotaTransfers[]` 已经稳定带出 `recordedBy` 后，再跑 `window.__DEMO.inspectQuotaTransferRecordedByCoverage()`。这里要把同一组边界继续反查成 `村里名册 / 村里口风 / 保长差事` 三类共享来源，并确认这些来源不只是字段里出现过，而是都被主链矩阵与自然回放双层锁住；只有这样，后续才不会把“名额转嫁已有证据字段”误读成“这组证据来源已经被系统性比较完了”。
- `名额转嫁来源原型矩阵`：当上述三类字面来源已经全部稳定后，再跑 `window.__DEMO.inspectQuotaTransferSourceArchetypeCoverage()`。这里要继续把 `村里名册 / 村里口风 / 保长差事` 保守抽成 `村里名目与口风 / 保长差事后账` 两类共享来源原型，并确认这些原型也被主链矩阵与自然回放双层锁住；只有这样，后续才不会把“字面来源不同”误读成“这组名额转嫁边界还不能上升到更高一层可比较模板”。
- `名额后账回应矩阵`：当 `payload.quotaRippleResponses[]` 已经具备 `rippleKind / rippleTier` 这两层结构后，再跑 `window.__DEMO.inspectQuotaRippleResponseKindCoverage()` 与 `window.__DEMO.inspectQuotaRippleResponseTierCoverage()`。前者把同一组边界反查成 `闭门不问 / 递小礼 / 跑差补账` 三类回应的比较矩阵，后者再继续反查成 `首轮后账 / 二次后账` 两层共享模板；只有这样，后续才不会把“名额后账已经被字段化”误读成“这组回应姿态已经被系统性比较完了”。
- `名额后账来源矩阵`：当 `payload.quotaRippleResponses[]` 已经进一步稳定带出 `recordedBy / recordedByArchetypes` 后，再跑 `window.__DEMO.inspectQuotaRippleResponseRecordedByCoverage()` 与 `window.__DEMO.inspectQuotaRippleResponseSourceArchetypeCoverage()`。前者要确认 `家门里人的猜测 / 村里口风 / 对方神色 / 保长差事` 这组字面来源都被主链矩阵与自然回放双层锁住，后者再确认 `门里门外的人情口风 / 差事补账后的后账口风` 这两类来源原型也已被双层锁住；只有这样，后续才不会把“名额后账已有回应矩阵”误读成“这组后账来源已经被系统性比较完了”。
- `制度路线矩阵`：`window.__DEMO.inspectInstitutionRouteCoverage()` 把 `payload.institutionDetails[]` 再按主路线桶收成 route 级摘要，显性回答“哪些主路线已经真正接上制度边界层、各自挂着哪类制度摘要、是否已被主链矩阵与自然回放双层锁住”。它不新增制度细节，只把制度 unknown 的迁移结果从全局总数推进成路线级可比资产。
- `地方兵役参数边界层`：当 `保甲摊派与免役认定口径` 已经稳定横跨 `苏北留乡 / 苏北士兵 / 苏北逃亡` 三条主路线时，不应继续只让它停在 `institution-detail` 与 missing-evidence 矩阵里。应进一步拆成独立 field group `draft-parameter-boundary`，并用 `payload.draftInstitutionBoundaries[]` 显性返回 `routeKind / currentPressureLabel / currentResponseLabel / responseTier / recordedBy / evidenceLabels / missingEvidenceLabels`；随后再用 `window.__DEMO.inspectDraftInstitutionBoundaryCoverage()` 锁主链矩阵 + 自然回放样板，并用 `window.__DEMO.inspectDraftInstitutionRouteCoverage()` 做 route 级比较。
- `地方兵役回应层级矩阵`：当 `payload.draftInstitutionBoundaries[]` 已经稳定带出 `responseTier` 后，再跑 `window.__DEMO.inspectDraftInstitutionTierCoverage()`。这里要把同一组边界继续反查成 `缓过一回 / 被带走 / 离乡避征 / 周旋未稳` 四层共享模板，并确认这些层级不只是字段函数里存在，而是都被主链矩阵与自然回放双层锁住；只有这样，后续才不会把“地方兵役参数边界已有 route 摘要”误读成“抓丁回应层级已经比较完了”。
- `地方兵役回应原型矩阵`：当 `payload.draftInstitutionBoundaries[]` 已经进一步稳定带出 `responseArchetype` 后，再跑 `window.__DEMO.inspectDraftInstitutionResponseArchetypeCoverage()`。这里不再只问四层字面 `responseTier` 是否都已存在，而是继续把它们保守收成 `留在门口周旋型 / 被逼离场改线型` 两类回应原型，并确认这些原型都被主链矩阵与自然回放双层锁住；只有这样，后续才不会把“字面层级各停一路”误读成“这组回应完全不能再抽成整局模板”。
- `地方兵役证据来源矩阵`：当 `payload.draftInstitutionBoundaries[]` 已经稳定带出 `recordedBy` 后，再跑 `window.__DEMO.inspectDraftInstitutionRecordedByCoverage()`。这里要把同一组边界继续反查成 `村里传到门口的告示与口风 / 保甲点验桌前后账 / 一家离乡前仓促应对后账` 三类共享来源，并确认这些来源不只是字段数组里出现过，而是都被主链矩阵与自然回放双层锁住；只有这样，后续才不会把“地方兵役参数边界已有 recordedBy”误读成“抓丁压力的可观察来源已经比较完了”。
- `地方兵役来源原型矩阵`：当上述三类字面来源已经全部稳定后，再跑 `window.__DEMO.inspectDraftInstitutionSourceArchetypeCoverage()`。这里要继续把 `recordedBy` 抽成 `村里门口口风 / 抓丁临门后的应对后账` 两类共享来源原型，并确认这些原型不只是说明文档里的人工归纳，而是都被主链矩阵与自然回放双层锁住；只有这样，后续才不会把“字面来源仍有桌前/离乡差异”误读成“这组制度边界还不能跨路线复用模板”。
- `金圆券保全边界层`：当共享制度缺口已经明确显示 `金圆券后哪些资产/门路真保住 / 硬货、厂壳、口粮账怎样继续折价或清算` 横跨 `工商家男线 / 工商家女线` 两条主路线时，不应继续只让它停在 `institution-detail` 与 missing-evidence 矩阵里。应进一步拆成独立 field group `gold-preservation-boundary`，并用 `payload.goldPreservationBoundaries[]` 显性返回 `routeKind / currentPreservationLabel / currentReckoningLabel / recordedBy / evidenceLabels / missingEvidenceLabels`；随后再用 `window.__DEMO.inspectGoldPreservationBoundaryCoverage()` 锁主链矩阵 + 自然回放样板，并用 `window.__DEMO.inspectGoldPreservationRouteCoverage()` 做 route 级比较。
- `金圆券保全种类/层级矩阵`：当 `payload.goldPreservationBoundaries[]` 已经稳定带出 `preservationKind / reckoningTier` 这两层结构后，再跑 `window.__DEMO.inspectGoldPreservationKindCoverage()` 与 `window.__DEMO.inspectGoldPreservationTierCoverage()`。前者把同一组边界反查成“藏退路 / 先清账发薪 / 先死保厂壳 / 先接家口 / 先守公共门面 / 先守借住去路 / 先守熟手小作”七种已进统一验收的保全姿态，后者再把它反查成 `退路暗账层 / 外壳苦撑层 / 口粮安身重摊层` 这组共享后账层级；只有这样，后续才不会把“工商家男女线各有 route 摘要”误读成“这组时代冲击已经被系统性比较完了”。
- `金圆券保全原型矩阵`：当 `payload.goldPreservationBoundaries[]` 已经稳定带出七类 `preservationKind`，但主家庭树层仍需要一个更高一层的比较口径时，再跑 `window.__DEMO.inspectGoldPreservationArchetypeCoverage()`。这里要继续把细种类保守抽成 `先护退路/安身去路 / 先撑厂壳/公共门面 / 先把人账/口粮账接住 / 先守熟手小作活路` 四类保全原型，并确认这些原型同样被主链矩阵与自然回放双层锁住；只有这样，后续才不会把“种类矩阵已经很满”误读成“主家庭树层已经足够回答哪类保全姿态在跨路线复用”。
- `金圆券保全证据来源矩阵`：当 `payload.goldPreservationBoundaries[]` 已经稳定带出 `recordedBy` 后，再跑 `window.__DEMO.inspectGoldPreservationRecordedByCoverage()`。这里先把同一组边界反查成 `顾家家门与账房在风暴里的口风 / 金圆券后残账与工友安置留下的后账 / 顾家家门在风暴里的口风 / 女工家口与安身网络在风暴里留下的后账` 四类字面来源，并确认这些来源不只是字段数组里出现过，而是都被主链矩阵与自然回放双层锁住。
- `金圆券来源原型矩阵`：当上述四类字面来源已经全部稳定后，再跑 `window.__DEMO.inspectGoldPreservationSourceArchetypeCoverage()`。这里要继续把男女线仍保留差异的 `recordedBy` 抽成 `顾家家门口风 / 风暴后家口工友安身后账` 两类共享来源原型，并确认这些原型不只是说明文档里的人工归纳，而是都被主链矩阵与自然回放双层锁住；只有这样，后续才不会把“字面来源仍不同”误读成“这组时代冲击还不能跨路线复用模板”。
- `制度缺口矩阵`：当制度路线矩阵已经存在后，再跑 `window.__DEMO.inspectInstitutionMissingEvidenceMatrix()`，把 `route -> institutionDetails[]` 反查成 `missingEvidenceLabel -> routes`。只有把共享缺口显性列出来，后续才能优先补“跨多条主路线共用”的制度边界，而不是又回到某一家某一枝局部修边。
- `shared unknown 跨组比较器`：当 `unknown 台账` 的 top3 已不再只是“场景数看起来很多”，而是都已推进到各自的 `recordedBy` 层后，再跑 `window.__DEMO.inspectSharedUnknownComparison()`。这里要把 `地方兵役参数边界 / 金圆券风暴保全边界 / 名额转嫁承接边界` 统一比较成三类成熟度：`跨路线共享来源 / 跨路线可比较但来源未共享 / 单路线内共享来源`；其中像 `地方兵役参数边界 / 金圆券风暴保全边界` 这样已经抽出“来源原型矩阵”的，应按原型层成熟度比较，同时继续保留字面 `recordedBy` 的路线差异，避免把“来源原型已共享”误写成“字面来源已完全并同”。
- `结构化 unknown 资产总表`：当 top3 shared unknown 之外，`多年确认真相边界 / 多年确认后续去向边界 / 名额后账回应边界` 这类字段层也已经稳定成型后，再跑 `window.__DEMO.inspectUnknownStructurePortfolio()`。这里要把当前整局已结构化的 `unknown` 统一压成 `制度与后账边界 / 多年确认边界` 两大桶，并显性回答“哪些边界已跨路线可直接比较、哪些仍只适合 route-local 保守判断”；只有把这层总表做出来，后续才不会继续只围着 top3 shared unknown 打转，或把“单路线字段化”误读成“已经足够当整局模板”。
- `route-local 候选矩阵`：当 `主体牵制原型矩阵 / 主体牵制锚点原型矩阵 / 主体牵制回应原型矩阵 / 主体牵制角色原型矩阵 / 终局收束原型矩阵 / 结构化 unknown 资产总表` 都已经成立后，再跑 `window.__DEMO.inspectRouteLocalTemplateCandidates()`。这里要把仍停在单路线、但已双层锁住的 `主体牵制原型 / 主体牵制回应原型 / 主体牵制角色原型 / 终局收束原型 / 结构化 unknown` 统一按主路线聚合，显性回答“哪条路线已经同时挂着多类候选、哪一类证据最接近下一组整局模板”；只有把这层候选前沿做出来，后续才不容易把“继续保留 route-local”误做成“继续围着单枝修边”。
- `route-local 类别比较层`：当 `route-local 候选矩阵` 与 `route-local 升格信号矩阵` 都已经成立后，再跑 `window.__DEMO.inspectRouteLocalCategoryComparison()`。这里不再先问“哪条路线最密”，而是把 `主体牵制原型 / 主体牵制锚点原型 / 主体牵制回应原型 / 主体牵制角色原型 / 终局收束原型 / 结构化 unknown` 这些仍在前沿的证据统一比较成 `已出现共享来源锚点 / 已出现可比较锚点 / 仍停在单路线候选` 三种成熟度；只有把“先看哪条路线”继续推进成“先看哪一类证据”，后续才更容易决定下一组整局模板究竟该从哪层冒头。
- `route-local 候选优先级矩阵`：当上述三层 route-local 比较都已经成立后，再跑 `window.__DEMO.inspectRouteLocalCandidatePriority()`。这里不直接宣称某个候选已经能升格成整局模板，而是把每个候选所处的**路线锚点层**与**证据类别层**保守合并，显性回答“当前先比哪一个具体候选最可能提升整局完整度”；只有把“路线优先级 / 类别优先级”继续推进到“候选项优先级”，后续才不容易又回到凭印象挑单枝。
- `route-local 候选阻塞矩阵`：当 `route-local 候选优先级矩阵` 已经成立后，再跑 `window.__DEMO.inspectRouteLocalCandidateBlockers()`。这里不再只问“先比谁”，而是显性回答“为什么这项现在还不能升格”，保守区分 `同路线已有更宽共享原型、但字面结果仍只在本路线成立`、`上位 unknown 桶已跨线、但子字段仍是单路线`、`共享类别已成立、但具体主体组合仍只在本路线成立`、`同路线已挂上共享锚点原型，但这组具体主体组合仍是 route-local`、`同路线已挂上共享角色原型，但这组回应姿态仍是 route-local` 等阻塞；只有把“继续保留 route-local 的原因”也结构化，后续才不会把高优先级候选误推进成整局共模。
- `终局收束先归并、后保留 literal`：当若干条主路线的 `后半生安放` 已经在 `主链矩阵 + 自然回放` 双层锁住，而且只是措辞不同、结构却明显指向同一类活法时，应先尝试把它们归并回既有 `家门安身 / 自立谋生 / 时局收束 / 归乡收束 / 外路延展` 原型，而不是长期把它们留在 literal route-local。当前已把 `门内修补 / 收回家门日常 / 后方收住后半生` 归并回 `家门安身型`，把 `守书脉 / 收成小本生计 / 收成合账小网 / 收成会馆外勤活` 归并回 `自立谋生型`，把 `收窄公开位 / 收窄成公共位置` 归并回 `时局收束型`；本轮再把原先过宽的 `战后还在接日子` 拆回 `归乡还在接日子 / 在外还在接日子 / 顺势还在接日子 / 离队后还在接日子 / 返乡还在接日子 / 后方还在接日子` 这组带路径向的保守标签，让它们重新并回既有原型层。只有在仍找不到这类路径向锚点时，才继续保留 literal，不为对称硬并。
- `route-local 升格信号矩阵`：当 `route-local 候选矩阵` 已经成立后，再跑 `window.__DEMO.inspectRouteLocalPromotionSignals()`。这里不再只看“哪条路线候选最多”，而是把这些候选与 `主体牵制原型矩阵 / 主体牵制锚点原型矩阵 / 终局收束原型矩阵 / shared unknown 跨组比较器` 叠在一起，显性区分三类状态：`已有跨路线共享来源锚点`、`已可跨路线比较但来源仍未共享`、`仍主要停在单路线前沿`；只有把“候选多”与“已挂锚点”分开，后续才不容易把一条候选密集但仍缺跨线证据的路线，误当成下一组整局模板的唯一来源。
- `返乡认地折算边界层`：当共享制度缺口已经明确显示 `旧屋/旧地/后路怎样一起折算 / 丈量与认人时怎样重新认你这一路旧账` 横跨多条返乡/归乡主路线后，不应继续只让它们停在 `missingEvidenceLabel` 矩阵里。应进一步拆成独立 field group `workteam-recognition-boundary`，并用 `payload.workteamRecognitionBoundaries[]` 显性返回 `routeKind / currentRecognitionLabel / currentReckoningLabel / reckoningTier / reckoningTierLabel / recordedBy / evidenceLabels / missingEvidenceLabels`；随后再用 `window.__DEMO.inspectWorkteamRecognitionBoundaryCoverage()` 锁主链矩阵 + 自然回放双层样板，用 `window.__DEMO.inspectWorkteamRecognitionRouteCoverage()` 做 route 级比较，再用 `window.__DEMO.inspectWorkteamRecognitionTierCoverage()` 把 route 摘要反查成“账目讲明 / 量力认领 / 留余退身 / 两头并账”层级矩阵。
- `返乡认地证据来源矩阵`：当 `payload.workteamRecognitionBoundaries[]` 已经稳定带出 `recordedBy` 后，再跑 `window.__DEMO.inspectWorkteamRecognitionRecordedByCoverage()`。这里要把同一组边界继续反查成 `工作队进村后的丈量/认人口风 / 家门旧账重新摆上桌后的后账 / 丈量场边重新认人的口风 / 旧街坊与回乡路上续回的消息 / 归乡以后家口与旧地重新接线的后账` 五类共享来源，并确认这些来源不只是字段数组里出现过，而是都被主链矩阵与自然回放双层锁住；只有这样，后续才不会把“返乡认地折算边界已有 route 摘要 + tier 矩阵”误读成“这组可观察来源已经比较完了”。
- `返乡认地来源原型矩阵`：当 `payload.workteamRecognitionBoundaries[]` 已经进一步稳定带出 `recordedByArchetypes` 后，再跑 `window.__DEMO.inspectWorkteamRecognitionSourceArchetypeCoverage()`。这里要把 `工作队进村后的丈量/认人口风 / 丈量场边重新认人的口风` 保守抽成 `丈量场上的认人口风`，再把 `家门旧账重新摆上桌后的后账 / 旧街坊与回乡路上续回的消息 / 归乡以后家口与旧地重新接线的后账` 保守抽成 `旧账/旧路/旧地重新接回后的后账`，用来回答“这组返乡认地边界是否已经共享同一种来源结构，同时仍保留每条返乡线各自怎么记账的字面差异”。这里仍不补丈量口径细数、旧屋归属细节或谁先认哪一笔旧账，只把来源原型结构化。
- `整局总览`：`window.__DEMO.inspectGlobalCompleteness()` 汇总 mixed frontier、外围主体分层、牵制锁定、主体牵制锚点原型、主体牵制回应原型、unknown 台账、route-local 候选前沿、route-local 升格信号与路线闭环，并给出 `recommendedNextFocus`；这张总表现在也要显性返回 `tensionAnchorArchetypeCoverage / tensionResponseArchetypeCoverage`，避免前沿矩阵已经补到“家门锚点层 / 回应姿态层”，整局总览却仍停在“主体组合 / 角色分工”两层。
- `外围主体分层`：`window.__DEMO.inspectPeripheralSubjectCoverage()` 用主路线桶聚合外围主体/外部门路是否过薄；若仍有只显性命中 1 组外围主体的路线，优先补 profile/主体层，而不是回头修已闭合 mixed 句式。
- `外围主体厚度`：除“是否只有 1 组外部门路”外，也看主家庭线是否已经把 `support` 从 `household / father / workers / marriage` 的混合语义里独立出来；若 `沈家 / 工商家` 这类主家庭仍只有 `connections + marriage` 或 `ledger/workers + connections`，优先补出“谁先把人和家门接住”的 `support`，再去扩更细的制度 unknown。
- `unknown 台账`：`window.__DEMO.inspectUnknownLedger()` 只登记边界与证据缺口；按 field group 汇总优先级，决定下一轮“结构化哪个 unknown”更能让整局更完整。这里要避免 `institution-detail` 父桶与其已独立出的子字段组双重抬高同一类缺口，否则 `recommendedFieldGroups` 会偏回“大桶看起来最大”，而不是“哪组模板层最值得继续推进”。
- `同字段多阶段覆盖`：若同一 `unknown` 字段组本身存在 `第一次 / 第二次`、`前战 / 晚段` 这类重复阶段，不应只锁一条代表态；要像当前 `quota-transfer-target` 一样，把“一次转嫁 + 二次转嫁”都并进 `主链矩阵 + 自然回放 + unknown 专门覆盖`，避免字段层看似存在、晚段却没有被系统验收。
- `制度细节双层覆盖`：对 `payload.institutionDetails[]` 里的字段，不只验 `礼钱与规矩 / 惩处口径 / 黑名单机制` 这类旧代表态，也要把新补的 `地方兵役参数边界`、`工作队/返乡重站丈量场时的认人认地口径边界`、`归乡士兵重站丈量场的旧屋/家口承接口径` 与 `上海工商家金圆券风暴保全口径边界` 一并拉进 `主链矩阵 + 自然回放 + unknown 专门覆盖`，确保制度参数不是只在某一个桌前失败节点里才可见。
- `制度路线覆盖`：当制度细节双层覆盖已经成立后，再跑 `window.__DEMO.inspectInstitutionRouteCoverage()`。这里要统一检查**仍以父桶 `institution-detail` 承担**的主路线，是否都已经带出 route 级 `institutionDetailLabels / missingEvidenceLabels`，并确认这些路线不只是 `unknown` 台账里出现过，而是真被主链矩阵与自然回放双层锁住；已拆成独立字段组的制度边界，则转由各自子矩阵负责。
- `support × unknown 联动覆盖`：当 `support` 已从 `household / workers / father / marriage` 里独立出来后，再跑 `window.__DEMO.inspectSupportUnknownLinkCoverage()`。这里要统一检查 `support` 是否已和 `payload.institutionDetails[]` 在同一条主线上共同出现，并继续统计是否已有 `support + longConfirmations[]` 的稳定双层代表态；当前除 `工商家女线 / 难民返乡 / 苏北逃亡返乡 / 归乡士兵` 外，也要把 `苏北留乡 workteam / 分田后账` 一并纳入；若暂无稳定代表态，就继续保留 `unknown`，不要为了对称硬造联动规则。
- `旧路回接落点覆盖`：当 `returnRouteLandings[]` 已经横跨 `海外 / 难民返乡 / 苏北逃亡回乡 / 归乡士兵` 这几类返身路径后，不应继续只跑 `window.__DEMO.inspectReturnRouteLandingEvidenceCoverage()`。还要继续跑 `window.__DEMO.inspectReturnRouteLandingRouteCoverage()`、`window.__DEMO.inspectReturnRouteLandingTierCoverage()`、`window.__DEMO.inspectReturnRouteLandingArchetypeCoverage()`、`window.__DEMO.inspectReturnRouteLandingRecordedByCoverage()` 与 `window.__DEMO.inspectReturnRouteLandingSourceArchetypeCoverage()`，分别确认这组边界是否已经具备 route 级摘要、`试探旧路 / 旧屋试接 / 收脚探口风 / 旧屋旧账回手 / 试路留外 / 两头并压` 这组落点层级矩阵、`试路探口风型 / 旧屋旧账回接型 / 旧路已触，但外路仍压身型` 这组落点原型矩阵、字面来源矩阵，以及保守抽出的 `旧门路/旧街坊递回的消息 / 试路或回身时听来的口风 / 回身以后家门旧账重摆的后账` 这组来源原型矩阵。只有当字段层、路线层、层级层、原型层、来源层都被主链矩阵与自然回放双层锁住，才算这组 shared unknown 真正进入模板层；证据不足处继续保留 `unknown`，不为了对称硬补“最终接住的真相”。
- `support + longConfirmations 字段覆盖`：当 `support + longConfirmations[]` 已经不只是“共现”，而是真能回答“谁把多年后账接住”时，再跑 `window.__DEMO.inspectSupportLongConfirmPlacementCoverage()`。这里要统一检查 `payload.supportLongConfirmPlacements[]` 是否真的在 `主链矩阵 + 自然回放` 里同时带出 `currentPlacementLabel / confirmedFactLabel / currentTruthLabel / recordedBy / evidenceLabels / missingEvidenceLabels`；若其中任一层还要靠读 HUD 或终局句子猜，就还不能算模板层真正闭环。
- `support + longConfirmations 路线矩阵`：当字段覆盖已经站稳后，再跑 `window.__DEMO.inspectSupportLongConfirmRouteCoverage()`。这里不再只问“有没有字段”，而是继续比较哪些主路线已经稳定回答“确认后的后账是由哪层安身主体接住”，并把这组结果推进成 route 级摘要；若某条路线还只锁住单层样板，就优先补主链矩阵或自然回放，而不是回头修单枝句式。

---

## 全局模板迁移矩阵

> 这张表只总结当前仓库里已经真实落地、并且会影响整局排序的主家庭树/主路径。若某一项当前只能确认到“有模板迁入，但具体制度细数或事实边界仍不足”，就继续写 `unknown`，不补想当然细节。

| 主家庭树 / 主路径 | 当前已经稳定迁入的模板层 | 当前最该看的整局缺口 | 当前角色 |
|---|---|---|---|
| `苏北贫农家·主家庭树` | `familySubjects + support + connections + longConfirm + returnRoute + workteam` | 主树本体当前无新增整局缺口；若后续没有新的跨路线复用证据，继续视作成熟样板 | `整局模板母树` |
| `苏北贫农家·留乡主分流` | `quotaTransfers + quotaRippleResponses + support × institution + phase atlas` | 当前两组 structured unknown 已成熟；除非出现新跨路线复用，否则继续留在模板库存 | `成熟库存，不再当前优先` |
| `苏北贫农家·士兵多年线` | `draftBoundary + longConfirm + supportLongConfirm + returnRouteLanding + workteamRecognition` | 仍可作为“多年失联/归来后旧屋旧地怎么接”的共享样板，但当前不是首位缺口 | `共享晚段样板` |
| `苏北贫农家·逃亡回乡线` | `draftBoundary + family household anchor + returnRouteLanding + support × unknown + refugee dual-profile tension` | 仍可服务返乡旧路回接、旧家重新接回与安身承接比较；当前本体无新增主链缺口 | `共享晚段样板` |
| `江南沈家·主家庭树` | `familySubjects + support + connections + tension anchor/support cluster + family atlas` | 当前真正还会改变整局排序的，只剩 `江南沈家·女线：婚家 + 外部门路` 这一个 fine combo；下一步只该核对现有共享来源能否提供第二条真实路线 | `当前唯一 active route-local 所在主树` |
| `上海工商家·主家庭树` | `familySubjects + support + workers/ledger + goldPreservation + tension anchor/support cluster + family atlas` | 当前主要承担城市第二路线库存位：继续校准 `父辈/家门 / 婚家 / 账房工友 + 安身承接 + 外部门路` 这些组合，但没有新 compare-ready 证据前，不再当默认首位前沿 | `城市第二路线库存位` |
| `纱厂女工·主路径` | `ledger + workers + support + connections + longConfirm + city-route phase closure` | 重点仍是充当第二条城市线，为 `上海工商家 / 江南沈家` 的主体牵制与安身承接组合提供真实对照证据；其中 `labor:postwar-ledger / labor:settle-ledger` 现已正式进入 `ledger` 主体，可直接与工商家账房线做 `ledger + workers + connections` 级比较 | `城市路径证据位` |
| `难民/流亡者·主路径` | `family household anchor + support + connections + longConfirm + returnRouteLanding + response archetype` | 当前重点已从“再补一条 late-home literal”转成“维持共享回应原型来源位”，继续服务 `苏北士兵 / 苏北逃亡 / 难民` 三路的共享比较 | `共享回应原型来源位` |
| `共享海外多年轨` | `family household anchor + connections + support + returnRouteLanding + family borrowed dependency` | 继续为 `江南沈家 / 上海工商家` 提供借入晚段模板；当前自身缺口继续保留 `unknown` 即可，但 `家书/家用` 已可按 household anchor schema 参与整局比较 | `共享借入路径` |

### 使用规则

- 若一轮改动不能让上表任意一行的“当前最该看的整局缺口”更清楚，通常就不是当前最值得做的工作。
- 若某条线已经落在 `成熟库存` 或 `共享晚段样板`，默认不再作为首位推进对象，除非它新长出了跨路线证据。
- 若 `江南沈家·女线：婚家 + 外部门路` 仍只拿到“结构偏粗”的双层样例，就继续 keep `route-local`；不要为了清零而跳回 `上海工商家 / 纱厂女工 / 共享海外多年轨` 硬找并不存在的 marriage-only fine combo。
- 若运行时已经明确 `tailFreezeSuggested = true`，就把这格 route-local 固定为“当前无第二路线证据”的保守尾项；后续只在出现新的真实第二路线时再重开，不再把它当默认主战场。
- 若 `上海工商家 / 纱厂女工 / 难民流亡者 / 共享海外多年轨` 没有拿到新的第二路线或第二类共享来源，就继续保守记为 `route-local` 或 `unknown`，并把它们留在库存位而不是误写成首位前沿。
