# 整局剩余主链路总表

> **版本说明（2026-08-12）**：本表保留为旧三家庭／十一领域切片的收口记录。它不覆盖 design-v2.0.0 的十八家庭、四十八领域、八去向和千级内容，旧“整局”措辞不再表示正式产品全部范围。当前产品设计入口见 `当前权威设计.md` 与 `完整版内容规模与生产蓝图.md`；代码是否已实现，以 demo 仓库当前版本与验证证据为准。

> 这份总表的目标不是复述所有已写剧情，而是回答三件事：
> 1. 整个游戏目前已经“做到了哪”
> 2. 还差哪些**主链路 / 主家庭树 / 规则层**
> 3. 下一轮推进应该优先补哪一块，才能真正让**整局更完整**
>
> 口径约束：
> - 只基于当前项目里已经真实存在的 `demo.html`、`assets/*.js`、`README.md`、`整局主链系统看板.md`、`持续推进记录.md` 汇总。
> - 不补写不存在的剧情、制度细数或史实。
> - 凡当前运行时或文档里仍未确认的地方，一律继续保留 `unknown`。

## 当前整体判断

- 当前已不是“只有一棵苏北样板树”的阶段，而是进入了**整局模板收口阶段**。
- 最近高频推进整体仍在朝“整局做完”前进，但确实出现过一段**短时漂移**：`2026-08-10` 的第 `238-239` 轮连续围绕 `江南沈家 / 上海工商家` 扩同类 `postwar mixed`，虽然都是真实可回归资产，却已经逼近“在城市家树成熟模板附近反复修边”的边缘。第 `240` 轮重新转去补 `难民/流亡者 / 苏北逃亡线` 的第二来源模板，才把主任务拉回“服务整局排序变化”的方向。
- 当前还存在一层**排序口径漂移**风险：轻量行动板（`inspectGlobalClosureActionBoard()` / `inspectRouteLocalDecisionBoard()`）适合回答“现在该做对照还是该补阶段”，但它的动作桶/摘要口径不应替代 route-local 候选的细排位。当前更可靠的细排位入口，是直接读取 `inspectRouteLocalCandidatePriority()` 与 `inspectRouteLocalCandidateBlockers()`：它们会显式告诉你“哪一条路线的哪一组候选最前、为什么还不能升格”。
- 最新可信“最后一格前沿”口径，已由本轮轻量直读再收紧一步：`window.__DEMO.inspectTopRouteLocalCandidateBundle()` 当前对 `江南沈家·女线：婚家 + 外部门路` 返回 `tailFreezeSuggested = true`、`matchedCandidateMissingCount = 4`、`familyComboPeerRouteCount = 0`。因此整局现在不再适合把它记成“当前唯一 active 前沿”，而应记成“当前无第二路线证据”的**保守尾项**；在未来出现新的真实 `peer route` 之前，不应为了清零去新编 marriage-only late-line。
  - 当前可直接对照的路线与样例：`上海工商家·男线`、`上海工商家·女线`、`苏北贫农家·士兵多年线`（样例 id 详见第 `259` 轮记录）
- 整局骨架层面，已经有：
  - 出生家庭 → 主路径 的两层模型
  - 三个主家庭入口
  - 多条主路径的前提 → 选择 → 跨年后果 → 终局事实回收闭环
  - 时代信息系统
  - 主体回应与死亡确认
  - HUD / 终局 / 回归脚本的系统级验证入口
- 当前更像“剩余的 10% 工作量在决定整局最终是否稳”，而不是“还差一大片完全没做”；也正因为如此，优先级一旦失真，就很容易看起来还在高频推进，实际却只是在成熟模板附近打转。

## 九行补完矩阵

> 这张矩阵不追求把每条线写得更细，而是用同一口径回答：这 9 行主家庭树/主路径里，哪些已经完成“五段闭环”，哪些当前只是库存位，哪些仍会真实改变整局排序。

| 行 | 五段闭环 | 当前角色 | 当前剩余重点 | 当前不该做 |
| --- | --- | --- | --- | --- |
| `苏北贫农家·主家庭树` | 已闭环 | 整局模板母树 | 继续提供 `状态字段 / 规则层 / 节点模式 / 验证方式` 的母模板，不再承担默认主战场角色 | 不再围着已闭合枝条反复修边 |
| `苏北贫农家·留乡主分流` | 已闭环 | 模板库存来源位 | `名额后账回应边界 / 名额转嫁承接边界` 继续作共享来源，不再算 active 前沿 | 不把已成熟 structured unknown 重新当首位缺口 |
| `苏北贫农家·士兵多年线` | 已闭环 | 对照库存位（compare-ready） | 以第 `259` 轮为准：当前已进入 `inspectTopRouteLocalCandidateBundle()` 的可直接对照列表 | 不在缺证据时继续长同类 years mixed 新枝 |
| `苏北贫农家·逃亡回乡线` | 已闭环 | 共享晚段样板 | 继续提供 `返乡试路 / 旧屋旧账 / 后方活路未退净` 的对照来源 | 不把它重新写回留乡通用主干 |
| `江南沈家·主家庭树` | 已闭环 | 保守尾项所在主树 | `江南沈家·女线：婚家 + 外部门路` 当前已确认 `tailFreezeSuggested = true`，应保守记为“当前无第二路线证据” | 不为清零而硬写新的 marriage-only mixed |
| `上海工商家·主家庭树` | 已闭环 | 对照库存位（compare-ready） | 以第 `259` 轮为准：`上海工商家·男线 / 女线` 当前已进入可直接对照列表；继续作为现成对照库存 | 不在没有新证据时继续扩同类城市 mixed |
| `纱厂女工·主路径` | 已闭环 | 城市路径证据位 | 继续为城市劳动线提供 `工友/家口 / ledger / support / connections` 的真实第二路线证据 | 不强拉成当前唯一 blocker 的替代路线 |
| `难民/流亡者·主路径` | 已闭环 | 共享回应原型来源位 | 已并入 `门内接回一点，外头活路未退净，两头日子先并着走` 共享回应 archetype，后续主要提供第二来源证据 | 不再把已归并 literal 继续当 active route-local |
| `共享海外多年轨` | 已闭环 | 共享借入路径 | 继续给 `江南沈家 / 上海工商家` 提供借入晚段模板；证据不足处继续保留 `unknown` | 不在当前没有 compare-ready 新证据时硬升为前排 |

## 已经比较稳的部分

### 苏北贫农家

- 已经具备完整样板树地位。
- 已落地的核心主枝包括：
  - 留乡主线
  - 抓丁主树
  - 士兵路径
  - 逃亡 / 难民路径
  - 病母 / 妻子的主体回应
  - 死亡确认
  - 1949 事实回收
- 当前不宜再把苏北树当成默认主战场，只在它仍会改变整局排序时继续推进。

### 时代系统

- 已有真实时代大事历。
- 已接入买报 / 读书 / 听书 / 闲聊的知情渠道。
- 已实现“知道才知道为什么被时代冲击”的信息不对称。

### 结局口径

- 已从“成功 / 安稳 / 挣扎”这类价值判断，改为**事实回收**。
- 这条口径应视为整局硬约束，不应再倒回评分结局。

## 仍待补完的主块

### 第一优先：保守尾项固定口径

最新真实运行时校验后，当前 route-local 候选优先级虽然仍会把 `江南沈家·女线 / 婚家 + 外部门路` 排在最前，但它已不再适合被视作 active candidate。轻量直读已确认：

- `tailFreezeSuggested = true`
- `matchedCandidateMissingCount = 4`
- `familyComboPeerRouteCount = 0`

这说明整局当前已不再被 `难民/流亡者` 或 `苏北士兵多年线` 的晚段回应原型卡住；剩下的也不是“再补一条 years mixed”，而是把沈家女线这格明确固定为“当前无第二路线证据”的保守尾项。

为避免后续又回到“围着沈家女线手工翻多张表”的旧节奏，运行时现补入 `window.__DEMO.inspectTopRouteLocalCandidateBundle()`，专门把当前最前一格 blocker 的 `优先级 / 阻塞 / 对照位 / 本路线样例 / 候选级证据阶段 / 下一问` 收成一束。优先建议在 `demo.html` 控制台直读它；本总表不把 Node smoke 命令本身当作“必须执行”的默认步骤。

若这束结果已经把 blocker 写死为 `shared-family-combo-template-but-local-detail`，下一步不要直接跳去写新剧情，而应先读 `window.__DEMO.inspectTensionFamilyComboDetailCoverage()`：它会把 `粗主家庭比较模板` 与 `exact 细主体组合` 拆开，直接回答“沈家女线这格 `婚家 + 外部门路` 当前到底是没有第二条细组合路线，还是第二路线已存在、只是还停在比较吸收前”。

本轮低成本直读已经把这点从“若继续保持”推进成“当前已成立”：`江南沈家·女线：婚家 + 外部门路` 在细模板账板里仍是 `peerRoutes = 0`，而 `inspectTopRouteLocalCandidateBundle()` 已同步返回 `tailFreezeSuggested = true`。因此现有 `上海工商家·男/女线` 与 `苏北士兵多年线` 仍更适合被读作“对照库存位”，而不是这组 exact combo 已经具备的第二路线证据；当前正确动作就是固定为保守尾项，而不是继续围着成熟库存反复修边。

### 第二优先：上海工商家 · 主家庭树

它当前不再是 active 前沿，但仍是最重要的**对照库存**。`inspectRouteLocalCandidateBlockers()` 已不再把它列为 active blocker 首位，可是它现成挂着的几组共享主家庭比较模板，仍是判断沈家女线是否能拿到第二路线证据的最直接参照：

- `上海工商家·男线`：`父辈/家门 + 安身主体/工友/家口 + 外部门路`
- `上海工商家·男线`：`账房/安身主体/工友/家口 + 外部门路`
- `上海工商家·女线`：`婚家 + 安身主体/工友/家口`

这说明上海工商家当前最需要的不是继续长新枝，而是作为现有库存回答：

- 判断这些组合是否终于拿到了第二条真实路线
- 判断是否已经出现新的第二类共享来源
- 若都没有，就继续明确保留 `route-local`

### 第三优先：苏北贫农家 · 士兵多年线 / 逃亡回乡线 / 难民流亡者

这一组当前已经不再是 active blocker，而是本轮刚刚完成归并的共享回应原型来源位。真实运行时里，`inspectTensionResponseArchetypeCoverage()` 现在已经把：

- `苏北贫农家·士兵多年线`
- `苏北贫农家·逃亡回乡线`
- `难民/流亡者·主路径`

三路都并进了 `门内接回一点，外头活路未退净，两头日子先并着走`。因此这组当前正确做法是：

- 继续保留为共享来源模板位
- 不再单独把相关晚段 literal 当 active route-local
- 只有出现新的跨路线证据差，才重新升回前排

### 第四优先：纱厂女工 / 难民流亡者 / 苏北逃亡线

这一组当前更适合作为**第二路线/第二来源库存**，而不是新的绝对前沿。

- `纱厂女工·主路径` 的 `工友/家口 + 外部门路` 仍是稳定对照位
- `难民/流亡者` 与 `苏北逃亡线` 当前更重要的价值，已经从“补 years mixed”转成“提供共享回应 archetype 的现成证据”
- 这几条线当前的价值在于改变排序判断，而不是各自继续独立扩同类 mixed

### 第五优先：运行时验证链路校准

根据仓库根目录的 `README.md`、`持续推进记录.md` 与本轮真实 Node 巡检，当前验证口径存在三处不能忽视的漂移：

- 行动板（`inspectGlobalClosureActionBoard()`）与候选细排位（`inspectRouteLocalCandidatePriority()` / `inspectRouteLocalCandidateBlockers()`）回答的是不同问题；若直接拿行动板首行当“细排位首位”，就会把动作桶摘要误写成 route-local 候选的真实顺序。
- 本轮 `demo.html` 轻量直读已确认 `inspectGlobalClosureChecklist({ includeRows:false }).closureReadyRowCount = 9 / 9`，说明整局闭环层面已不再缺“树体/路径体”。
- 同一轮（见 `持续推进记录.md` 第 `259` 轮）已补入 `inspectTopRouteLocalCandidateBundle()` 作为最后一格 blocker 的首要事实来源：当前可对照路线已明确为 `上海工商家·男线 / 女线 / 苏北士兵多年线`，但仍只有 `compare-only`，`exact = 0 / skeleton = 0`。因此下一轮若继续推进，应优先回答“为何仍只到 compare-only、缺口在 HUD 键对齐还是主体细组合吸收”，而不是为了清零去新增剧情。

这不代表主链断了，但意味着“最新排序”和“最新百分比”不能再无保留地照抄 README 文案。  
因此技术校准虽不是内容主任务，也应作为配套优先项持续保留。

## 规则层仍未完全统一的部分

### 主体规则 schema

当前已经有：

- 病母规则
- 妻子规则
- 家庭主体规则
- 补充主体规则

但这几类规则仍不是完全同一套 schema。  
这意味着项目现在“能跑”，但后续扩展新家庭 / 新主体时，维护成本还可以继续下降。

这是**全游戏层面**的剩余工作，不属于某一棵树；但它目前不应压过前述三类主链判断。

### 成熟模板库存 vs 当前前沿

当前系统已经开始区分：

- 哪些 shared bucket 已经成熟
- 哪些仍是当前待推进前沿

这层区分非常重要，后续需要继续守住：

- 不把已经成熟的 shared template 反复当成“当前缺口”
- 不把仍只有单路线证据的东西硬抬成共享模板

## 整体推进顺序建议

### 现在应该优先做

1. `江南沈家·女线` 的 `婚家 + 外部门路` 真比较：先只核对 `inspectTopRouteLocalCandidateBundle()` 给出的对照位（第 `259` 轮为 `上海工商家·男线 / 上海工商家·女线 / 苏北贫农家·士兵多年线`），判断它们与沈家女线共享的 HUD 键与主体细组合到底差在哪，而不是新编 marriage-only mixed
2. 若核对后仍只能得到“可比较但不够同键/同骨架双层对照”，就明确继续 keep `route-local`，不要再默认跳回成熟模板里硬找不存在的 fine combo
3. `上海工商家·主家庭树`、`纱厂女工 / 共享海外多年轨 / 其他现成路线` 继续作为第二路线/第二来源库存保留
4. `苏北士兵 / 苏北逃亡 / 难民流亡者` 维持这轮已完成的共享回应 archetype 归并结果
5. 运行时验证链路校准
6. 主体规则统一 schema

### 现在不该默认优先做

- 再回头反复细修苏北已闭合节点
- 把已成熟的 shared unknown 继续当主前沿
- 继续在 `support-core / support-cluster / anchor-connection / family-combo` 这条已成熟的主家庭比较梯子上叠新层
- 再连续多轮围绕 `上海工商家 / 江南沈家` 追加同类 `postwar mixed`，却没有带来新的第二路线或第二来源
- 只为了“对称感”去补没有证据支撑的跨线共模

## 什么时候算“整局设计做完”

不是所有句子都写到极致，而是以下几件事成立：

1. 主家庭树都已具备可运行的完整闭环
2. 主路径都有前提 → 选择 → 跨年 → 长期 → 终局回收
3. 主体规则、死亡确认、时代信息等整局规则层稳定
4. HUD / 终局 / 回归脚本对主要线路都能给出一致事实层
5. 当前前沿只剩局部 `route-local` 微调，而不再存在“整棵树还没站起来”的情况

## 接下来建议

- 主任务继续保持高频推进，但每轮都先回答：
  - 这一步是不是让**整局**更完整？
  - 还是只让某一枝更细？
- 若下一轮仍主要落在 `沈家 / 工商家` 的同一批主家庭比较模板附近，应先把“为什么这轮确实拿到了新的第二条真实路线证据或第二类共享来源”写明，再决定是否继续推进；否则宁可继续保留 `route-local`。
- 若下一轮继续推进 `江南沈家·女线`，必须先说明新证据到底来自哪条已有路线；若没有，就明确 keep `route-local`，不要为了清零而写新的 marriage-only late-line。
- 巡检任务负责定期刷新本总表和整局看板，防止主任务再次在单一成熟模板附近打转。

## 备注

- 本表是“整局剩余主链路”的总入口，不替代 `整局主链系统看板.md` 或 `持续推进记录.md`。
- 看板负责排序，推进记录负责细节，这张表负责把“还差什么”说成人能快速判断的清单。
