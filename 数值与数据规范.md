# 《民国人生》数值与数据规范

版本：`1.4.1 / current-schema-4 / target-schema-5`

适用范围：文字版验证原型与后续正式游戏

本规范把设计意图收成可实现、可存档、可迁移、可测试的数据契约。正式游戏可以更换引擎、界面和表现形式，但不应绕过这里定义的身份连续性、主体独立、信息边界与事实账本。

## 一、设计原则

1. 数值只描述角色此刻拥有的条件，不给人生价值排名。
2. 路径由长期行动和关键抉择形成，不能由某个属性达到阈值后自动替玩家选择。
3. 家庭成员、配偶、工友和联系人拥有独立状态，不能被折算成主角的一项“好感度资源”。
4. 历史事件改变可行动空间，但不保证所有角色都知道事件名称和原因。
5. 每个年度都必须留下生活叙事；重大事件之外的年份不能成为空白跳时。
6. 所有会进入终局的判断必须先进入事实账本，不允许结局临时补写。
7. 路线、婚姻、迁移、经营和疾病等抽象状态必须有具体生活实体，不能只依靠一段总结文案。

## 二、基础数值域

除精神外，角色能力与资源统一使用 `0—100` 的闭区间。任何结算都必须经过截断函数：

```text
clamp(value) = min(100, max(0, value))
```

### 六维能力

| 字段 | 含义 | 低值表现 | 高值表现 |
| --- | --- | --- | --- |
| `body` | 体魄 | 难以承担重体力与长途迁徙 | 更能承受劳作、疾病和行路 |
| `learning` | 学识 | 只能接触口耳信息 | 可阅读报刊、理解制度文字 |
| `craft` | 手艺 | 工作替代性高 | 拥有更具体的营生与议价空间 |
| `mind` | 心智 | 压力下行动空间收窄 | 更能维持判断与长期安排 |
| `network` | 人脉 | 消息与门路稀薄 | 可获得介绍、互助和异地落点 |
| `reputation` | 声望 | 他人缺乏信任依据 | 在特定圈层有可调用的信用 |

### 四类资源

| 字段 | 含义 | 注意事项 |
| --- | --- | --- |
| `money` | 可支配钱财 | 不代表资产、生产资料或币值稳定性 |
| `health` | 当前健康 | 归零不自动等于死亡，需进入独立生命事件 |
| `relations` | 可兑现的关系账 | 不能替代具名联系人及其个人回应 |
| `situation` | 当下处境余地 | 综合表示住所、证件、工作连续性和安全空间 |

### 精神与行动预算

- `spirit` 是跨年度行动预算，范围 `0—12`，开局为 `12`。
- 每次完成年度结算后恢复 `6`，但不超过 `12`。
- 当前普通行动消耗 `1—5`；玩耍与休息用负消耗恢复精神。
- 精神不足时不是角色“失败”，而是该年不能继续安排额外主动行动。
- 当年结算后精神不高于 `2` 时，健康额外降低 `2`，用于表达长期透支。

## 三、变化量与门槛

### 变化量级

| 变化 | 推荐范围 | 用途 |
| --- | --- | --- |
| 轻微 | `±1—3` | 一次普通行动、一次通信、一次小额支出 |
| 明显 | `±4—8` | 一年持续学习、换工、疾病或迁徙准备 |
| 重大 | `±9—15` | 灾害、战争、失去营生、建立稳定职业 |
| 结构变化 | 不用单一数值表达 | 死亡、婚姻、迁徙、入伍、失联、资产制度变化 |

结构变化必须由状态、事实和叙事共同表达，不能用“健康 -100”或“处境 +30”代替。

### 行动门槛

行动门槛只回答“此刻是否具备尝试条件”，不能提前保证结果。推荐写法：

```text
available = 年龄条件
  && 场景条件
  && 所需精神
  && 至少一个现实门路
```

不得使用的写法：

```text
learning >= 60 => 自动成为读书人
money >= 70 => 自动保住工厂
relations >= 50 => 配偶自动同行
```

### 重复行动递减

同一年重复安排同一行动时，数值收益按 `1 / (1 + 0.35 × 已执行次数)` 递减。前三次约为 `1.00 / 0.74 / 0.59`；跨年后重新计算。

事实、联系人变化和剧情解锁不做机械折半；它们按各自条件只结算一次。

## 四、统一数据实体

### 角色身份 `identity`

```js
{
  name: "string",
  gender: "男 | 女",
  born: 1910,
  familyKey: "subeipoor | jiangnanshen | shanghaigongshang",
  familyName: "string",
  place: "string"
}
```

出生后 `name`、`born`、`familyKey` 不可因换路径而重建。路径使用身份对象之外的 `routeKey` 与 `routeHistory[]`，避免换路时重建角色。正式产品增加自定义性别时，应扩展枚举并保持旧档兼容。

### 独立主体 `subject`

```js
subjects: {
  mother: {
    label: "母亲",
    status: "alive-sick | alive | dead-unconfirmed | dead-confirmed",
    health: 4,
    agency: 72,
    note: "有自己的去留与求医判断"
  }
}
```

主体 key 就是稳定 id；不同主体可按需要拥有 `health` 或 `strength`。任何会进入终局的主体变化都要写入事实账本。`unknown` 是合法状态，不能为了结局整齐而自动补全。

### 具名联系人 `contact`

```js
contacts: {
  contact_id: {
    label: "string",
    role: "string",
    status: "nearby | distant | missing | coworker | colleague | traveling",
    relation: 0,
    agency: 70,
    note: "这个人自己的责任和边界"
  }
},
contactHistory: [{ year: 1934, contactKey: "contact_id", effect: { relation: 1 } }]
```

联系人不是统一关系点数的皮肤。正式版至少还需记录其所在地、个人目标、可提供帮助、拒绝条件和是否仍有行动能力。

### 公共生活 `publicLife`

```js
publicLife: {
  status: "unaffiliated | public-participant | peripheral-helper | network-member | discreet-liaison | inactive | withdrawn | statement-under-pressure",
  affiliationType: "none | public-local-network | secret-civic-network | nonparty | withdrawn",
  organizationKey: "fictional-organization-key | null",
  publicRole: "string",
  secrecy: "open | limited | discreet",
  familyKnowledge: "none | partial | full",
  coverRole: "string | null",
  contactKey: "string | null",
  trust: 0,
  exposure: 0,
  coercion: 0,
  lastUpdate: "string",
  history: []
}
```

`organizationKey` 只能引用带明确虚构标记的本作合成组织，不能保存真实党派键。真实事件和组织名称另由史实注册表与 `historicalReferenceTags` 管理，只供编剧和审校核对，不生成玩家身份。`trust / exposure / coercion` 是内部触发数值，普通界面只展示定性状态。每条 `history` 至少保存 `year / source / status / affiliationType / organizationKey / role / secrecy / familyKnowledge / exposure / coercion / text`。

### 事实 `fact`

```js
{
  id: "stable-fact-id",
  year: 1937,
  kind: "identity | route | subject | life | era | decision | ending",
  source: "decision-or-event-id",
  text: "只陈述已发生或已确认的事实"
}
```

事实 id 一经发布不得改变含义。文案可以校订，但同一 id 不能从“听说”改成“亲历”。角色知情边界另存于 `knownEvents[]` 与 `unknownImpacts[]`；正式产品可在不破坏现有字段的前提下为单条事实增加 `certainty` 和 `visibility`。

### 年度叙事 `annualNarrative`

```js
{
  year: 1928,
  id: "ordinary-event-or-rhythm-id",
  title: "string",
  kind: "scene | rhythm",
  text: "这一年实际发生的生活内容"
}
```

从出生年份到主角死亡，每个实际推进年度必须恰好写入一条主要年度生活叙事；历史事件、抉择和行动可额外记录，但不得占用这一条个人日常。

### 1949 后处境 `post1949` 与生命状态 `life`

```js
{
  post1949Choice: "mainland | hong-kong | taiwan | overseas | in-motion | unsettled",
  post1949: {
    region: "string",
    place: "string",
    arrival: "string | null",
    livelihood: "string | null",
    companions: "string | null",
    leftBehind: "string | null",
    correspondence: "string | null",
    care: "string | null",
    legacy: "string | null",
    employment: {
      status: "not-started | seeking | casual | trial | employed | reduced-hours | retired",
      track: "manual | skilled | literate | care | null",
      role: "string | null",
      workplace: "string | null",
      duties: "string | null",
      terms: "string | null",
      startedYear: "number | null",
      attempts: "number",
      yearsWorked: "number",
      lastResult: "string | null",
      lastResultYear: "number | null",
      nextStep: "string | null",
      history: "EmploymentRecord[]"
    }
  },
  life: {
    status: "alive | dead",
    dangerSince: "number | null",
    naturalDeathAge: "number",
    deathOccurredYear: "number | null",
    deathConfirmedYear: "number | null",
    deathPlace: "string | null",
    cause: "string | null"
  }
}
```

`identity.place` 是出生地，`post1949.place` 是后半生实际落脚地，两者不得互相覆盖。场景、时代范围和死亡地点要读取当时实际处境，不能只读出生家庭或旧战时路径。

`employment.history[]` 是追加式事实记录，每条至少包含 `year / source / status / role / workplace / result / nextStep`。求职行动可以得到短工、试工、留用或落选，但不得只修改 `money / network / position` 而没有就业结果。旧存档缺失 `employment` 时只可迁移为 `seeking` 并公开说明记录缺口；不得为过去年份补造一份从未结算的工作。

### 具体生活 `lived`

```js
{
  lived: {
    parents: { mother: ParentLife, father: ParentLife },
    relationship: RelationshipLife,
    children: ChildLife[],
    career: CareerLife,
    health: { current, lastEpisode, history, treatedCount },
    social: { lastUpdate, history },
    inner: { current, history },
    yearHistory: LivedYearRecord[]
  }
}
```

`CareerLife` 至少保存 `role / workplace / employer / supervisor / colleague / publicPerson / duties / terms / history / transitions / retiredYear`；经营路线另存 `business.name / supplier / product / openedYear / lastActiveYear / ordersHandled / lastCustomer / lastProblem`。

`ParentLife` 保存姓名、职业、活动、话语、所在地、健康、死亡年与确认年。`RelationshipLife` 保存配偶姓名与职业、共同生活办法、争执、分居、迁移后的会合／异地、死亡与历史。`health.history` 必须用 `type` 区分 `episode / treatment / check-up`；无诊断检查的 `condition` 为 `null`。

`LivedYearRecord` 至少包含 `year / location / work / family / relationship / health / social / thought / decision`。`thought` 是角色当年可形成的第一人称判断，不得读取未来事件或他人未表达的心理。

### 时代近况 `eraHistory`

```js
{
  id: "stable-era-event-id",
  year: 1950,
  scope: "香港",
  title: "事件名称，或影响先于完整消息抵达",
  eventTitle: "内部稳定事件名",
  text: "角色当时能知道或感受的内容",
  known: true,
  source: { label: "来源名称", url: "https://..." }
}
```

史实定义同时保存 `year / eraScope / post1949Choices / routes / knownThrough / knownText / unknownText / historySource`。`currentEraUpdates[]` 只服务当年界面，`eraHistory[]` 跨年保存；不知情时 `source` 对玩家为 `null`，但内容数据中的 `historySource` 仍须存在，以便审校。

## 五、年度结算顺序

统一结算顺序如下，正式版不得因界面动画改变实际次序：

```text
1. 年初恢复精神并处理年龄阶段
2. 玩家安排当年行动
3. 结算行动数值、联系人、公共生活和信息渠道
4. 生成当年普通生活事件或路线节律
5. 触发与年份、实际地域和路径相符的时代事件：先生活冲击，后按渠道显示名称、背景和来源
6. 推进父母、配偶、子女、朋友、职业、疾病、公共经历和心理生活
7. 推进死亡、失联和确认等主体状态
8. 触发符合条件的关键抉择，并结算其他主体独立回应
9. 写入事实账本、年度叙事和具体生活记录
10. 记录年度曲线并完成跨年状态
11. 若主角尚未死亡，年份加一；1949 年只生成阶段里程碑并进入后半生，只有死亡发生与确认才生成一生终局
```

若同一年有多个关键抉择，按已发布内容表的稳定顺序每次只呈现一个，其余进入待处理队列，不得因界面只能放一张卡而跳过。普通生活与时代近况在抉择出现前已经完成当年结算。

## 六、确定性随机与可复现

- 每局创建一个整数 `seed`，显示在开局与导出档案中。
- 所有普通事件抽取和随机回应只能调用局内伪随机序列。
- 相同版本、种子、身份与选择序列必须得到相同事实账本。
- 内容版本变化允许改变未来结算，但读取旧档时必须保留原版本号并提示迁移。
- 自动测试使用固定种子，不以人工多刷得到的最好结果作为平衡证据。

## 七、存档与迁移

目标文字版存档至少包含：

```js
{
  format: "minguo-life-save",
  schemaVersion: 5,
  gameVersion: "0.8.0",
  state: {}
}
```

v0.7.0 的 schema 4 是必须兼容的导入格式，不是新内容继续写入的目标格式。正式产品发布前应在保持迁移能力的前提下增加 `exportedAt` 与 `checksum`。

### 迁移规则

1. 新字段必须有安全默认值。
2. 缺失主体或联系人时从出生家庭模板补入，但不能伪造过去互动历史。
3. 旧事实 id 不得在迁移时改写。
4. 无法安全推断的状态设为 `unknown`，并记录迁移说明。
5. 未来正式版应加入校验和与存档尺寸上限；文字版导入上限为 `2 MB`。
6. v0.6 及更早存档缺少 `publicLife` 时，只能补为“没有参加公共网络”；不得反推旧年份未记录的参与身份。
7. v0.7 存档中的 `ccp / kmt`、`infiltration` 或 `coerced-cooperation` 只作为旧版事实文本保留，并标记 `legacyUnaligned: true`；迁移不得把它们继续转换为可玩的真实组织身份。

## 八、平衡目标

平衡不要求十一条路径收入相同或风险相同，而要求：

- 每条路径每个年龄阶段都有至少一种有意义的主动行动。
- 贫困出身的选择较窄，但不存在连续多年“只有等待”。
- 富裕出身拥有更多缓冲，同时承担更多家业、雇员和关系责任。
- 没有单一属性成为全路线最优解。
- 每条路径至少有一次资源之间的真实取舍。
- 婚姻、生育、照料与公共责任都允许拒绝，不作为高分必选项。
- 结局丰富度来自事实组合，不来自隐藏总分。

正式版数值调优使用三类测试人格：

1. **专精型**：长期重复一种能力建设，验证递减与路径局限。
2. **照料型**：优先家庭与联系人，验证其不会被系统惩罚成“低价值人生”。
3. **流动型**：频繁换路和迁徙，验证代价存在但仍能闭环。

## 九、发布验收门槛

- 三个家庭与十一条路径都能跨过 1949，并在主角死亡发生与确认后结束。
- 六种 1949 年后地域去向都能进入，且地域时代事件不会串入其他落点。
- 同种子同选择得到相同事实账本。
- 每个年度都有一条年度生活叙事。
- 每条路径至少拥有三条路线专属普通事件。
- 每条路径至少拥有两次专属关键抉择和两项专属重复行动。
- 每个路线选项都必须写入 `echo`，并在后续年份保证触发至少一条生活回响。
- 每条标准人生至少发生十次关键抉择、两次路线抉择和四次选择回响。
- 三个家庭各有至少三名具名持久联系人。
- 十一条路径都有具体职业现场；标准人生至少六名具名社会人物、四次疾病发作和逐年心理记录。
- 父母、配偶、子女、经营结束和晚年停止固定工作均有状态事实，不由终局临时补写。
- 婚姻、生育、照料和公共责任均有非默认选择。
- 史实层与合成个人层分开；不加入、退出、公开服务、谨慎联络、家庭知情与问话压力均有相容可达人生，且不存在加入真实党派、潜入真实机构或选择指认对象的交互。
- 存档可本地恢复、导出并重新导入。
- 终局只引用已经写入的事实，不含价值排名。
- 所有迁移、路线和终局自动测试通过。

当前文字版 `v0.7.0` 使用 schema 4，仍含本版已经废弃的真实党派与潜入字段，只能作为迁移输入，不能作为本契约的可执行参考。目标文字版 `v0.8.0` 使用 schema 5 并完成上述迁移。原静态数量只作为内容改造前的覆盖参考，不能证明新公共参与设计已经实现。
