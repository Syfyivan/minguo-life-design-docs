# 整局主链系统看板

> 本看板只整理当前 `demo.html` / `README.md` / `设计规范-分支节点.md` 已真实落地的主家庭树、主路径、共享路径推进结果，不补写不存在的剧情、制度细节或史实。凡运行时仍未确认的地方，一律继续保留 `unknown`。
>
> 当前建议先用 `window.__DEMO.inspectWholeGameProgressBundle()` 判断“这一步是否真的让整局更完整”；它会把 `inspectGlobalClosureChecklist()`、`inspectWholeGameTemplateMigrationBoard()`、`inspectWholeGameMainlineBlueprint()`、`inspectWholeGameCompletionLedger()` 的关键结论压成一份整局汇总。若这份汇总已经确认 `wholeGameStageLabel = 整局模板收口阶段`，再按它指出的前沿层，去读 `window.__DEMO.inspectGlobalMainlineBoard()`、`window.__DEMO.inspectTopRouteLocalCandidateBundle()` 或对应单表。`window.__DEMO.inspectGlobalClosureActionBoard()` 仍适合回答“这一行该补阶段还是该做对照”，但它把同属 `优先比较 route-local 证据` 的行先按粗粒度动作桶分组，再按标签打散，不适合单独承担同组内的细排位。

## 用法

- 运行时入口：
  - 先看 `window.__DEMO.inspectWholeGameProgressBundle()`，先把 `整局阶段 / 模板迁移前沿 / 主链蓝图前沿 / 完工账本硬指标 / 尾部 route-local` 用同一份预计算结果读出来
  - 若整局汇总显示真正前沿在 `模板迁移`，再下钻 `window.__DEMO.inspectWholeGameTemplateMigrationBoard()`
  - 若整局汇总显示真正前沿在 `主链蓝图` 或 `整局总账`，再下钻 `window.__DEMO.inspectWholeGameMainlineBlueprint()` / `window.__DEMO.inspectWholeGameCompletionLedger()`
  - 只有当整局汇总明确把问题收紧到尾部 `route-local` 时，再用 `window.__DEMO.inspectRouteLocalCandidatePriority()` / `window.__DEMO.inspectRouteLocalCandidateBlockers()` 抓当前 **route-local** 前沿与“为什么还不能升格”的阻塞口径
  - `window.__DEMO.inspectGlobalClosureActionBoard()` / `window.__DEMO.inspectRouteLocalDecisionBoard()` 负责把“要先补阶段还是先做对照”翻成行动板，但在 Node 侧重聚合较慢时，不要用它们来替代“候选优先级/阻塞矩阵”的细排位
- 回归入口：`WHOLE_GAME_PROGRESS_BUNDLE_SMOKE=1 node demo-regression.mjs` 可先轻量读取整局汇总；若仍需更细，再分别使用对应单表 smoke
- 这张看板只回答三件事：
  - 哪一整棵树/哪一条主路径，当前最值得继续推进
  - 当前推进应归到 `优先补整局系统 / 先收共享主路径借入 unknown`
  - 哪些前沿仍应继续保留 `route-local` 或 `unknown`

## 当前推进顺序

- 第一步先看 `主链闭环模板.md` 与 `window.__DEMO.inspectWholeGameProgressBundle()`：先确认这一步是在迁移 `状态字段 / 规则层 / 节点模式 / 验证方式`，还是只是在某一枝上继续长句子。
- 第二步先处理“最后一格是否已该冻结”为保守尾项：当前轻量直读已确认 `window.__DEMO.inspectTopRouteLocalCandidateBundle()` 对 `江南沈家·女线：婚家 + 外部门路` 返回 `tailFreezeSuggested = true`、`matchedCandidateMissingCount = 4`、`familyComboPeerRouteCount = 0`。这说明整局剩余问题不再是“继续把这格当 active route-local 比库存”，而是应把它降成“当前无第二路线证据”的保守尾项。
- 第三步再用 `window.__DEMO.inspectTopRouteLocalCandidateBundle()` 只守两件事：现有可对照路线与样例仍为 `上海工商家·男线 / 上海工商家·女线 / 苏北贫农家·士兵多年线`，且当前证据阶段仍只到 `compare-only`（`exact = 0 / skeleton = 0`）；在未来出现新的真实 `peer route` 之前，不再为清零去新编 marriage-only late-line。
- 若 bundle 明确给出 `blocker = shared-family-combo-template-but-local-detail`，下一步应直接读 `window.__DEMO.inspectTensionFamilyComboDetailCoverage()`：先确认这格候选究竟是“粗主家庭模板已跨路线、但 exact 细主体组合仍只在单路线”，还是“细主体组合本身也已拿到第二路线，只是比较器还没吸进去”。在这一步之前，不为清零去新编 marriage-only late-line。
- 依据本轮新增的低成本直读，`江南沈家·女线：婚家 + 外部门路` 当前在 `inspectTensionFamilyComboDetailCoverage()` 里返回的是 `主家庭比较模板与细主体组合都仍单路线`，`peerRoutes = 0`。这说明现有 `上海工商家 / 苏北士兵` 更多是**可比较对照位**，还不是这组 exact combo 的第二路线证据；当前正确动作仍是保守记录，而不是补一条人造对称枝。
- 第四步把上述三条路线继续视为**对照库存位**，而不是“继续叠同类城市/苏北 mixed 的默认主战场”：下一轮优先只核对它们与 `婚家 + 外部门路` 共享的 HUD 键与主体组合、以及为什么仍只到 `compare-only`，而不是为了清零去新编 marriage-only late-line。
- 第五步把 `纱厂女工·主路径`、`苏北贫农家·留乡主分流`、`苏北逃亡回乡线`、`难民/流亡者·主路径`、`共享海外多年轨` 继续作为第二来源/共享样板/模板库存位；除非 `inspectTopRouteLocalCandidateBundle()` 或 `inspectRouteLocalCandidatePriority()` 显式把它们抬回 active，否则不要把它们误写成当前前排。
- 第六步继续把 `苏北留乡主分流` 的两项结构化 unknown 只当模板库存使用，不再误算成 active 任务。
- 只要一轮改动不能让某一整棵主树、整条主路径或整组主系统更完整，就不应优先投入。

## 当前总览

| 行 | 当前优先 | 当前焦点 | 备注 |
| --- | --- | --- | --- |
| `江南沈家·女线` | `保守尾项（当前无第二路线证据）` | `婚家 + 外部门路` | 轻量直读已确认 `tailFreezeSuggested = true`、`matchedCandidateMissingCount = 4`、`familyComboPeerRouteCount = 0`。这格现在应记为“现有库存未给出第二路线细签名”，不再作为默认 active 前沿；只有未来出现新的真实 `peer route` 时再重开 |
| `苏北贫农家·士兵多年线` | `对照库存位（compare-ready）` | `妻子/病母 + 安身主体 + 外部门路` | 以第 `259` 轮 `inspectTopRouteLocalCandidateBundle()` 为准：它当前已进入可直接对照列表（样例 id 见推进记录）；后续优先拿它去核对 “共享 HUD 键/主体细组合命中差” |
| `难民/流亡者·主路径` | `共享回应原型来源位` | `返乡后两头并行`、`后方屋檐 + 脚程活路` | 本轮不再把 `旧底盘接回后，两头活路并着顶上来` 另留成单路线回应原型；它已并回跨 `苏北士兵 / 苏北逃亡 / 难民` 的共享回应 archetype，因此当前主要作用是充当第二来源模板位 |
| `上海工商家·主家庭树` | `对照库存位（compare-ready）` | `父辈/家门 + 安身主体/工友/家口 + 外部门路`（男线）、`婚家 + 安身主体/工友/家口`（女线）等 | 以第 `259` 轮 `inspectTopRouteLocalCandidateBundle()` 为准：`上海工商家·男线 / 女线` 当前已进入可直接对照列表（样例 id 见推进记录）；用途是核对与沈家女线共享的 HUD 键/主体组合，以及为什么仍卡在 `compare-only` |
| `纱厂女工·主路径` | `第二路线库存位` | `工友/家口 + 外部门路` | 当前继续作为校准库存位存在；只有当它能提供 `婚家 + 外部门路` 这类更细的第二路线证据时，才值得重新升回 active 前沿 |
| `苏北贫农家·主家庭树` | `成熟样板承接位` | `留乡主分流`、`shared unknown` 库存 | 苏北整棵树仍是成熟样板承接面；但需要把 `士兵多年线` 从总树中单独抬出来看，不能再用“苏北整体已成熟”把它误压回库存 |
| `苏北贫农家·留乡主分流` | `当前无新增总表前沿` | `名额后账回应边界`、`名额转嫁承接边界`（已转入模板库存） | 真实运行时里，`inspectSubeiStayUnknownAtlas()` 已返回 `4/4` 分流双层带出两组边界；本轮又把 `inspectRouteLocalCandidatePriority()` 中这两项改记为 `已转入模板库存`，不再占当前整局前沿 |
| `苏北贫农家·主家庭树` | `当前无新增总表前沿` | `留乡主分流的名额后账回应边界 / 名额转嫁承接边界` | 苏北 shared bucket 已成熟；当前真正还会改变整局排序的，只剩留乡主分流那两组子字段，不宜再把整棵苏北树当默认主战场 |
| `苏北贫农家·逃亡回乡线` | `当前无新增总表前沿` | `return-route-landing / workteam-recognition-boundary / draft-parameter-boundary`（已成熟） | 当前更像共享模板承接面，而不是新前沿来源 |
| `共享海外多年轨` | `当前无新增总表前沿` | `return-route-landing` 晚段回接模板（已成熟） | 继续服务 `江南沈家 / 上海工商家` 借入路线，但自身暂不再作为优先行 |

## 本轮验证注记

- 本看板的“当前前沿/对照位”口径，当前以本轮轻量直读与 `持续推进记录.md` 的最新可信口径合并为准：
  - 尾部候选：`江南沈家·女线 / 主体牵制原型 / 婚家 + 外部门路`
  - 当前阻塞：`同路线已挂上共享主家庭比较模板，但更细的安身/账房/工友组合仍是 route-local`
  - 当前尾项结论：`tailFreezeSuggested = true`，可固定为“当前无第二路线证据”的保守尾项
  - 当前可直接对照的路线与样例：`上海工商家·男线`、`上海工商家·女线`、`苏北贫农家·士兵多年线`（样例 id 详见第 `259` 轮记录）
- 本次巡检任务不重跑 `demo-regression.mjs`；仅同步“看板排序/剩余主链路”表述，避免看板继续沿用旧的对照位口径。
- 当前未重新把 `inspectGlobalMainlineBoard()` 当作细排位事实来源；因为这次巡检只需要“当前前沿是否漂移”与“剩余主链路怎么排”，因此本看板继续以 `route-local 候选优先级/阻塞矩阵` 为准，`inspectGlobalClosureActionBoard()` 只保留为动作桶辅助口径。

## 读表规则

- `优先补整局系统`：只在这行仍挂着**待推进**的跨路线 `unknown` 或系统边界时使用；若某组 structured unknown 已 `allAxesReady`，它继续留在模板库存里，但不再占当前前沿。
- `先去共享主路径收回借入 unknown`：这行自身并不是缺树体，而是在借共享路径的晚段余账；应优先补共享路径。
- `暂保留 route-local，先比较证据`：shared bucket 已成熟，当前真正还会改变全局排序的，是仍只在单路线成立、但已挂上共享锚点的候选组合或子字段。
- `保守尾项`：当前已有共享主家庭/锚点骨架可比较，但 `matchedCandidateMissingCount > 0` 且 `familyComboPeerRouteCount = 0`；这类尾项不再计作默认前沿，只在未来出现新的真实 `peer route` 时重开。
- `当前无新增总表前沿`：这行当前只剩成熟模板库存，没有新的整局级缺口；除非出现新的主路径证据，否则不优先回头修边。

## 当前不该做的事

- 不再围着 `苏北贫农家` 单一闭合节点反复修边，但也不因它是成熟样板就忽略 `苏北士兵多年线` 仍真实报错的代表态/HUD 对齐问题。
- 不把 `江南沈家 / 上海工商家` 的代表态/HUD/总账对齐问题误算成“再加一层 route-local 模板名字就能解决”。
- 不在 `主体牵制支撑簇 / 安身簇子类 / 锚点门路骨架 / 主家庭比较模板` 这条已经连续推进了四轮的成熟梯子上继续加新中层，除非先拿到第二条真实路线或第二类共享来源，或先解决当前回归失败清单里的事实键缺口。
- 不把 `上海工商家`、`江南沈家`、`女工` 当前仍只在单路线成立的主体牵制组合，硬抬成跨线共模。
- 不把制度细数、具体资产明细、具体承接人身份从 `unknown` 补成“看起来更完整”的伪设定。

## 本轮建议

1. 先以 `江南沈家·女线` 为当前唯一 active 候选，但不去新编 marriage-only 剧情；优先只核对 `inspectTopRouteLocalCandidateBundle()` 已列出的对照位（第 `259` 轮：`上海工商家·男线 / 上海工商家·女线 / 苏北贫农家·士兵多年线`），判断它们与 `婚家 + 外部门路` 共享的 HUD 键与主体组合到底差在哪。
2. 若核对后仍只能得到“可比较但不够同键/同骨架双层对照”，就继续明确 keep `route-local`；此时不要为了清零而回到成熟模板附近继续叠同类 mixed，也不要硬找不存在的 marriage-only 细组合。
3. `难民/流亡者·主路径` 与 `苏北士兵 / 苏北逃亡` 当前已完成这轮回应原型收拢；后续主要承担共享来源模板位，不继续单独叠同类 late-home mixed。
4. `苏北留乡主分流` 的两项结构化 unknown 继续留在模板库存，不回头当 active 前沿。
